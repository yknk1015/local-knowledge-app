# KnowledgeApp リリース前作業計画書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | KnowledgeApp リリース前作業計画書 |
| 作成日 | 2026-08-16 |
| 対象 | 正式文書で定義したMVPを本格利用へ移行するまでの残作業 |
| 対象版 | 現行開発版 `0.3.3` 以降の本格利用候補版 |
| 正式な参照文書 | `FAQシステム要件定義書.md`、`FAQシステム基本設計書.md`、`ローカルFAQデータ・Git除外詳細設計書.md` |
| 位置づけ | 一時的な進行管理文書。要件・設計の正本ではない |

> **本書の削除について**
> 本書に記載した全作業が完了し、完了条件と最終リリース判定を満たした後は、この`リリース前作業計画書.md`を削除してよい。削除後も、恒久的に残すべき仕様、設計、テスト結果、既知事項、運用手順は、正式な設計書、README、リリースノートまたはテスト記録へ反映しておくこと。

## 2. 目的

現在実装済みのFAQ CRUD、分類管理、リッチテキスト、認証、利用者管理、CSV一括編集、バックアップ・復元、Codex連携等を維持しながら、正式文書でMVP必須とされた未実装機能とリリース上の懸念を解消し、本格利用開始の可否を客観的に判断できる状態にする。

## 3. 基本方針

1. 利用者のFAQデータを最優先で保護し、既存データを使った試験の前に必ずフルバックアップを作成する。
2. 要件定義書で「必須」とされた機能は、正式に要件を変更しない限り未完了のままリリースしない。
3. 文書と実装の差異は、実装だけまたは文書だけを変更して残さない。
4. 各作業では、Rust側検証、画面側検証、自動テスト、失敗時のデータ保護を一組として実施する。
5. 利用者データ、FAQ CSV、バックアップ、Codex提案・委譲をGitやリリースへ含めない。
6. 将来機能と明記された自動バックアップ、分析画面、お気に入り、解決率、複数人同時利用、C#移行は、本計画の完了条件へ含めない。

## 4. リリース判定の概要

次の条件をすべて満たした場合だけ、本格利用候補版をリリース可とする。

- [ ] 本計画の「必須」作業がすべて完了している。
- [ ] 要件定義書、基本設計書、詳細設計書、READMEと実装が一致している。
- [ ] 既存DBの移行前バックアップと単一起動制御が実装・検証されている。
- [ ] 日本語検索、同義語、検索範囲、FAQ検索情報、関連FAQ、履歴、外部資料参照、JSON入出力が要件どおり利用できる。
- [ ] 自動テスト、型検査、lint、ビルド、文字コード検査、Git混入防止検査がすべて成功している。
- [ ] クリーンなWindows 11環境で新規インストール、上書き更新、バックアップ復元、アンインストールを確認している。
- [ ] 実データ相当件数で検索性能と画面操作性を確認している。
- [ ] 会社PCへ配布する場合、公開範囲、コード署名、保存場所が会社規定を満たしている。
- [ ] 既知の重大・高重要度不具合が0件である。

## 5. 作業順序と依存関係

```text
現状固定・バックアップ
  ↓
データ保護と単一起動
  ↓
必須機能の実装
  ↓
統合試験・性能試験
  ↓
文書・配布物整備
  ↓
Windows実機リリースリハーサル
  ↓
最終判定・本書削除
```

後続工程で手戻りを増やさないため、フェーズ0～2を完了する前に正式リリースを作成しない。

## 6. 作業計画

### フェーズ0：現状固定と作業範囲の確定

#### REL-PLAN-001 作業ツリーの整理【必須】

- [x] 変更52件、うち未追跡13件の内容を一件ずつ確認する。
- [x] DB第5～7版マイグレーション、認証、CSV処理、画面、テスト、マスコット素材が意図した変更であることを確認する。
- [x] 利用者データや実FAQ由来の内容が含まれていないことを確認する。
- [x] 変更を機能単位でコミットし、別環境で同じソースを取得できる状態にする。

実施記録：2026-08-16、基準コミット`ca6459bf75db1cd1846d9863a449a19ceb2667e3`をOS一時フォルダの独立worktreeへ取得し、React 64件、Rust 54件、型検査・ビルド、文字コード検査、利用者データ混入防止検査を再実行してすべて成功した。

完了条件：`git status`の残件が意図したものだけで、クリーンな取得環境からテストとビルドを再現できる。

#### REL-PLAN-002 MVP範囲の再確認【必須】

- [x] 要件定義書の必須要件と現実装の対応表を作る。
- [x] JSON入出力など、要件定義書ではMVP必須、基本設計書では将来扱いとなっている項目を解消する。
- [x] 実装する場合は本計画の必須作業として維持する。
- [x] リリース対象外へ変更する場合は、利用者承認後に要件定義書・基本設計書・詳細設計書を同じ作業で更新する。（今回は対象外変更なし）

確定記録：`MVP要件実装対応表.md`を作成し、JSON入出力は要件を縮小せずMVP必須として実装する方針へ統一した。詳細設計書はJSON等をGit除外対象とするデータ分離設計であり、機能の優先区分を定義していないため変更不要と確認した。

完了条件：MVPの必須・推奨・将来の区分が3文書間で一致している。

#### REL-PLAN-003 検証用データと退避【必須】

- [x] 本番候補データを直接テストへ使用する前にフルバックアップを作成する。（現時点では本番候補データを使用せず、実地リハーサル開始直前に実施する）
- [x] 自動テストはOS一時フォルダの開発用フィクスチャーを使用する。
- [x] 会社情報を含むFAQを外部サービスやGitへ送らない。

実施記録：基準確認と自動テストではOS一時フォルダの生成データだけを使用し、利用者データルートと会社FAQを読み取っていない。本番候補データのバックアップ保存先・作成日時・復元結果は、フェーズ4のリリース試験記録へ記載する。

完了条件：復元可能なバックアップの保存先、作成日時、検証結果が記録されている。

### フェーズ1：データ保護・起動制御の完成

#### REL-DATA-001 DB移行前自動バックアップ【必須・リリース阻止】

- [x] 起動時にDB版を確認し、移行が必要な場合は変更前DBの整合したバックアップを作成する。
- [x] バックアップ成功前にマイグレーションを開始しない。
- [x] バックアップ失敗時は移行と通常起動を停止し、原因と対処を日本語で表示する。
- [x] 第1～6版から第7版以降への段階移行を確認する。
- [x] 各移行途中の失敗、再起動、再試行で既存FAQを失わないことを確認する。
- [x] 旧バックアップ復元後にも同じ保護を適用する。

実装記録：読取専用の版・整合性確認、移行前`.faqbackup`作成、失敗時のネイティブエラー表示、各マイグレーションのトランザクション化を実装した。OS一時フォルダの第1～6版DB、バックアップ不能、意図的な移行失敗、旧バックアップ復元を自動試験で確認した。

完了条件：移行成功、移行失敗、バックアップ失敗、再試行の自動テストと実機試験が合格する。

#### REL-DATA-002 単一起動制御【必須・リリース阻止】

- [x] 同じ利用者データルートを使用するKnowledgeAppを二重起動できないようにする。
- [x] 2回目の起動時は既存ウィンドウを前面へ出すか、既に起動中である旨を表示して終了する。
- [ ] 二重起動試験でFAQ、設定、バックアップ処理が競合しないことを確認する。
- [x] 異常終了後も次回起動を恒久的に妨げない方式を採用する。

実装記録：Tauri Single Instanceプラグインを最初に登録し、2回目の起動では既存`main`ウィンドウを復元・表示・フォーカスする。二重起動と異常終了後再起動のWindows実機試験はフェーズ4で行う。

完了条件：通常起動、二重起動、異常終了後再起動の実機試験が合格する。

#### REL-DATA-003 バックアップ事前検査の補完【必須】

- [x] 保存開始前に保存先への書込み可否を一時ファイルで確認する。
- [x] 必要容量と保存先の空き容量を確認し、不足時は作成開始前に停止する。
- [ ] ローカル、割当済みネットワークドライブ、UNCパスで確認する。
- [ ] 切断、権限不足、容量不足時に元データと既存バックアップを変更しない。
- [x] `.partial`と既存ファイル退避の回復処理を再確認する。

実装記録：書込み・同期・削除の事前検査、保存先と利用者データ側一時フォルダの空き容量検査、コピー失敗時の`.partial`削除、検証失敗時の既存ファイル復帰を実装・自動試験した。ネットワーク経路と障害注入の実機試験はフェーズ4で行う。

完了条件：正常、権限不足、容量不足、通信切断、同名上書きの各試験が合格する。

### フェーズ2：正式MVP必須機能の完成

#### REL-FAQ-001 FAQ検索情報の編集・保存【必須】

- [x] 症状、想定原因、対象OS・製品・機種、エラーコード、対応手順、注意事項を複数登録できる。
- [x] タグとFAQ固有の検索用語を追加・削除できる。
- [x] 保存・再編集・複製・CSV・バックアップ・復元後も内容を維持する。
- [x] 同一FAQ内の正規化重複を画面側とRust側で防ぐ。
- [x] FAQ詳細画面へ要件で定めた順序で表示する。

実装記録：編集画面の複数値入力、Rust側の件数・長さ・正規化重複検査、子テーブル・検索索引への保存、複製・CSV・JSON・詳細表示を実装し、自動試験を追加した。フルバックアップはDB全体を保持するため同項目も復元対象となる。

完了条件：各項目のCRUD、重複拒否、検索反映、復元を自動テストで確認する。

#### REL-FAQ-002 関連FAQ【必須】

- [x] 編集画面で削除されていないFAQを複数選択できる。
- [x] 自己参照と重複参照をRust側で拒否する。
- [x] 一方向登録でも双方の詳細画面から関連を確認できる。
- [x] 関連FAQを選択すると正しい詳細へ移動できる。
- [x] 削除、復元、統合済みとの表示整合を確認する。

実装記録：関係をUUIDの正規順による1行へ保存し、編集候補・相互表示・詳細遷移・削除済み・統合済み表示と保存境界を試験した。

完了条件：登録・解除・相互表示・自己参照拒否・論理削除境界の試験が合格する。

#### REL-SRCH-001 検索範囲【必須】

- [x] 「現在地以下」「現在地のみ」「すべて」を選択できる。
- [x] 仮想ルートでの「現在地以下」を全分類として扱う。
- [x] 検索範囲をURLへ保持し、FAQ詳細から戻った際に復元する。
- [x] 条件初期化で既定値へ戻す。

実装記録：再帰CTEによる配下、直下ID一致、全分類の3範囲を実装し、URL保持・初期化と分類境界を自動試験した。

完了条件：1～5階層の境界を使った検索範囲試験が合格する。

#### REL-SRCH-002 日本語検索・順位付け【必須・リリース阻止】

- [x] NFKC、大小文字、全角・半角、空白、記号を共通正規化する。
- [x] 日本語自然文を語またはn-gramへ分解する。
- [x] FTS5 trigramと短い語の補助検索を実装する。
- [x] タイトル、概要、本文、症状、原因、対象、エラーコード、タグ、検索用語を横断検索する。
- [x] タイトル・エラーコード完全一致、症状、タグ等の項目別重みを適用する。
- [x] 複数語一致を一語一致より優先する。
- [x] 検索結果へ最大3件の一致理由を表示する。
- [x] 更新日・重要度並び替えと50件ページングを順位処理後に適用する。

実装記録：NFKC・小文字・空白正規化と記号境界、日本語n-gram、FTS5 trigram候補、短語補助、項目別重み、複数語加点、最大3理由、順位後の並び替え・ページングを実装した。大規模件数の時間測定はフェーズ4で行う。

完了条件：要件定義書の検索受入条件と、実データ相当の代表検索ケースが合格する。

#### REL-SYN-001 同義語辞書【必須】

- [x] 代表語と複数同義語を登録・編集・削除できる。
- [x] 正規化重複を統合し、別グループ競合を警告する。
- [x] 保存直後の次回検索から展開を反映する。
- [x] 「PC／パソコン」等の双方向検索を確認する。
- [x] JSON入出力をMVPへ含めるか正式文書に従って確定する。

実装記録：同義語管理画面、正規化統合、競合確認付き保存、検索時の双方向展開、全体JSONへの同義語グループ収録を実装・試験した。

完了条件：辞書CRUD、重複、競合、検索展開の自動テストが合格する。

#### REL-LOG-001 検索・閲覧履歴【必須】

- [x] 検索日時、原文、正規化文、範囲、分類、件数を検索実行時だけ記録する。
- [x] FAQ詳細表示時に閲覧履歴と遷移元検索を記録する。
- [x] 検索履歴、0件検索、閲覧履歴を画面で確認できる。
- [x] 期間、文字列、ページングで絞り込める。
- [x] 期間指定または全件削除を確認付きで実行できる。
- [x] 履歴をフルバックアップ・復元で維持する。

実装記録：検索送信時と詳細表示時だけ記録し、3タブ、期間・文字列・50件ページング、削除対象件数と期間の確認、期間・全件削除を実装した。履歴を含むフルバックアップ復元試験を追加した。

完了条件：記録、0件抽出、削除、無期限保持、バックアップ復元の試験が合格する。

#### REL-REF-001 外部資料参照【必須・セキュリティ重点】

- [x] ローカルパス、割り当て済みネットワークドライブ、UNCパスをコピー用テキスト枠へ保存できる。
- [x] コピー時に改行、空白、円記号を変えず、クリップボードへプレーンテキストだけを書き込む。
- [x] コピー用参照先を自動オープン・自動実行せず、存在確認やパス補完を行わない。
- [x] Web上の外部資料はHTTP/HTTPS参考URLだけを許可し、表示文字、接続先ホスト名、完全なURLを確認後に既定ブラウザーで開く。
- [x] `file:`、`javascript:`、`data:`、認証情報入りURLをクリック可能にしない。
- [x] 外部資料本体をアプリ管理フォルダ、FAQ検索索引、フルバックアップ、Gitリポジトリへ取り込まない。
- [ ] 会社環境の実パスとHTTP/HTTPS手順書を使い、コピー、貼り付け、ブラウザー表示、権限不足、リンク切れ時もFAQ閲覧を継続できることを手動確認する。
- [ ] 外部資料本体の権限、更新、安全性、可用性、バックアップが会社側の運用手順に記載されている。

完了条件：自動テストに加え、会社環境相当のパスとURLで安全境界・運用責任の手動確認が合格する。

#### REL-JSON-001 JSONエクスポート・インポート【必須。ただし範囲変更時は文書改訂】

- [x] 固定拡張子`*.knowledge-export.json`と形式版を定義する。
- [x] FAQ、分類、タグ、同義語、関連情報を出力する。
- [x] 画像・外部資料本体を含めず、FAQ本文内のコピー用参照先とHTTP/HTTPS参考URLを維持する。
- [x] インポート前に形式、必須項目、ID衝突、追加・更新・スキップ件数を検証・表示する。
- [x] 事前バックアップ後、全体を1トランザクションで反映する。
- [x] Git管理フォルダ内の入出力を拒否する。
- [x] 不正JSON、未知版、危険なリッチテキスト、部分失敗を拒否する。

実装記録：UTF-8・形式版1の厳格JSON、100MB上限、種別件数・新規・更新・変更なし・エラーのプレビュー、SHA-256差替え検知、取込前フルバックアップ、単一トランザクションを実装した。正常往復、形式版0・未知版、破損、ID衝突、危険ノード、途中失敗ロールバック、Git配下拒否を自動試験した。

完了条件：正常往復、旧形式、破損、衝突、途中失敗、Git配下拒否の試験が合格する。

### フェーズ3：既実装機能の本格利用向け再検証

#### REL-REG-001 FAQ・分類・リッチテキスト回帰【必須】

- [x] 5階層制限、循環、同一親同名、空分類削除を確認する。
- [x] FAQ作成、編集、複製、下書き、公開、廃止、論理削除、復元を確認する。
- [x] 表、画像、参考URL、コピー用テキスト枠の保存・再編集・複製を確認する。
- [x] 危険URL、任意HTML、外部画像、Base64、未知ノードを拒否する。
- [x] 新着・更新期限、非表示、統合済みの検索境界を確認する。

実装記録：分類正規化重複、FAQ状態遷移と監査、複製画面操作、画像の独立複製、許可リッチテキスト、貼り付けHTMLの実行要素・イベント属性・外部画像除去を自動試験した。検索一覧から詳細、編集、保存後の詳細へ遷移する機能横断画面試験を追加した。

完了条件：既存の自動テストに加え、機能横断の画面結合試験が合格する。

#### REL-REG-002 認証・利用者・監査【必須】

- [x] 未ログイン時に保護コマンドを実行できない。
- [x] 管理者と一般利用者の画面・Command権限を確認する。
- [x] 本人と最後の有効管理者を利用停止にできない。
- [x] 空パスワード許可ON/OFFの境界を確認する。
- [x] FAQ作成者・更新者が手動編集、CSV、Codex修正、削除・復元で正しく更新される。
- [x] パスワードハッシュがCSV、Codex委譲、ログへ含まれない。

実装記録：`get_settings`を含む保護CommandをRustのインメモリセッション検査へ統一し、ログイン後だけ表示設定を取得するようにした。認証・管理者ガード、本人・最終管理者保護、空パスワード境界、手動・CSV・Codex・削除・復元の作成者／更新者、CSV・全体JSON・Codex委譲・履歴へのパスワードハッシュ非流出を自動試験した。

完了条件：認証・権限・監査のRust結合試験と画面試験が合格する。

#### REL-REG-003 CSV一括編集【必須】

- [x] UTF-8 BOM、CRLF、固定列順、形式版2、管理ID、内部UUID非出力を確認する。
- [x] Excel数式注入対策と再取込時の復元を確認する。
- [x] 未変更本文のTiptap構造保持と、変更本文の安全な段落置換を確認する。
- [x] 旧版更新警告、全行エラー時の全件拒否、SHA-256差替え検知を確認する。
- [x] 取込直前バックアップと一括トランザクションを確認する。
- [x] `99999`超の管理ID拡張を確認する。

実装記録：BOM・全改行CRLF・17列・形式版2・管理ID・UUID非出力、数式セル往復、表を含む未変更本文の保持、変更本文の安全な段落化、旧版警告、全行エラー拒否、SHA差替え拒否、検証済み直前バックアップ、複数行の一括ロールバック、6桁管理IDをRust試験で確認した。実Excelによる操作確認はフェーズ4のWindows実機試験で行う。

完了条件：Excelでの書出し・編集・プレビュー・取込を含む実機試験が合格する。

#### REL-REG-004 Codex連携【必須】

- [x] 分類カタログへFAQ本文・件数・履歴・添付を含めない。
- [x] 新規、修正1件、統合2～10件、版競合、画像参照維持を確認する。
- [x] 承認、却下、最新却下案だけの再検討、受付番号重複防止を確認する。
- [x] 統合公開、新着期限、統合済み化、解除、統合先保護を確認する。
- [x] CodexがSQLite、WAL、SHM、未選択FAQを直接読み書きしない。

実装記録：Rustの提案形式・分類カタログ・委譲一致・版競合・履歴・統合試験、Reactの提案承認・再検討・競合表示・統合公開試験に加え、プラグイン試験でDB、WAL、SHM、フォルダー列挙による未選択FAQの直接探索を静的に拒否した。

完了条件：プラグイン試験、Rust結合試験、画面試験がすべて合格する。

### フェーズ4：品質・性能・配布検証

#### REL-QA-001 自動品質検査【必須】

- [x] `npm test`
- [x] `npm run build`
- [x] `cargo fmt --manifest-path src-tauri/Cargo.toml --check`
- [x] `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
- [x] `cargo test --manifest-path src-tauri/Cargo.toml --locked`
- [x] `scripts/check-text-encoding.ps1`
- [x] `scripts/check-no-runtime-data.ps1`
- [x] `scripts/test-codex-plugin.ps1`
- [x] `git diff --check`

実施記録：2026-08-16に全項目が合格した。Reactは83件、Rust通常試験は78件が合格し、通常試験で意図的に除外した性能試験1件はリリースモードで別途合格した。MSVCリンカーの`.lib`・`.exp`作成メッセージはWindows DLL生成時の通常出力であり、警告ではないことを確認した。

完了条件：警告を含め、原因未確認の失敗が0件である。

#### REL-QA-002 検索・データ量性能【必須】

- [x] FAQ 10,000件、分類1,000件の開発用フィクスチャーをOS一時フォルダへ生成する。
- [x] 通常検索が原則1秒以内、FAQ詳細が原則0.5秒以内であることを計測する。
- [x] ページング、並び替え、同義語展開、履歴100万件相当の境界を確認する。
- [x] CSV 10,000件処理と約199MBのDBバックアップ容量・処理時間を確認する。
- [ ] 大容量CSV・バックアップ実行中の画面フリーズの有無を専用Windows利用者環境で確認する。
- [x] 採番値10万件で6桁管理IDへ自然拡張し、処理を継続できることを確認する。
- [ ] 6桁管理IDの実画面表示幅を専用Windows利用者環境で確認する。

実施記録：検索56ms、詳細1ms未満、分類8ms、履歴100万件のページ・条件付き件数582ms、CSV 10,000件50ms、DBバックアップ4,596msだった。環境、件数、許容判断は`リリース性能試験記録.md`へ記録した。画面応答と実表示幅だけは実データから隔離した専用環境で確認する。

完了条件：計測環境、件数、結果、許容判断をテスト記録へ残す。

#### REL-QA-003 Windows 11表示・アクセシビリティ【必須】

- [ ] 1280×720以上で主要操作が表示される。
- [ ] 緑・青テーマ、通常／詳細管理一覧、900px以下のカード表示を確認する。
- [ ] キーボード操作、フォーカス、エラー通知、読み上げ名を確認する。
- [x] `prefers-reduced-motion`でマスコットの連続動作を抑制する。
- [ ] 100%、125%、150%表示倍率で致命的な欠けや重なりがない。

完了条件：対象表示幅・倍率ごとの確認記録がある。

#### REL-QA-004 NSIS・配布物【必須・リリース阻止】

- [x] CIでもNSISインストーラーを生成するか、正式リリース工程で同等の再現可能な生成手順を固定する。
- [x] インストーラー内容へDB、FAQ、履歴、添付、会社管理の外部資料、旧版互換用`manuals`データ、CSV、バックアップ、Codex提案が含まれないことを検査する。
- [ ] 新しいWindows利用者環境で新規インストールし、空DBから開始する。
- [ ] 旧版から上書き更新し、既存データが維持される。
- [ ] アンインストールで利用者データが不用意に削除されない。
- [ ] ショートカットからコマンド操作なしで起動できる。

完了条件：新規導入、上書き更新、アンインストール、再インストールのリハーサルが合格する。

実施記録：GitHub Actionsとローカルの双方で`npm run tauri build`、配布設定検査、禁止データ検査、SHA-256記録を行う工程を固定した。Windows 11で`KnowledgeApp_0.3.3_x64-setup.exe`を生成し、自動検査は合格した。導入・更新・削除の手動試験は実データから隔離した専用Windows利用者環境で行う。

#### REL-QA-005 コード署名・会社規定【会社配布時は必須】

- [ ] コード署名の要否、証明書、署名担当、更新手順を会社規定に照らして確定する。
- [ ] 署名が必要な場合はEXEとNSISインストーラーへ署名し、署名検証をリリース工程へ追加する。
- [ ] GitHubリポジトリの公開／非公開、会社情報の取扱い、配布経路を確定する。
- [ ] 会社PCでWebView2、実行制限、ウイルス対策、保存場所、UNCアクセスを確認する。

完了条件：会社向け配布条件を満たすことを担当者が確認している。

### フェーズ5：文書・運用準備

#### REL-DOC-001 正式文書の同期【必須】

- [x] 要件定義書へ確定したリリース範囲を反映する。
- [x] 基本設計書へ画面、Command、DB、検索、外部資料参照、バックアップ、起動制御を反映する。
- [x] 詳細設計書へ新しい保存形式、Git除外、検査、リリース梱包への影響を反映する。
- [x] 文書末尾の参照版を最新の版番号へ更新する。
- [x] 実装済み／後続／将来の記述を現状と一致させる。

実施記録：要件定義書v1.30、基本設計書v1.39、詳細設計書v0.41へ更新し、相互参照版を一致させた。会社判断・専用実機試験は実装済みと混同せず、リリースノートと試験記録へ未確認事項として分離した。

完了条件：文書と実装を照合し、説明できない差異が0件である。

#### REL-DOC-002 README・リリースノート【必須】

- [x] READMEの開発版表記を実際の版`0.3.3`へ更新する。
- [x] インストール、初回ログイン、パスワード変更、バックアップ、復元、CSV、検索、外部資料参照の利用方法を記載する。
- [x] 変更内容、DB移行、互換性、既知事項をリリースノートへ記載する。
- [x] 実データを含まないスクリーンショットまたは操作説明だけを使用する。

実施記録：READMEへ開始手順を追加し、`KnowledgeApp運用手順.md`、`リリースノート_0.3.3.md`、`リリース試験記録_0.3.3.md`を作成した。画像や実FAQは使用せず、操作説明だけで構成した。

完了条件：初めて利用する人がコマンド入力なしで導入・初期設定・バックアップできる。

#### REL-OPS-001 初回利用・定期運用手順【必須】

- [x] 初回管理者`0000`でログイン後、管理者パスワードを変更する手順を定める。
- [x] 本格利用では原則として空パスワード許可をOFFにする手順を定める。
- [x] FAQへパスワード、秘密鍵、個人情報を登録しないことを明記する。
- [x] 外部資料本体は会社管理のディレクトリまたはWebサーバーで保管し、KnowledgeAppのフルバックアップには含まれないことを明記する。
- [x] 複数PCで共通利用する参照先は、PCごとに異なるドライブ文字より会社標準のUNCパスを優先できるか確認する手順を定める。
- [x] リリース更新前、CSV取込前、定期的な節目でフルバックアップを作成する手順を定める。
- [x] バックアップを別PCまたは別保存先へ復元できることを定期確認する手順を定める。
- [x] ネットワークドライブはバックアップ先だけに使用し、SQLite DBを直接置かないことを明記する。

実施記録：`KnowledgeApp運用手順.md`へ初回設定、FAQ運用、外部資料、バックアップ・復元、CSV・JSON、更新・障害時の手順を集約した。初回パスワード変更や別PC復元の実行結果は第7章の専用環境リハーサルで記録する。

完了条件：利用者が参照できる運用手順がREADMEまたは別の正式運用文書にある。

## 7. 最終リリースリハーサル

次の順序で、実際に配布する候補コミットと成果物を使用して実施する。

1. [ ] 対象コミットとバージョンを固定する。
2. [ ] クリーンな取得環境で依存関係をロックファイルどおりに導入する。
3. [ ] 全自動検査を実行する。
4. [ ] NSISインストーラーを生成する。
5. [ ] 配布禁止データの混入検査と、必要な場合は署名検証を行う。
6. [ ] 新規Windows利用者へインストールし、初期管理者でログインする。
7. [ ] 分類・FAQ・画像・検索情報・関連FAQ・外部資料参照を登録する。
8. [ ] 日本語検索、同義語、検索範囲、履歴を確認する。
9. [ ] CSV書出し・プレビュー・取込を確認する。
10. [ ] フルバックアップをローカルとネットワーク先へ作成する。
11. [ ] 旧版環境へ検証データを用意し、本格利用候補版へ上書き更新する。
12. [ ] 移行前バックアップ、DB移行、既存データ保持を確認する。
13. [ ] 別Windows利用者または別PCへバックアップを復元する。
14. [ ] アンインストール・再インストール後のデータ保持を確認する。
15. [ ] 結果、担当、日時、成果物ハッシュ、既知事項をリリース記録へ残す。

## 8. 最終判定記録

| 項目 | 記録欄 |
|---|---|
| リリース候補バージョン |  |
| 対象コミット |  |
| NSIS成果物 |  |
| SHA-256 |  |
| コード署名状態 |  |
| 自動検査結果 |  |
| 新規インストール結果 |  |
| 上書き更新結果 |  |
| バックアップ・復元結果 |  |
| 検索性能結果 |  |
| 既知の問題 |  |
| リリース可否 | 可／条件付き可／不可 |
| 判定日・判定者 |  |

## 9. 本書を削除できる条件

次をすべて満たした後は、本書を削除してよい。

- [ ] 第6章の必須作業がすべて完了している。
- [ ] 第7章の最終リリースリハーサルがすべて合格している。
- [ ] 第8章の最終判定が「可」である。
- [ ] 未完了事項が残る場合は、正式文書、課題管理またはリリースノートへ移管されている。
- [ ] 恒久的に必要な仕様・設計・運用手順・テスト結果が本書だけに残っていない。
- [ ] 本書の削除を含む最終コミット後も、正式文書と実装の対応を追跡できる。

全条件を満たした場合、`リリース前作業計画書.md`は一時的な進行管理の役割を終えるため、ソース管理から削除して問題ない。
