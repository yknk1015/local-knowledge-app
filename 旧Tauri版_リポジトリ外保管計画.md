# 旧Tauri版のリポジトリ外保管計画

作成日：2026-09-06。利用者承認を受けて実施へ移行した。以下は当初の判断と手順を残した計画であり、現在の状態・実際の保存先・確認結果は[外部保管実施記録](旧Tauri版_外部保管実施記録.md)を優先する。GitHubへのpushは別途指示を受けて行う。

## 推奨方針

GitHubリポジトリは新しく作らず、現在のリポジトリをC#正式版の開発先として継続する。旧版は履歴の目印となるタグとリポジトリ外アーカイブで保存し、依存を分離してから現在の作業ツリーから除く。過去のGit履歴は削除しない。

「現在の開発フォルダーから旧版をなくす」ことと「過去のGit履歴からも完全に消す」ことは別である。今回は前者を推奨する。機密情報の混入や履歴容量の問題がある場合は別の調査・履歴整理が必要で、新規リポジトリを作るだけでは元の履歴は消えない。

## 整理前に確認した依存と対応方針

| 参照元 | 現在の依存 | 退避前の対応 |
|---|---|---|
| `src-csharp/KnowledgeApp.Data/KnowledgeApp.Data.csproj` | `src-tauri/migrations/*.sql`をC#へ埋込み | SQLをC#管理下へ移し、内容・順序・埋込み結果・DB第1～7版復元を確認する |
| `KnowledgeApp.CodexInteropCheck` / `KnowledgeApp.FinalInteropCheck` | `src-tauri/Cargo.toml`の探索、Rust実コマンド比較、旧tauri.conf.json | 通常C#試験を独立させ、実Rust比較は明示的な別の互換検証経路へ分離する。無言で試験を削除しない |
| `.github/workflows/quality.yml` | Rust検査・Tauriビルドと、それによるNSISコンパイラー準備 | C#だけで配布まで完了するCIへ変更する。互換ジョブは固定した旧ソースを明示取得する等、独立した手順にする |
| 旧配布・性能検査スクリプト等 | `src-tauri`や旧インストーラーを参照 | 現役/旧版専用を分類し、旧版専用だけを退避対象にする |

共通の`src/`はC#でも使うReact/Tiptap UIであり、旧版専用フォルダーではない。共通UI、静的画像、package設定などを一括して外へ移動しない。Tauri依存パッケージやフロントエンドの互換経路も、使用箇所を調べてから整理する。

整理前にはC#移行成果を含む未コミット変更・未追跡ファイルがあった。このため対象差分と利用者データ混入を確認し、基準コミット`277f38c`へ保存してからタグ・アーカイブを作成した。

## 実施を依頼いただいた場合の順序

1. 現在のソース差分を確認し、型検査・C#試験・ビルド・UTF-8/LF・データ混入検査を通す。無関係な変更や実FAQをまとめてステージしない。確認済みソースを保存するコミットは、実施依頼の範囲を確認してから作る。
2. 保存したコミットにC#正式移行の目印を付ける。例として`csharp-0.5.0`、旧版保管基準として`legacy-tauri-final`を使用できるが、実在するタグとは扱わず、対象コミットを確定してから作成する。同じコミットのスナップショットに旧版とC#が共存していてもよい。
3. 確定したコミットから`git archive`でソースZIPを作り、リポジトリ外で展開・照合する。`node_modules`、`target`、`.git`等を含む作業フォルダーの丸ごとコピーを既定の退避方法にしない。旧インストーラーまたは必要な実行物一式は、バージョン・ハッシュ・依存環境とともに別途保存する。
4. 移行SQL、通常テスト、配布手順の依存をC#側へ移し、旧ソースが隣にない新規チェックアウトでもC#をビルド・試験・配布できることを確認する。必要なRust比較は明示的な互換試験として継続する。
5. アーカイブが復元可能であることを確認後、旧版専用ソース・設定・不要になった旧スクリプトを現在の作業ツリーから取り除き、独立した整理コミットにする。履歴の全書換えやforce pushは行わない。GitHubへのpushは別途指示された範囲で実施する。

タグは履歴の目印であってファイルの完全な外部バックアップではないため、リポジトリ外アーカイブも残す。`.gitignore`への追加だけでは既に追跡済みの旧ソースはGit管理から外れない。

## 利用者確認済みの保管先

`C:\Users\takow\Documents\game_development\program\old\KnowledgeApp_Archive\Tauri_0.4.4_20260906\`

- `source/`：確定コミットのソースZIP。移行時点の共通UI・ビルド定義を含む。
- `release/`：旧インストーラーまたは必要な実行物一式。exe単体でよいかは配布方式に沿って確認する。
- `README.md`：コミット・タグ・版・ハッシュ・起動手順・「旧版は通常更新しない」こと。

ここへ実FAQのDB、画像、履歴、バックアップを混ぜない。旧版の利用者データは既に`LocalApplicationData/jp.local.webknowledgesystem`へ分離されており、そのまま残す。C#の正本`jp.local.webknowledgesystem.csharp`も移動しない。

## 新しいGitHubリポジトリが向く場合

別製品として公開範囲・アクセス権・管理主体を分けたい場合、または旧版を別チームで継続保守したい場合は検討する。今回の「同じFAQアプリをC#へ移行し、今後も改修する」目的では、今のリポジトリを継続した方が過去の経緯を参照しやすく、設定の作り直しも少ない。

## 根拠

- [Git公式：git archive](https://git-scm.com/docs/git-archive)：指定コミット/ツリーのソースをアーカイブする仕組み。未コミットの作業差分のバックアップと混同しない。
- [Git公式：git tag](https://git-scm.com/docs/git-tag)：確定した履歴へ名前を付ける。
- [Git公式：gitignore](https://git-scm.com/docs/gitignore)：既に追跡されているファイルはignore指定だけでは追跡解除されない。

本計画は保存先や現行アプリの安全境界を変更するものではない。実施時は要件・基本・データ詳細設計とCIを同じ作業内で同期する。
