# FAQシステム要件定義書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | FAQシステム要件定義書 |
| 版 | 1.30（本格利用候補版範囲確定版） |
| 作成日 | 2026-08-08 |
| 元資料 | `2026.08.08_ChatGPTとの会話.txt` |
| 対象 | 個人PCで開発・テストし、会社PCでもローカル利用するFAQ・ナレッジ管理アプリ |

### 1.1 本書の位置づけ

本書は、元資料に記載された希望と提案、2026-08-08に受領したQ1～Q10への回答、および保存失敗通知、URL文字列保存、FAQ表示フラグ・非表示、CodexによるFAQ作成・推敲・修正・統合提案、提案履歴、FAQ本文の標準構成、必要な画像・スクリーンショットの利用方針、メイン画面とFAQ編集画面の視認性・操作性改善、緑・青のカラーテーマ切り替え、FAQ詳細から戻った際の検索状態復元と検索条件初期化、検索結果のFAQタイトルへトップ分類名を表示する設定、ネットワークパスなどを正確にコピーするためのコピー用テキスト枠、左下へ常駐して通常時は静止しクリック時だけランダムな動作を行うFAQマスコット、友達の猫が遊びに来る一連の動作、表示・非表示設定、検索結果を比較しやすい1列カード一覧への統一、ブランド補足と検索画面見出し、Codex提案画面の説明、Codex連携用保存先の表示場所、検索結果の50件ページング、分類ツリーのコンパクト表示と任意開閉、検索パネルの単色化、更新日・重要度による検索結果の並び替えを整理する追加要望を整理し、実装可能な要件へ具体化したものである。

Q1～Q10への回答事項に加え、2026-08-16に確定したCSV一括編集、ローカル利用者認証、FAQ作成者・更新者の記録、空パスワード許可方針の管理者設定、FAQ管理一覧の通常・詳細・レスポンシブ表示、CSV用FAQ・分類管理IDの可読化、C#移行の将来検討、会社管理の外部資料をコピー用パスまたはHTTP/HTTPS参考URLで参照する方針を確定要件として反映した。プロトタイプで実データを用いて調整する事項は「調整対象」、将来の本格運用時に検討する事項は「今後の検討事項」と記載する。

## 2. 背景と目的

PC修理、PC設定、ネットワーク、プログラム修正などの業務知識を、階層型のFAQとして登録・検索・更新できるようにする。

短い回答はアプリ内に保存し、画像や説明が多い複雑な手順は、会社が既存ディレクトリやWebサーバーで管理するHTML、Excel、PDF、Wordなどの外部資料へ誘導する。ローカルパスやUNCパスはコピー用テキストとして扱い、HTTP/HTTPSの参考URLだけを確認後に既定ブラウザーで開く。利用者が自然な文章で質問を入力した場合も、生成AIを使わずに関連FAQを見つけられることを目指す。

Codexが作成するFAQは、利用者が最初に結論を把握し、そのまま必要な操作へ進める簡潔な構成とする。情報量を増やしすぎず、結論、手順、注意、必要な備考、参考リンクを一定の順序で示す。文章だけでは操作箇所や状態の違いが伝わりにくい場合は画像・スクリーンショットで視認性を補うが、装飾目的では使わず、著作権侵害のおそれや利用条件が不明な画像は無理に使用しない。

検索結果で別分類のFAQをすぐに区別できるよう、表示設定がONの場合はFAQタイトルの先頭へ現在のトップ分類名を`【トップ分類名】`として表示する。分類名の変更やFAQ移動後に古い接頭辞が残らないよう、接頭辞はタイトル本体へ保存せず表示時に現在の分類階層から生成する。設定がOFFの場合は保存済みのタイトル本体だけを表示する。

利用者が自宅で作成するFAQは「生成AIの活用事例」を中心とする場合があり、会社ではPCトラブルなどの会社業務FAQを登録・利用する。これは登録されるFAQの題材に関する前提であり、アプリを生成AI専用にする要件ではない。

アプリへOpenAI APIキーや従量課金の生成AI APIを組み込まない主な理由は、利用量に応じた費用増大を避けるためである。生成AI連携そのものを避ける方針ではなく、利用者が定額利用するCodexへ、必要なFAQ作成・推敲・修正・統合を明示的に委譲できる構成を採用する。セキュリティ対策は費用方針とは別に、誤操作、未選択データの送信、DB不整合を防ぐために維持する。

### 2.1 達成目標

- FAQを画面から登録、編集、複製、削除でき、本文では画像・表・クリック可能な参考URLを扱える。
- 分類階層を1～5階層の範囲で自由に構成できる。
- 選択中の階層と、その配下の階層にあるFAQだけを検索できる。
- 日本語の自然文、同義語、表記揺れを考慮してFAQを検索できる。
- 複雑な内容は、FAQに記載した参照先パスをコピーするか、HTTP/HTTPSの参考URLを既定ブラウザーで開いて外部資料を確認できる。
- 通常の検索・閲覧・手動編集・バックアップは外部サービスへFAQデータを送信せず、オフラインで利用できる。参考URLを開く操作と、利用者が対象を選んでCodexへ明示的に委譲する操作だけを例外とする。
- バックアップからFAQデータと添付画像を復元でき、FAQ本文に保存した外部資料の参照先文字列も復元できる。外部資料本体は会社側の保管・バックアップを利用する。
- 従来の手動作成に加え、利用者がCodexへ明示的に依頼した新規FAQ、既存FAQの推敲・修正案、複数FAQの統合案を確認待ち提案として受け取れる。
- Codex提案を承認または却下した履歴を確認し、同じ依頼系列の最新却下案だけを再検討へ戻せる。
- Codex統合FAQを新着として公開し、利用者が確認した場合だけ元FAQを解除可能な「統合済み」として統合先へ関連付け、検索結果の重複を防げる。
- Codexが作成するFAQを、結論を先に示し、必要最小限の手順と補足へ進む統一された読みやすい構成にできる。
- Codexが作成するFAQで、文章だけでは伝わりにくい箇所に限って権利と機密性を確認した画像・スクリーンショットの追加位置を案内し、視認性を高められる。
- 検索結果のFAQタイトルへトップ分類名を表示するか設定でき、分類名変更やFAQ移動後も現在のトップ分類名へ自動追従できる。
- ネットワークパスやファイル名などの文字列を回答内のコピー用テキスト枠として登録し、FAQ閲覧時に一操作で正確にクリップボードへコピーできる。
- FAQマスコットを画面操作の邪魔にならない隅へ静止画で常駐表示し、クリックまたはキーボード操作時だけ、眠る動作や友達の猫との交流を含む複数の動作からランダムに1つを再生できる。
- FAQと回答本文をCSVへ書き出し、Excelで一括編集した内容をプレビュー後に取り込める。
- ローカル利用者でログインし、管理者が利用者追加・利用停止・パスワード再設定を行い、FAQの作成者・更新者を確認できる。

## 3. 対象範囲

### 3.1 初回リリース（MVP）の対象

- Windows上で動作するローカルデスクトップアプリ（同時利用は1セッション）
- 分類階層の作成・変更
- FAQの登録・編集・複製・削除
- FAQ一覧・詳細表示
- 画像、表、参考URLに対応するリッチテキスト編集
- 選択文字列をコピー用テキスト枠へ変換し、詳細画面で正確にコピーする機能
- 任意の終了日まで表示する新着・更新フラグと、FAQ単位の非表示設定
- 現在地以下を対象とする検索
- 生成AIを使わない日本語検索
- タグ・同義語・表記揺れ対応
- 関連FAQの設定
- ローカルパス・UNCパスを自動実行せず正確にコピーする外部資料参照
- HTTP/HTTPSの参考URLを確認後に既定ブラウザーで開く外部資料参照
- 検索履歴、検索結果0件の記録
- バックアップ、復元、JSONエクスポート
- 全画面へ反映される緑・青のカラーテーマ選択、検索結果タイトルのトップ分類名表示切り替えと端末内への設定保存
- 全画面共通のFAQマスコット静止表示、クリック時だけのランダム動作、眠る動作、友達の猫「ムギ」の訪問・交流・帰宅動作
- 個人PCでの開発、GitHub等を用いたソースコードとビルド成果物の配布
- Codexプラグインによる新規FAQ下書き、既存FAQの推敲・修正、複数FAQの非破壊統合、分類提案、アプリ上での差分確認と反映
- Codex提案の承認・却下履歴と、依頼系列ごとの最新却下案の再検討
- 統合FAQの新着公開、公開後確認による元FAQの「統合済み」表示、通常検索除外、統合解除
- FAQ・回答本文を対象とするExcel編集用CSVエクスポート、取込プレビュー、インポート
- 初期利用者`0000`、ローカルログイン、利用者追加・利用停止・パスワード再設定、FAQ作成者・更新者表示

### 3.2 将来候補

- よく閲覧されるFAQの表示
- お気に入り
- 解決した／解決しなかったの記録
- 検索語、解決率、未整備分野などの分析画面
- 一般利用者のFAQ操作権限をさらに細分化するロール・承認フロー
- 検索キーワード傾向、人気FAQ、FAQ未登録ワードを管理者だけが閲覧できる管理画面
- 任意スケジュールによる自動バックアップと保持世代管理
- 分類の新規作成を含む一括インポート
- FAQまたは分類の管理ID採番値が9万件へ達した段階を目安とする、桁数、画面・CSV表示幅、検索・取込性能、運用ルールの再評価
- 利用者確認後にローカル・UNCパスのファイルを既定アプリで直接開く機能（現行はコピー専用）
- SQLite以外のデータベースや共有サーバーへの移行
- 会社デモ後に判断するASP.NET（`.aspx`・C#・CSS）または.NET Framework 4.8 C#デスクトップアプリへの移行

### 3.3 対象外

- アプリへのOpenAI APIキーまたは生成AIモデルの組み込み
- 利用者確認なしでのFAQ自動登録・公開
- Codexによる利用者確認なしの既存FAQ更新、公開、削除
- CodexによるSQLite、WAL、SHM、既存FAQ本文、添付画像、外部資料の参照先・内容、検索・閲覧履歴の直接読取・変更
- ローカル生成AIの実行
- 生成AIによる質問意図の推定や回答生成
- 複数人による同時利用、ネットワーク共有、クラウド同期
- SSO、外部IDプロバイダー、同時接続を前提とするサーバー認証、FAQ承認フロー
- Teamsなど外部サービスとの連携
- インターネット上へのFAQ公開
- MVPにおける自動バックアップのスケジュール実行

## 4. 前提条件と制約

| ID | 内容 | 状態 |
|---|---|---|
| PRE-01 | 利用者はローカルDBへ複数登録できるが、1台のアプリで同時に有効なログインセッションは1つとし、複数PC・複数人の同時更新は対象外とする。 | 決定 |
| PRE-02 | 開発環境と利用環境の対象OSはWindows 11（64ビット）とする。 | 決定 |
| PRE-03 | 通常の検索・閲覧・手動編集・バックアップはローカルで完結し、インターネット接続を必要としない。参考URLを開く操作と、利用者が対象を選んでCodexへ明示的に委譲する操作は例外とする。 | 決定 |
| PRE-04 | 起動時にコマンド入力を必要とせず、ショートカットまたは実行ファイルから起動できる。 | 決定 |
| PRE-05 | FAQデータはSQLite、FAQ内画像は利用者別アプリデータフォルダへ保存する。会社管理の外部資料本体はKnowledgeAppへ取り込まない。 | 決定 |
| PRE-06 | 会社PCでは自作Windows実行ファイルを起動できる。実行ファイルとデータの配置は社内規定に従う。 | 決定 |
| PRE-07 | FAQや参照先の外部資料には、パスワード、秘密鍵、個人情報など保存禁止情報を登録しない。 | 決定 |
| PRE-08 | アプリは外部CDN、外部フォント、外部JavaScriptを必須としない。 | 決定 |
| PRE-09 | 個人PCで開発し、ソースコードとビルド成果物をGitHub等で管理・配布して会社PCへ導入する。 | 決定 |
| PRE-10 | GitHub等へ登録するのはプログラム、設計文書、設定例、明示的な開発用フィクスチャーに限定し、利用者がローカル環境で作成したFAQ、検索・閲覧履歴、添付画像、データベース、バックアップを登録しない。会社管理の外部資料もソース管理へ複製しない。 | 決定 |
| PRE-11 | 既存FAQの初回一括取り込みは行わず、新規登録から運用を開始する。既存の会社資料は元のディレクトリで管理し、必要な参照先だけをFAQへ記載する。 | 決定 |
| PRE-12 | 自宅の個人PCで作成するFAQは「生成AIの活用事例」を中心とする場合があるが、アプリ自体は題材を限定しない汎用FAQシステムとする。 | 決定 |
| PRE-13 | 会社PCでは、PCトラブル、PC設定、ネットワーク、プログラム修正などの会社業務FAQを登録・利用する。 | 決定 |
| PRE-14 | リポジトリルートの`AGENTS.md`を開発ルールとし、開発担当は要件定義書・基本設計書を確認して実装と文書を同期する。 | 決定 |
| PRE-15 | Codex連携は利用者が会話で明示的に依頼した場合だけ使用し、会社データを提示できるかは会社規定を優先する。 | 決定 |
| PRE-16 | 生成AI APIをアプリへ組み込まない主目的は従量課金費用の抑制とし、定額利用のCodexへの明示的委譲は新規作成・修正・統合に活用する。 | 決定 |

## 5. 用語

| 用語 | 定義 |
|---|---|
| 分類 | FAQを整理する階層上の項目。カテゴリとも呼ぶ。 |
| 現在地 | 左側の分類ツリーで現在選択している分類。 |
| 現在地以下 | 現在地自身と、その子・孫にあたる全分類。 |
| FAQ | 質問、概要、回答、症状、原因、タグなどをまとめたナレッジ。 |
| 簡易回答 | アプリ内で直接表示する比較的短い回答。 |
| 外部資料参照 | 会社がKnowledgeAppの外で管理するHTML、Excel、PDF、Wordなどへ利用者を案内する情報。ローカルパス・UNCパスはコピー用テキスト、Web上の資料はHTTP/HTTPS参考URLとしてFAQ本文へ記載する。 |
| 自然文検索 | 文章で入力でき、語の分割、正規化、同義語展開、全文照合を行う非生成AI検索。 |
| 仮想ルート | 全分類の上位に存在する画面上の起点。「すべて」などと表示し、5階層には数えない。 |
| 分類カタログ | Codexが所属分類を提案するための、分類ID・名前・説明・階層だけを含むローカルJSON。FAQ本文や件数を含めない。 |
| Codex提案箱 | Codexが作成したFAQ下書き候補を、アプリで確認するまでリポジトリ外へ保管するフォルダ。 |
| Codex委譲 | 利用者がアプリ画面で選んだ既存FAQだけを、推敲・修正・統合の対象としてCodexへ渡すローカルファイル。 |
| 依頼系列 | 同じ委譲番号または同じ作成依頼から生成された複数のCodex提案をまとめる単位。 |
| トップ分類 | 仮想ルート直下にある1階層目の分類。FAQが下位分類に所属する場合も、その分類から親をたどった最上位の分類を指す。 |
| FAQ管理ID | CSVで既存FAQを識別する`FAQ-00001`形式の人間向け固定コード。内部UUIDとは分離する。 |
| 分類管理ID | CSVで既存分類を識別する`CAT-00001`形式の人間向け固定コード。内部UUIDとは分離する。 |

## 6. 利用者と主な利用場面

### 6.1 利用者

- FAQを検索・閲覧する本人
- FAQ、分類、同義語を管理し、会社管理の外部資料への参照先を記載する本人

初期利用者`0000`は管理者とする。管理者は利用者管理を行い、一般利用者と管理者はいずれも現段階ではFAQの検索・作成・更新を行える。管理者だけが利用者管理と、利用者情報を含む完全バックアップ・復元を操作する。

### 6.2 主な利用場面

1. 困っている内容を自然な文章で入力し、関連FAQを探す。
2. 分類ツリーで業務分野を選び、その配下だけを検索する。
3. FAQの簡易回答を確認し、必要な場合は参照先パスをコピーするか、参考URLから外部資料を開く。
4. 新しい対応結果をFAQとして登録する。
5. 既存FAQを更新、複製、廃止する。
6. 分類や同義語辞書を整理し、検索精度を改善する。
7. 検索結果0件の言葉を確認し、追加すべきFAQを判断する。
8. KnowledgeAppのデータをバックアップし、外部資料本体は会社側の保管・バックアップ方針に従う。
9. 自宅では生成AI活用事例などのFAQを登録し、FAQ作成・検索・編集機能を検証する。
10. 会社ではPCトラブルなどの業務FAQを登録・検索する。
11. 利用者がCodexへFAQ作成を依頼し、アプリで本文と所属分類を確認して下書きへ取り込む。
12. 既存FAQを1件選び、Codexへ推敲・修正を委譲し、現在内容との差分を確認して反映する。
13. 既存FAQを2～10件選び、Codexへ統合を委譲し、元FAQを残したまま統合版を新規下書きへ取り込む。統合版を新着として公開した後、確認して元FAQを「統合済み」とし、通常検索を統合版へ一本化する。
14. 誤って却下した提案を履歴で確認し、同じ依頼系列の最新案であれば再検討へ戻す。

## 7. 機能要件

優先度は「必須」「推奨」「将来」の3段階とする。

### 7.1 分類階層

| ID | 要件 | 優先度 |
|---|---|---|
| FR-CAT-01 | 仮想ルートの下に分類を作成できる。 | 必須 |
| FR-CAT-02 | 分類は1階層から最大5階層まで作成できる。 | 必須 |
| FR-CAT-03 | 枝ごとに階層数が異なってよい。 | 必須 |
| FR-CAT-04 | 分類名を変更できる。 | 必須 |
| FR-CAT-05 | 分類とその配下を、別の分類の下へ移動できる。 | 必須 |
| FR-CAT-06 | 移動後に6階層以上になる操作を拒否し、理由を表示する。 | 必須 |
| FR-CAT-07 | 自分自身または自分の子孫の下へ移動する循環操作を拒否する。 | 必須 |
| FR-CAT-08 | 同一親の中で表示順を変更できる。 | 推奨 |
| FR-CAT-09 | 分類を削除できる。ただし、配下分類またはFAQが残っている場合は削除を禁止し、先に移動または削除を求める。 | 必須 |
| FR-CAT-10 | 同じ親の直下では同名分類を禁止し、別の枝では同名を許可する。 | 必須 |
| FR-CAT-11 | 分類ごとのFAQ件数を表示できる。 | 推奨 |
| FR-CAT-12 | 分類ごとに任意の説明を500文字以内で登録でき、Codexの分類提案用カタログへ含める。 | 必須 |

### 7.2 FAQ管理

| ID | 要件 | 優先度 |
|---|---|---|
| FR-FAQ-01 | FAQを新規登録できる。 | 必須 |
| FR-FAQ-02 | FAQを編集できる。 | 必須 |
| FR-FAQ-03 | 既存FAQを複製し、別FAQとして登録できる。 | 必須 |
| FR-FAQ-04 | FAQを削除できる。誤操作防止の確認を行い、MVPでは論理削除とする。 | 必須 |
| FR-FAQ-05 | 削除済みFAQを復元できる。 | 推奨 |
| FR-FAQ-06 | FAQを下書き、公開、廃止のいずれかに設定できる。 | 必須 |
| FR-FAQ-07 | 通常検索では公開FAQだけを対象にし、編集画面では全状態を絞り込める。 | 必須 |
| FR-FAQ-08 | FAQは1つの所属分類を持ち、複数の観点はタグで補う。 | 必須 |
| FR-FAQ-09 | FAQ同士を関連FAQとして相互に参照できる。 | 必須 |
| FR-FAQ-10 | 登録日と最終更新日を自動記録する。 | 必須 |
| FR-FAQ-11 | 保存前に必須項目とリンク先を検証する。 | 必須 |
| FR-FAQ-12 | 未保存の変更がある状態で画面を離れる場合は警告する。 | 必須 |
| FR-FAQ-13 | 簡易回答をリッチテキストとして編集し、画像、表、見出し、段落、太字、箇条書き、番号付きリスト、クリック可能な参考URLを扱える。 | 必須 |
| FR-FAQ-14 | FAQ内の画像はアプリ管理下の添付ファイルフォルダへコピーし、FAQからは相対パスで参照する。 | 必須 |
| FR-FAQ-15 | リッチテキストから検索用プレーンテキストを生成し、簡易回答の文字部分を検索対象にできる。 | 必須 |
| FR-FAQ-16 | URLらしい文字列を、クリックや自動実行を行わない通常テキストとして保存できる。通常テキストの場合はURLスキームで登録を拒否しない。 | 必須 |
| FR-FAQ-17 | FAQごとに新着フラグを任意設定し、利用者が指定した表示終了日当日まで検索結果と詳細画面へ表示できる。 | 必須 |
| FR-FAQ-18 | FAQごとに更新フラグを任意設定し、利用者が指定した表示終了日当日まで検索結果と詳細画面へ表示できる。新着フラグと同時設定できる。 | 必須 |
| FR-FAQ-19 | FAQごとに状態とは独立した非表示設定を持ち、非表示FAQを通常検索から除外する。非表示FAQはFAQ管理画面から確認・編集できる。 | 必須 |
| FR-FAQ-20 | 従来どおり利用者が一からFAQを作成する導線を維持する。 | 必須 |
| FR-FAQ-21 | CodexはFAQのタイトル、概要、リッチテキスト回答、重要度と、最大3件の既存分類候補または1件の新規分類案を提案できる。 | 必須 |
| FR-FAQ-22 | Codex提案はアプリの確認画面で本文を表示し、利用者が既存分類を再選択するか、新規分類案を承認してから取り込む。 | 必須 |
| FR-FAQ-23 | Codexの新規作成・統合提案から作成するFAQは常に新規下書きとする。既存FAQは、利用者が1件を明示委譲した修正案を画面で承認した場合だけ更新し、Codexから自動公開・自動削除しない。 | 必須 |
| FR-FAQ-24 | 同じCodex提案の再送・再操作によるFAQの重複登録を防ぐ。 | 必須 |
| FR-FAQ-25 | Codexの新規・統合提案では画像を含めず、必要な画像は下書き取込後に利用者が追加する。既存FAQの修正提案では委譲元の画像参照を全件そのまま維持し、追加・削除・差替えを行わない。 | 必須 |
| FR-FAQ-26 | FAQ詳細画面から既存FAQ1件を明示的に選び、Codexへ推敲・修正対象として委譲できる。 | 必須 |
| FR-FAQ-27 | Codexの修正案は現在のFAQと並べて確認し、利用者承認後にタイトル、概要、回答、重要度だけを既存FAQへ反映する。所属分類、状態、新着・更新期限、非表示、添付画像は維持する。 | 必須 |
| FR-FAQ-28 | 委譲後に元FAQが更新または削除されている場合、古いCodex修正案の反映を拒否し、再委譲を案内する。 | 必須 |
| FR-FAQ-29 | FAQ管理画面で既存FAQを2～10件選び、Codexへ統合対象として委譲できる。 | 必須 |
| FR-FAQ-30 | Codexの統合案は新しいFAQ下書きとして取り込み、統合元FAQの内容・状態・削除状態を自動変更しない。 | 必須 |
| FR-FAQ-31 | Codex提案の承認・却下履歴をローカルDBへ保存し、内容、種類、判断日時、反映先FAQを確認できる。 | 必須 |
| FR-FAQ-32 | 却下済み提案は、同じ依頼系列により新しい提案が存在しない最新版だけを再検討へ戻せる。古い却下案は閲覧専用とする。 | 必須 |
| FR-FAQ-33 | 却下は提案ファイルの物理削除だけで処理せず、履歴を保持する。再検討後も同じ受付番号の重複反映を防ぐ。 | 必須 |
| FR-FAQ-34 | Codexが作成するFAQは、概要へ原則1文の結論を置き、本文を手順、明示した注意、必要な備考、参考リンクの順で簡潔に構成する。存在しない注意・備考・リンクのための空見出しや、確認できない情報を作らない。 | 必須 |
| FR-FAQ-35 | Codexは、画像・スクリーンショットが視認性・理解を実質的に高める場合だけ、その内容、関係する手順直後の挿入位置、代替テキスト案を示す。利用者が作成・提供した画像または利用条件を確認できた画像だけを対象とし、著作権侵害のおそれ、利用条件の不明確さ、個人情報・機密情報・認証情報の写り込みが解消できない場合は使用せず文章で補う。新規・統合案の提案JSONには画像を含めず下書き取込後の追加を案内し、修正案では委譲元画像を変更しない。 | 必須 |
| FR-FAQ-36 | Codexが作成する`faq.title`は検索しやすい質問文とし、`【分類名】`の接頭辞を含めない。検索結果の分類接頭辞は表示設定に従ってアプリが動的に付与し、修正案でも表示設定を理由に既存タイトルへ接頭辞を追加・削除しない。 | 必須 |
| FR-FAQ-37 | FAQ回答内の選択文字列をコピー用テキスト枠として保存できる。枠内の改行、空白、記号、円記号を含む文字列を変更せず保持し、FAQ詳細では一操作で同じ文字列をクリップボードへ書き込める。コピー操作からファイル、フォルダ、URLを自動で開かない。 | 必須 |
| FR-FAQ-38 | Codex統合案から作成した下書きを初めて公開する場合は、新着フラグと表示終了日を必須とする。画面は公開選択時に新着をONにして端末の現在日付から7日後を初期表示し、Rust側も未設定の公開保存を拒否する。 | 必須 |
| FR-FAQ-39 | 統合FAQの公開成功後、利用者へ統合元FAQを「統合済み」にするか確認する。承認した場合だけ、委譲時から変更・削除されていない元FAQと統合先FAQの関係を同一トランザクションで保存する。元FAQの本文、状態、非表示、論理削除は変更しない。 | 必須 |
| FR-FAQ-40 | 「統合済み」は下書き・公開・廃止・非表示・論理削除を兼ねる状態フラグではなく、元FAQから統合先FAQへの解除可能な関係として扱う。統合済みFAQは通常検索から除外し、管理・詳細画面で統合先を表示する。統合解除後は元の状態と非表示設定に従う。関係が残る間は検索先を失わないよう、統合先FAQを公開・非表示OFFに保ち、下書き・廃止・非表示・削除への変更は先に統合解除を求める。 | 必須 |

#### 7.2.1 FAQの項目

| 項目 | 必須 | 内容 |
|---|---:|---|
| FAQ ID | 自動 | 内部で一意となり、登録後は変更しない。 |
| タイトル（質問文） | ○ | 接頭辞を含まない質問文を正本として保存する。検索結果では表示設定がONの場合だけ、現在のトップ分類名を`【トップ分類名】`として先頭へ動的に表示する。 |
| 所属分類 | ○ | 1～5階層のいずれか1分類を指定する。 |
| 概要 | ○ | 検索結果に表示する短い説明。Codex提案では、利用者が最初に把握すべき結論を原則1文で記載する。 |
| 簡易回答 | ○ | 画像、表、参考URL、コピー用テキスト枠などを含むリッチテキスト形式の回答。 |
| 症状 | － | 複数登録できる。 |
| 想定原因 | － | 複数登録できる。 |
| 対象OS・製品・機種 | － | 複数登録できる。 |
| エラーコード | － | 複数登録でき、完全一致を強く評価する。 |
| 対応手順 | － | 短い手順を番号付きで登録できる。 |
| 注意事項 | － | 作業前後の注意を複数登録できる。 |
| タグ | － | 複数登録できる。 |
| 検索用語 | － | FAQ固有の言い換えや別名を複数登録できる。 |
| 関連FAQ | － | 複数指定できる。 |
| 外部資料参照 | － | 回答内へ、ローカルパス・UNCパスをコピー用テキストとして、Web上の資料をHTTP/HTTPS参考URLとして複数記載できる。 |
| 添付画像 | － | 簡易回答へ挿入し、アプリ管理フォルダへ保存する。 |
| 状態 | ○ | 下書き、公開、廃止。初期値は下書き。 |
| 重要度 | － | 1～3。初期値は1。 |
| 新着表示終了日 | － | 設定時は指定日当日まで「新着」を表示する。日付を過ぎた後は自動的に表示しない。 |
| 更新表示終了日 | － | 設定時は指定日当日まで「更新」を表示する。新着と同時設定できる。 |
| 非表示 | － | 初期値はOFF。ONの場合、状態にかかわらず通常検索から除外し、管理画面からのみ管理する。 |
| 統合先FAQ | － | Codex統合FAQの公開後に利用者が確認した場合だけ、統合元から統合先への関係を記録する。元FAQの状態・非表示・削除とは独立し、解除できる。 |
| 作成日時 | 自動 | 初回保存時に記録する。 |
| 更新日時 | 自動 | 更新保存時に記録する。 |

簡易回答はリッチテキストエディターで編集する。リッチテキストはアプリが許可した要素だけを扱い、任意のHTML、JavaScript、iframe、埋め込みオブジェクトは保存・実行しない。画像や表で収まらない複雑なレイアウトや長い手順は、会社管理の外部資料へ分離し、コピー用テキストまたはHTTP/HTTPS参考URLで参照先を案内する。

Codex提案のタイトル本体には分類接頭辞を固定保存しない。簡易回答は1 FAQ 1目的とし、操作を伴う場合は`■手順`の番号付きリストを必要最小限かつ原則7項目以内で記載する。注意は関係する手順の近くへ`※注意`と明記し、色だけに依存しない。必要な補足だけを`■備考`へ記載し、補足リンクがある場合は本文の最後へ`■参考リンク`として表示名・参照目的とともに記載する。操作を伴わないFAQでは架空の手順を作らず、`■確認方法`や`■対応`など内容に合う見出しを用いる。正確性または安全性に必要な情報は、簡潔化のために削除しない。

本文へ通常入力または貼り付けたURLは、クリック機能を持たないプレーンテキストとして保存できる。プレーンテキストはURLとして解釈・実行しないため、URLスキームを理由にFAQ全体の保存を拒否しない。

利用者が明示的に「参考URL」を設定した箇所だけをクリック可能なリンクとして扱う。クリック可能な参考URLは `https://` または `http://` で始まるURLだけを許可し、クリック時は確認後に既定ブラウザーで開く。`javascript:`、`data:`、`file:`、実行ファイルへのリンクなど、危険な形式をクリック可能なリンクとして保存・実行しない。リンク設定は解除でき、表示文字列を通常テキストとして残せる。

ネットワークパス、ローカルパス、ファイル名、コマンド文字列など、正確な転記が必要な文字列は、利用者が選択してコピー用テキスト枠へ変換できる。コピー用テキスト枠はクリック可能なリンクへ変換せず、詳細画面の「コピー」操作では枠内文字列をプレーンテキストとしてクリップボードへ書き込むだけとする。アプリはクリップボードの既存内容を読み取らず、コピーした文字列を自動実行・自動オープンしない。

### 7.3 検索

| ID | 要件 | 優先度 |
|---|---|---|
| FR-SRCH-01 | キーワードまたは自然な日本語の文章を入力して検索できる。 | 必須 |
| FR-SRCH-02 | 検索範囲として「現在地以下」「現在地のみ」「すべて」を選択できる。初期値は「現在地以下」とする。 | 必須 |
| FR-SRCH-03 | 現在地が仮想ルートの場合、「現在地以下」は全分類を意味する。 | 必須 |
| FR-SRCH-04 | 現在地以下の検索には、現在地自身に所属するFAQも含める。 | 必須 |
| FR-SRCH-05 | FAQのタイトル、概要、簡易回答、症状、原因、対象、エラーコード、タグ、検索用語を横断検索する。 | 必須 |
| FR-SRCH-06 | 文字の全角・半角、英字の大文字・小文字、前後空白などを正規化する。 | 必須 |
| FR-SRCH-07 | 日本語の文章を検索可能な語または文字列に分解する。 | 必須 |
| FR-SRCH-08 | 管理された同義語辞書を使って検索語を展開する。 | 必須 |
| FR-SRCH-09 | 部分一致と完全一致を組み合わせる。 | 必須 |
| FR-SRCH-10 | 検索結果は初期状態で更新日の新しい順に表示し、利用者が更新日または重要度の昇順・降順へ切り替えられる。 | 必須 |
| FR-SRCH-11 | 検索結果を分類、タグ、状態、更新日で追加絞り込みできる。 | 推奨 |
| FR-SRCH-12 | 検索結果にタイトル、概要、所属階層、主要タグ、更新日、一致理由を表示する。 | 必須 |
| FR-SRCH-13 | 検索語が空の場合は、選択範囲内のFAQ一覧を表示する。 | 必須 |
| FR-SRCH-14 | 検索結果が0件の場合、その旨と検索範囲を表示する。 | 必須 |
| FR-SRCH-15 | 検索語、検索範囲、日時、件数をローカルに記録する。 | 必須 |
| FR-SRCH-16 | 会社管理の外部資料本体と外部Webページの本文は取り込まず、検索対象に含めない。コピー用テキスト枠の参照先文字列と参考URLの表示文字はFAQ回答の一部として検索できる。 | 必須 |
| FR-SRCH-17 | 検索結果からFAQ詳細を開いて一覧へ戻った場合、検索語、選択分類、下書き・廃止を含める条件を復元して自動的に同じ条件で検索し、可能な範囲で元のスクロール位置へ戻る。 | 必須 |
| FR-SRCH-18 | 検索画面に常時確認できる「条件を初期化」操作を設け、検索語、選択分類、下書き・廃止を含める条件を一括して初期状態へ戻す。初期状態では操作を無効表示する。 | 必須 |
| FR-SRCH-19 | タイトル分類表示設定がONの場合、検索結果の各FAQタイトル先頭へ所属分類から求めた現在のトップ分類名を`【トップ分類名】`として表示する。OFFの場合はタイトル本体だけを表示し、どちらの場合も保存済みタイトルと検索索引を変更しない。 | 必須 |
| FR-SRCH-20 | 検索結果は画面幅にかかわらず1列の横長カード一覧で表示する。各カードでは分類・状態・新着／更新、タイトル・概要、更新日・重要度、詳細への移動を同じ位置関係で示し、上から順に比較しやすくする。 | 必須 |
| FR-SRCH-21 | 統合先が記録された元FAQは、状態が公開かつ非表示OFFでも通常検索から除外する。FAQ管理画面と直接の詳細表示では保持し、統合先を確認できる。 | 必須 |
| FR-SRCH-22 | 検索結果は1ページ50件で返し、総件数、現在ページ、表示範囲、前後ページ、主要なページ番号を確認・選択できる。検索条件変更時は1ページ目へ戻し、FAQ詳細から戻る場合は現在ページも復元する。 | 必須 |
| FR-SRCH-23 | 分類ツリーは多数の分類を一覧しやすい行間で表示し、子分類を持つ分類ごとの開閉と「すべて開く」「すべて閉じる」を提供する。開閉状態はアプリ利用中だけ一時保持し、FAQまたは端末設定として永続保存しない。 | 必須 |
| FR-SRCH-24 | 並び替えは現在の検索条件に一致する全件へ適用してから1ページ50件に分割する。並び順変更時は1ページ目へ戻し、FAQ詳細から戻る場合は選択した並び順とページを復元する。「条件を初期化」では更新日（新しい順）へ戻す。 | 必須 |

#### 7.3.1 検索順位の初期方針

検索対象ごとに一致点を変え、合計点で順位を決める。具体値は実データを使った試験で調整する。

| 一致対象 | 初期の重み |
|---|---:|
| タイトルの完全一致・エラーコードの完全一致 | 10 |
| 症状 | 8 |
| タイトルの部分一致 | 8 |
| タグ・検索用語・対象 | 6 |
| 概要・原因 | 4 |
| 簡易回答・対応手順 | 1 |

複数の検索語に一致するFAQを、一語だけに一致するFAQより優先する。同点の場合は重要度、更新日時の順で並べる。

#### 7.3.2 「自然文検索」の範囲

MVPの自然文検索は生成AIによる意味理解ではなく、次の処理を組み合わせて実現する。

1. Unicode、全角・半角、英字大小、空白、記号を正規化する。
2. 入力文を日本語の語またはn-gramへ分解する。
3. 「の」「が」「です」など、検索への寄与が小さい語を除外する。
4. 同義語辞書とFAQ固有の検索用語を展開する。
5. 各FAQ項目を照合して点数を計算する。
6. 一致理由とともに結果を順位表示する。

このため、「PCの画面が真っ暗でスタートが開かない」のように具体語を含む文章には対応するが、具体的な症状を含まない曖昧な質問の意図推定は対象外とする。

### 7.4 同義語辞書

| ID | 要件 | 優先度 |
|---|---|---|
| FR-SYN-01 | 代表語と複数の同義語を登録できる。 | 必須 |
| FR-SYN-02 | 同義語を編集、削除できる。 | 必須 |
| FR-SYN-03 | 同じ語が矛盾する複数グループに登録される場合は警告する。 | 推奨 |
| FR-SYN-04 | 同義語辞書をJSONでエクスポート、インポートできる。 | 推奨 |
| FR-SYN-05 | 辞書変更後は、次回検索から変更を反映する。 | 必須 |

例：`パソコン / PC / 端末 / コンピューター`、`黒い画面 / 真っ黒 / ブラックスクリーン / 黒画面`。

### 7.5 FAQ詳細表示

| ID | 要件 | 優先度 |
|---|---|---|
| FR-VIEW-01 | FAQのタイトル、概要、回答、症状、原因、対象、手順、注意、タグを表示する。 | 必須 |
| FR-VIEW-02 | パンくず形式で所属階層を表示する。 | 必須 |
| FR-VIEW-03 | 関連FAQを表示し、選択するとその詳細へ移動する。 | 必須 |
| FR-VIEW-04 | 回答内の外部資料参照を表示し、ローカルパス・UNCパスはコピー操作、HTTP/HTTPS参考URLは接続先確認後の外部オープンとして区別する。 | 必須 |
| FR-VIEW-05 | FAQを開いた履歴と最終閲覧日時をローカルに記録する。 | 必須 |
| FR-VIEW-06 | 詳細画面から編集画面へ移動できる。 | 必須 |
| FR-VIEW-07 | リッチテキスト内の参考URLを選択すると、確認後にWindowsの既定ブラウザーで開く。 | 必須 |
| FR-VIEW-08 | 簡易回答内の画像と表を崩れずに表示する。 | 必須 |
| FR-VIEW-09 | 簡易回答内のコピー用テキスト枠へ、文字列全体をクリップボードへ書き込む「コピー」操作を表示し、成功または失敗を文字で通知する。枠内文字列は利用者が手動選択できる表示も維持する。 | 必須 |
| FR-VIEW-10 | 統合済みFAQの詳細には「統合済み」、統合先FAQへのリンク、通常検索から除外される旨、統合解除操作を表示する。 | 必須 |

### 7.6 外部資料参照

| ID | 要件 | 優先度 |
|---|---|---|
| FR-REF-01 | FAQに0件以上の外部資料参照を回答の一部として記載できる。 | 必須 |
| FR-REF-02 | ローカルパス、割り当て済みネットワークドライブ、UNCパス、ファイル名をコピー用テキスト枠へ保存し、文字列を正規化・補完せずそのままコピーできる。 | 必須 |
| FR-REF-03 | コピー用テキスト枠の内容をクリック可能なリンクへ自動変換せず、自動オープン、自動実行、存在確認を行わない。 | 必須 |
| FR-REF-04 | Web上の外部資料はHTTP/HTTPS参考URLとしてだけ登録でき、表示文字、接続先ホスト名、完全なURLを確認した後にWindows既定ブラウザーで開く。 | 必須 |
| FR-REF-05 | KnowledgeAppは外部資料本体を取り込み、読取り、変更、検索索引化、リンク切れ検査しない。外部資料の内容はFAQ検索結果へ影響しない。 | 必須 |
| FR-REF-06 | 外部資料本体のアクセス権、配置、更新、安全性、可用性、バックアップは会社側の管理対象とし、KnowledgeAppの完全バックアップには含めない。 | 必須 |
| FR-REF-07 | FAQ本文に保存した参照先文字列は通常のFAQデータとしてバックアップ・復元し、外部資料本体と区別する。 | 必須 |

### 7.7 編集画面

| ID | 要件 | 優先度 |
|---|---|---|
| FR-EDIT-01 | 基本情報、検索情報、回答、関連情報を見出しで分けて編集できる。外部資料参照は回答内のコピー用テキストまたは参考URLとして編集する。 | 必須 |
| FR-EDIT-02 | タグ、症状、原因、対象など複数値の項目を追加・削除できる。 | 必須 |
| FR-EDIT-03 | 所属分類をツリーまたは選択リストから指定できる。 | 必須 |
| FR-EDIT-04 | 必須項目の不足を項目単位で表示する。 | 必須 |
| FR-EDIT-05 | 保存成功、失敗と理由を表示する。保存失敗時は、保存ボタン付近に留まったまま見落とすことがないよう、原因と対処を示す画面上部の通知まで自動スクロールするか、同等に気づけるポップアップを表示する。 | 必須 |
| FR-EDIT-06 | 下書きのまま保存できる。 | 必須 |
| FR-EDIT-07 | 削除や廃止の前に確認を表示する。 | 必須 |
| FR-EDIT-08 | リッチテキストエディターで見出し、段落、太字、箇条書き、番号付きリストを編集できる。 | 必須 |
| FR-EDIT-09 | リッチテキストエディターで表を作成し、行・列の追加と削除ができる。 | 必須 |
| FR-EDIT-10 | ファイル選択または貼り付けにより画像を挿入できる。 | 必須 |
| FR-EDIT-11 | 画像に代替テキストを設定できる。 | 推奨 |
| FR-EDIT-12 | 表示文字列とURLを指定して参考リンクを挿入・編集・削除できる。 | 必須 |
| FR-EDIT-13 | クリック可能な外部URLは編集画面で形式を検証し、詳細画面では確認後に既定ブラウザーで開く。 | 必須 |
| FR-EDIT-14 | コピー＆ペーストされた内容から、スクリプトや許可していないHTML属性を除去する。 | 必須 |
| FR-EDIT-15 | PNG、JPEG、WebP、GIF形式の画像を扱い、画像1件あたりの上限は初期値10MBとする。 | 必須・調整対象 |
| FR-EDIT-16 | 回答へ入力・貼り付けたURLを通常テキストとして保持でき、選択した文字列だけを参考URLへ変更または通常テキストへ戻せる。 | 必須 |
| FR-EDIT-17 | 回答欄の通常段落は過大な上下余白を設けず、連続する文章をチャット入力欄に近い密度で読みやすく編集できる。見出し・箇条書き・表の区切りは維持する。 | 必須 |
| FR-EDIT-18 | 新着、更新、非表示をチェックボックスで個別設定できる。新着・更新をONにした時点で表示終了日が空欄の場合は、端末の現在日付から7日後を初期表示する。保存済みまたは利用者が入力した日付は上書きせず、日付は変更できる。新着・更新の表示終了日は必須とし、不足時は保存できない理由を画面上部に表示する。 | 必須 |
| FR-EDIT-19 | 回答内で選択した1～4,000文字をコピー用テキスト枠へ変換できる。枠内文字列を編集でき、不要になった場合は通常の文章へ戻せる。選択がない場合は変換せず、先に文字列を選択するよう案内する。 | 必須 |
| FR-EDIT-20 | 回答欄の直下には、画像の保存場所、参考URLのリンク化条件、コピー用テキスト枠の内部動作を説明する開発者向け補助枠を表示しない。これらの詳しい操作説明はマニュアルまたはヘルプで扱う。 | 必須 |
| FR-EDIT-21 | Codex統合案から作成したFAQの編集画面では統合元一覧を表示し、初回公開時の新着必須と、公開成功後に統合済み化を確認する流れを案内する。元FAQが委譲後に変更・削除されている場合は統合済み化せず理由と確認方法を表示する。 | 必須 |
| FR-EDIT-22 | 基本情報のタイトル、所属分類、重要度、概要を入力単位ごとの薄い背景と枠で区切り、ラベル、必須・任意・公開時必須の区別を文字でも明示する。未入力の必須項目と選択中の入力単位はテーマ色、入力エラーのある単位はエラー色でカード全体を強調する。新規FAQの所属分類は先頭分類を自動選択せず「選択してください」から開始し、利用者が明示的に選択する。既存FAQの分類は保存値を維持する。 | 必須 |
| FR-EDIT-23 | 回答区分の説明には「最初に結論を簡潔に示し、その後に手順や詳細を記載すると、読み手に伝わりやすくなります。」と表示し、結論を先に示してから詳細へ進む書き方を案内する。 | 必須 |

### 7.8 検索履歴と簡易分析

| ID | 要件 | 優先度 |
|---|---|---|
| FR-LOG-01 | 検索日時、検索文、検索範囲、結果件数を記録する。 | 必須 |
| FR-LOG-02 | 結果0件の検索を一覧表示できる。 | 必須 |
| FR-LOG-03 | 検索履歴と閲覧履歴は保存期間を設けず保持し、設定画面から期間指定または全件削除できる。 | 必須 |
| FR-LOG-04 | よく検索される語を集計し、検索キーワードの傾向を確認できる。 | 将来 |
| FR-LOG-05 | FAQごとの閲覧数を集計し、人気FAQの傾向を確認できる。 | 将来 |
| FR-LOG-06 | 「解決した／しなかった」を記録し、解決率を集計できる。 | 将来 |
| FR-LOG-07 | 検索結果0件または十分な一致がない検索語を集計し、追加すべきFAQの検討材料として表示できる。 | 将来 |
| FR-LOG-08 | 複数人で本格運用する段階では、検索・閲覧分析を管理者だけが閲覧できるよう権限制御する。 | 将来 |

### 7.9 バックアップ、復元、移行

| ID | 要件 | 優先度 |
|---|---|---|
| FR-BK-01 | FAQ、分類、タグ、同義語、関連情報をJSONへエクスポートできる。 | 必須 |
| FR-BK-02 | エクスポートしたJSONを検証し、インポートできる。 | 必須 |
| FR-BK-03 | データベース、設定、FAQ内画像をまとめた完全バックアップを作成できる。FAQ本文に保存した外部資料の参照先文字列を含めるが、外部資料本体は含めない。 | 必須 |
| FR-BK-04 | 完全バックアップから復元できる。 | 必須 |
| FR-BK-05 | 復元前に現在の状態を自動退避し、復元対象の概要を表示して確認を求める。 | 必須 |
| FR-BK-06 | 完全バックアップの保存先として、書き込み可能なローカルフォルダ、割り当て済みネットワークドライブ、またはUNCパスを選択できる。 | 必須 |
| FR-BK-07 | データ形式にバージョンを持たせ、将来の更新時に移行できる。 | 必須 |
| FR-BK-08 | 「フルバックアップ」ボタンから、利用者が任意の名前を付けて完全バックアップを作成できる。 | 必須 |
| FR-BK-09 | バックアップ名に使用できない文字や同名ファイルを検出し、修正または上書き確認を求める。 | 必須 |
| FR-BK-10 | 保存開始前に保存先への書き込み可否と必要容量を確認し、失敗時は作成途中のファイルを完成済みとして扱わない。 | 必須 |
| FR-BK-11 | 完全バックアップを使用して、別のPCへデータを移行・復元できる。 | 必須 |
| FR-BK-12 | 自動バックアップの頻度、保存先、保持世代数のスケジュール設定は今後の検討事項とする。 | 将来 |
| FR-BK-13 | 削除済みを除くFAQを、固定列順・形式バージョン付きのUTF-8 BOM・CRLF CSV（`*.knowledge-faq.csv`）へ書き出せる。回答本文と、内部UUIDではなく`FAQ-00001`／`CAT-00001`形式の人間向け管理IDを含め、Excelで編集できる。 | 必須 |
| FR-BK-14 | CSVはFAQ管理ID空欄を新規、登録済み管理IDを更新として扱い、行不足を削除とは扱わない。分類は登録済みの分類管理IDまたは分類パスだけを受け付け、CSVから分類を自動作成しない。 | 必須 |
| FR-BK-15 | CSV取込前に全行を検証し、新規・更新・変更なし・本文置換・書出し後更新・エラーの件数と行別理由を表示する。エラーが1件でもあれば全件を反映しない。 | 必須 |
| FR-BK-16 | 回答本文セルが書出し時ハッシュから変更されていない行は、現在DBのTiptap本文、表、画像、リンク、コピー用テキスト枠を保持する。変更された行はCSV文字列を安全なプレーンテキスト段落へ置換し、構造が外れることをプレビューで警告する。 | 必須 |
| FR-BK-17 | CSVの更新日時が現在DBより古くても、利用者確認後はCSVを優先して上書きする。書出し後更新件数を警告するが版競合を理由に拒否しない。 | 必須 |
| FR-BK-18 | CSV取込は確認したファイルのSHA-256一致を再確認し、取込直前に完全バックアップを自動作成してから、DB全体を1トランザクションで反映する。 | 必須 |
| FR-BK-19 | FAQ管理IDと分類管理IDは接頭辞と5桁以上の連番で自動採番し、名称変更・移動・論理削除で変更せず、削除後も番号を再利用しない。採番値が`99999`を超えた場合は6桁以上へ自動拡張して枯渇させない。 | 必須 |
| FR-BK-20 | FAQまたは分類の採番値が9万件へ達した段階を目安に、管理IDの桁数、画面・CSV表示幅、一括処理性能、外部運用手順への影響を再評価し、必要に応じて次期形式を設計する。 | 将来 |

### 7.10 表示設定

| ID | 要件 | 優先度 |
|---|---|---|
| FR-SET-01 | 設定画面で緑基調と青基調のカラーテーマを選択できる。 | 必須 |
| FR-SET-02 | 選択したテーマを、共通メニュー、検索、一覧、詳細、編集、管理、設定を含む全画面の主要操作色・選択色・フォーカス色へ直ちに反映する。 | 必須 |
| FR-SET-03 | 選択したテーマを端末内へ保存し、アプリ再起動後も維持する。 | 必須 |
| FR-SET-04 | 公開・成功、注意、エラーなど意味を持つ状態色はテーマから分離し、青テーマでも識別できる色と文字・アイコンを併用する。 | 必須 |
| FR-SET-05 | 検索結果のFAQタイトルへトップ分類名を表示する設定をON/OFFできる。初期値はONとし、選択値を端末内へ保存して再起動後も維持する。 | 必須 |
| FR-SET-06 | 金縁の片眼鏡と紺色の角帽を身につけた青灰色のちびフクロウを、約64×70ピクセルで主要操作や本文を隠さない左共通メニュー下部の空きスペースへ全画面共通で固定表示する。 | 必須 |
| FR-SET-07 | マスコットは通常時にコマ送りを行わない静止画で表示する。クリックまたはキーボードによる実行時だけ、手振り、ジャンプ、見回し、作業中、考え中、眠る、友達が遊びに来るから直前と異なる動作をランダムに1つ再生し、完了後は静止画へ戻る。 | 必須 |
| FR-SET-08 | マスコットはボタンとしてフォーカス・読み上げ可能にし、OSで動きを減らす設定が有効な場合は連続アニメーションを抑制する。 | 必須 |
| FR-SET-09 | 設定画面でマスコット表示をON/OFFできる。初期値はONとし、選択値を端末内へ保存して再起動、フルバックアップ、復元後も維持する。 | 必須 |
| FR-SET-10 | 表示中のマスコットを右クリックすると「マスコットを非表示にする」を選択でき、非表示後は設定画面から再表示できる。同じメニューはメニューキーまたはShift+F10でも開ける。 | 推奨 |
| FR-SET-11 | 友達は、フクロウより小柄な暖色系の茶トラ猫「ムギ」とし、紺色の輪郭、青緑のスカーフ、フクロウと共通するドット絵表現で識別できるようにする。 | 必須 |
| FR-SET-12 | ムギが遊びに来る動作の完了後は2体の静止画へ切り替える。ムギ滞在中のクリックでは、一緒にお茶を飲む、ハイタッチ、すり寄る、一緒に遊ぶ、一緒に本を読む、帰っていくから直前と異なる動作をランダムに再生する。帰宅以外は2体の静止画、帰宅後はフクロウだけの静止画へ戻る。2体を含む場面はスプライトセルの縦横比を維持し、横長または縦長に引き伸ばさない。 | 必須 |
| FR-SET-13 | Codexの分類カタログと提案箱の保存先を設定画面へ参照情報として表示する。保存先はappLocalDataDir配下の固定場所とし、入力欄、フォルダ選択、変更コマンドを設けない。 | 必須 |
| FR-SET-14 | 管理者は設定画面で、新規利用者追加および以後のパスワード再設定に空欄を許可するか切り替えられる。初期値は許可とし、許可をOFFにしても登録済み利用者のパスワードハッシュは変更せず、すでに空パスワードの利用者は引き続きログインできる。 | 必須 |

### 7.11 ローカル利用者認証と監査表示

| ID | 要件 | 優先度 |
|---|---|---|
| FR-AUTH-01 | アプリ起動時にログイン画面を表示し、認証前はFAQ、分類、ファイル、バックアップ等の操作コマンドを実行できない。 | 必須 |
| FR-AUTH-02 | 初期管理者のログインIDを`0000`、パスワードを空欄とする。 | 必須 |
| FR-AUTH-03 | 空パスワード許可設定の初期値はONとする。OFF後の新規利用者追加とパスワード再設定は1文字以上を必須とするが、設定変更前から空パスワードの利用者を無効化・強制変更しない。すべてのパスワードは平文ではなくArgon2ハッシュだけを保存する。 | 必須 |
| FR-USR-01 | 管理者はログインID、表示名、初期パスワード、管理者／一般利用者を指定して利用者を追加できる。ログインIDは正規化後に一意とする。 | 必須 |
| FR-USR-02 | 利用者は物理削除せず利用停止・再開できる。ログイン中の本人と最後の有効な管理者は利用停止にできない。 | 必須 |
| FR-USR-03 | 管理者は任意の利用者のパスワードを再設定できる。空パスワード許可設定がONの場合だけ空欄へ再設定でき、OFFの場合は1文字以上を必須とする。 | 必須 |
| FR-AUD-01 | FAQ新規作成時に作成者と更新者を記録し、編集、CSV取込、Codex修正、削除・復元では更新者をログイン利用者へ更新する。既存FAQはDB移行時に初期管理者へ帰属させる。 | 必須 |
| FR-AUD-02 | FAQ管理一覧は通常表示を初期状態として作成者・更新者を省略し、詳細表示へ切り替えた場合に作成者表示名と最終更新者表示名を表示する。狭い表示領域では一覧行をカード形式へ切り替え、監査情報を1文字単位で折り返さない。利用停止後も過去の表示名と参照を保持する。 | 必須 |

## 8. 画面要件

### 8.1 画面一覧

| 画面ID | 画面名 | 主な役割 |
|---|---|---|
| SCR-01 | FAQ検索・一覧 | 分類選択、自然文検索、絞り込み、結果表示 |
| SCR-02 | FAQ詳細 | 回答、関連FAQ、外部資料参照の表示・コピー・確認付き外部オープン |
| SCR-03 | FAQ編集 | 新規登録、リッチテキスト編集、複製、廃止、削除 |
| SCR-04 | 分類管理 | 追加、名称変更、移動、並べ替え、削除 |
| SCR-05 | 同義語管理 | 同義語グループの登録、編集、削除 |
| SCR-06 | 検索履歴 | 検索履歴と結果0件の確認 |
| SCR-07 | バックアップ・設定 | カラーテーマ、タイトル分類表示、マスコット表示、空パスワード許可方針、Codex連携用固定保存先の参照、任意名のフルバックアップ、保存場所、復元、エクスポート |
| SCR-08 | 管理者向け分析 | 検索語、人気FAQ、FAQ未登録ワードの分析（将来） |
| SCR-10 | Codex提案 | 新規・修正・統合案の確認、承認、却下、承認・却下履歴、最新却下案の再検討 |
| SCR-11 | ログイン | ログインID・パスワードによるローカル認証 |
| SCR-12 | 利用者管理 | 利用者追加、利用停止・再開、パスワード再設定（管理者専用） |
| SCR-13 | CSVインポート | CSV選択、全行プレビュー、警告・エラー確認、一括取込 |

- 共通ヘッダーのブランド補足は「Knowledge Base」とし、用途を簡潔に示す。

#### 8.1.1 共通FAQマスコット

- 共通App Shellの左共通メニュー下部へ、フクロウ単独時は約64×70ピクセル、ムギを含む場面は元セルの縦横比を維持した約88×95ピクセルで固定表示し、主要な入力欄、保存操作、ダイアログと重ならないようにする。
- 透明背景のドット絵スプライトシートを同梱静的素材として使用し、利用者FAQの添付画像・バックアップ・エクスポートとは分離する。
- 通常はフクロウだけ、またはムギ滞在中の2体を1枚の静止画で表示し、自動のコマ送りは行わない。クリックまたはEnter・Spaceでランダム動作を再生した後、現在の登場状態に応じた静止画へ戻る。
- フクロウ単独時は従来の手振り、ジャンプ、見回し、作業中、考え中に加えて、眠る、ムギが遊びに来る動作を候補とする。ムギ滞在中は、お茶、ハイタッチ、すり寄り、遊び、読書、帰宅を候補とする。
- ポインターを重ねた場合とキーボードフォーカス時だけ操作可能であることが分かる背景・枠を表示し、色だけに依存しない読み上げ名を付ける。
- `prefers-reduced-motion: reduce`ではクリック時の連続コマ送りを抑制し、選択された姿勢を短時間表示して登場状態に応じた静止画へ戻す。
- 設定画面の「マスコットを表示する」は初期値をONとし、OFFでは全画面から隠す。表示中は右クリックまたはキーボードのコンテキストメニュー操作でも非表示にでき、再表示は設定画面から行う。

### 8.2 FAQ検索・一覧画面

- 画面見出しは「FAQを探す」、補足は「必要な情報をすばやく検索」とし、「FAQナレッジ」など役割が重複する装飾文言を表示しない。
- 左側に行間を詰めた分類ツリーを配置し、子分類を持つ分類ごとの開閉と「すべて開く」「すべて閉じる」を設ける。分類名の選択操作と開閉操作は分離する。
- 右側上部に検索欄と検索範囲を配置する。
- 検索欄は複数語や文章を入力できる十分な幅を持つ。
- 検索パネルと検索入力欄は白の単色とし、背景グラデーションを使用しない。入力欄はテーマ色の明確な2ピクセル枠、パネルは周囲から区別できる実線枠で示し、影へ依存せず入力位置を判別できるようにする。
- 選択中の現在地をパンくずまたは見出しで明示する。
- 結果には一致したFAQの件数と、一致理由を表示する。
- 結果を選択するとFAQ詳細を表示する。
- 「新規FAQ」「分類を管理」への入口を設ける。
- 画面上部では見出し、説明、主要操作である「新しいFAQ」、検索欄を視線の流れに沿って配置し、検索操作と新規登録を見つけやすくする。
- 共通メニュー、分類絞り込み、検索結果の役割を領域ごとに分け、選択中の分類と「配下を含む」検索範囲を検索欄付近と結果見出しの両方で確認できるようにする。
- FAQ一覧は画面幅にかかわらず白を基調とした1列の横長カードで表示する。カード左側に分類、公開状態、新着・更新、タイトル、概要、右側に更新日、重要度、詳細への移動を配置し、タイトルと概要を上から比較しやすくする。狭い表示では右側情報をカード下部へ移して1列を維持する。
- FAQ一覧は1ページ50件とし、総件数と表示範囲を示す。複数ページでは前後およびページ番号から移動でき、ページ移動後は結果一覧の先頭を認識できるようにする。
- 結果見出し付近に並び替えを配置し、「更新日（新しい順）」「更新日（古い順）」「重要度（高い順）」「重要度（低い順）」を選択できるようにする。
- 配色は白を基調とし、設定で選択した落ち着いた緑または鮮明な青を主要操作の強調色に使う。色だけに依存せず文字、アイコン、選択状態を併用して現在地と状態を示す。
- 下書き・廃止を含める設定は通常の公開FAQ検索を妨げない補助条件として表示する。検索条件の近くに「条件を初期化」を常設し、選択中のキーワード、分類、下書き・廃止を含める条件を一操作で初期状態へ戻せるようにする。
- FAQ詳細の「一覧へ戻る」では、直前の検索語、分類、下書き・廃止を含める条件を復元して自動検索し、可能な範囲で元の結果位置を表示する。
- タイトル分類表示設定がONの場合は、各FAQカードのタイトルを`【現在のトップ分類名】タイトル本体`として表示する。分類名変更または別トップ分類への移動後は現在の分類階層から再計算し、OFFの場合はタイトル本体だけを表示する。

### 8.3 FAQ編集画面

- 「基本情報」「検索情報」「回答」「関連情報」に分ける。外部資料参照は回答内で編集する。
- 回答欄にリッチテキスト用ツールバーを置き、画像、表、参考URLを挿入できる。
- 入力エラーは保存時だけでなく、可能な範囲で入力中にも表示する。保存失敗時は原因と対処の表示位置へ自動的に移動し、キーボード操作でも通知を認識できるようにする。
- 配列項目はEnterキーや追加ボタンで登録し、個別に削除できる。
- ローカルパス・UNCパスはコピー用テキスト枠へ変換できるが、ファイルの存在確認や自動オープンは行わない。
- 保存、下書き保存、キャンセルを明確に区別する。
- 現在入力できる項目は「基本情報」「回答」「公開・表示設定」の独立した区分で一覧でき、画面上部の入力ナビゲーションから各区分へ移動できるようにする。区分を順番に完了しないと先へ進めない方式にはしない。
- タイトルと概要の文字数、回答の入力状況、公開時だけ必須となる項目、保存できない理由を、関係する入力欄の近くへ表示する。
- 基本情報の各入力項目は薄い背景を持つ入力単位として区切る。必須項目はテーマ色のラインと必須バッジ、任意項目は任意バッジ、概要は公開時必須のバッジで区別し、未入力、フォーカス中、入力エラーの状態を入力単位全体で強調する。新規FAQの所属分類は自動選択せず、利用者が「選択してください」から明示的に選ぶ。
- 下書き・公開・廃止は説明付きの選択肢として表示し、新着・更新・非表示の各設定と区別する。
- 回答ツールバーは文字、リスト、表・画像、参考URL、履歴の用途別にまとめ、表内では行・列の追加と削除を選択位置に対して実行できるようにする。
- 回答区分の見出し下には「最初に結論を簡潔に示し、その後に手順や詳細を記載すると、読み手に伝わりやすくなります。」と表示する。
- 回答ツールバーに「コピー用」を設け、選択したネットワークパスなどを専用枠へ変換できるようにする。専用枠ではコピー、文字列編集、通常の文章へ戻す操作を区別して表示する。
- 新着または更新をONにしたとき、その表示終了日が空欄なら端末の現在日付から7日後を表示する。既存の日付は上書きせず、利用者が変更できるようにする。
- 回答欄の直下には画像、参考URL、コピー用の開発者向け説明枠を置かず、詳しい操作説明はマニュアルまたはヘルプへ集約する。
- 画面下部へ保存状態、キャンセル、保存をまとめた追従領域を置き、現在選択中の状態と保存可否を確認できるようにする。
- 入力エラーは画面上部の一覧と該当項目の両方へ表示し、キーボード・読み上げ操作でもエラー項目を識別できるようにする。
- `Ctrl+S`で現在選択中の状態を保存できることを画面上で案内する。

### 8.4 バックアップ・設定画面

- 緑基調と青基調のテーマを、画面イメージが分かるプレビュー付きの選択肢として表示する。
- 選択したテーマは操作直後に全画面へ反映し、保存中・使用中・保存失敗を文字でも表示する。
- 状態を示す成功・注意・エラー色はテーマ切り替えの対象外であることを案内する。
- 検索結果のFAQタイトルへトップ分類名を表示するON/OFF設定を設け、接頭辞はタイトル本体へ保存されず分類変更へ自動追従することを説明する。
- 「フルバックアップ」ボタンを常時分かりやすい位置に表示する。
- バックアップ名と保存先を利用者が指定できる。
- 保存先にはローカルフォルダ、ネットワークドライブ、UNCパスを指定できる。
- バックアップ対象、推定サイズ、保存先を確認してから作成する。
- バックアップ作成の進行状況と成功・失敗理由を表示する。

### 8.5 Codex提案・履歴画面

- 確認待ちと承認・却下履歴をタブで切り替える。
- 新規案は本文と分類候補、修正案は現在FAQと修正後、統合案は統合元一覧と統合後を表示する。
- 提案を承認または却下でき、却下時は履歴へ残ることを確認画面で説明する。
- 同一依頼系列の最新却下案だけ「再検討へ戻す」を有効にし、古い案は閲覧専用の理由を表示する。
- FAQ詳細から1件の推敲・修正委譲、FAQ管理一覧から2～10件の統合委譲を開始し、Codexへ送る委譲番号付き依頼文を表示する。
- FAQ案では概要の結論を本文より先に表示し、本文内の手順、`※注意`、`■備考`、末尾の`■参考リンク`を提案どおりの順序で確認できる。

## 9. データ要件

### 9.1 論理データ構成

| データ | 主な内容 |
|---|---|
| categories | 分類名、説明、親分類、階層、表示順 |
| articles | FAQ基本情報、リッチテキスト回答、検索用プレーンテキスト、状態、重要度、日時 |
| article_symptoms | FAQの症状 |
| article_causes | FAQの原因 |
| article_targets | 対象OS、製品、機種 |
| article_error_codes | エラーコード |
| tags / article_tags | タグとFAQの多対多関係 |
| article_search_terms | FAQ固有の検索用語 |
| article_relations | 関連FAQ |
| article_attachments | FAQ内画像の相対パス、形式、サイズ、代替テキスト |
| synonym_groups / synonyms | 代表語と同義語 |
| search_logs | 検索文、範囲、結果件数、日時 |
| view_logs | FAQの閲覧日時と対象FAQの履歴 |
| codex_proposal_receipts | Codex提案の受付番号、作成したFAQ、取込日時。重複取込を防止する。 |
| codex_proposal_history | Codex提案の受付番号、依頼系列、種類、提案内容、確認状態、判断日時、反映先FAQ。 |
| article_merge_relations | 統合元FAQ、統合先FAQ、委譲時の元FAQ更新日時、統合済み化日時。 |

### 9.2 整合性

- データベースの外部キー制約を有効にする。
- FAQ保存、分類移動、関連FAQ更新はトランザクション内で処理する。
- 分類階層の循環と6階層以上を、画面側とデータ保存側の両方で防止する。
- 内部のFAQ ID・分類IDと、人間向けFAQ管理ID・分類管理IDは名称変更や移動で変化させない。管理IDの採番済み番号は削除後も再利用しない。
- 同じ関連FAQの重複登録と自己参照を禁止する。
- 削除済みFAQは通常検索と関連FAQ候補から除外する。
- 統合済み関係は元FAQ1件につき最大1件とし、自己参照を禁止する。記録時は統合FAQが公開済みかつ新着期限設定済みであり、全元FAQが委譲時から未変更・未削除であることを再確認し、途中失敗時は全件をロールバックする。

### 9.3 保存先の初期案

```text
KnowledgeApp/
├─ KnowledgeApp.exe
├─ data/
│  └─ knowledge.db
├─ attachments/
│  └─ articles/
├─ manuals/                 # 旧版バックアップ互換用。現行画面からは追加しない
├─ backup/
└─ settings/
```

会社PCの規定やWindowsの書き込み権限に合わせ、実行ファイルと利用者データの保存場所を分離できる構成とする。

実行ファイルの更新でFAQデータが失われないよう、利用者データの標準保存先はWindowsの利用者別アプリデータフォルダとする。ポータブル版では、利用者が明示的に選択したデータフォルダを使用できるようにする。

## 10. 非機能要件

### 10.1 操作性

| ID | 要件 |
|---|---|
| NFR-USE-01 | 通常起動にコマンド操作を要求しない。 |
| NFR-USE-02 | よく使う検索、閲覧、登録操作をマウスとキーボードの両方で行える。 |
| NFR-USE-03 | エラーは原因と次に行う操作を日本語で表示し、保存操作の位置から離れた場所に表示する場合は自動スクロールまたはポップアップで利用者へ通知する。 |
| NFR-USE-04 | 削除、復元、上書きなど影響の大きい操作には確認を表示する。 |
| NFR-USE-05 | 画面幅1280×720以上で主要操作が行える。 |
| NFR-USE-06 | カラーテーマを切り替えても、本文と背景、主要ボタン、フォーカス表示に十分な判別性を持たせ、色だけで状態を伝えない。 |
| NFR-USE-07 | マスコットは主要操作を妨げない小さい表示とし、マウス・キーボード・読み上げ操作と動きを減らすOS設定に対応する。 |

### 10.2 性能

| ID | 要件 |
|---|---|
| NFR-PERF-01 | FAQ 10,000件、分類1,000件を想定上限として設計する。 |
| NFR-PERF-02 | 一般的な業務用PCで、通常検索の結果を原則1秒以内に表示する。 |
| NFR-PERF-03 | 分類ツリーの展開やFAQ詳細表示を、操作後原則0.5秒以内に行う。 |
| NFR-PERF-04 | 大量データ時も画面を固めないよう、必要に応じて一覧を分割表示する。 |

性能値はテスト環境と実データの量が確定した後に再調整する。

### 10.3 可用性・データ保護

| ID | 要件 |
|---|---|
| NFR-DATA-01 | アプリを再起動しても保存済みデータが保持される。 |
| NFR-DATA-02 | 保存中の異常終了でデータベース全体が破損しないようにする。 |
| NFR-DATA-03 | 復元操作を除き、既存バックアップを無断で上書きしない。 |
| NFR-DATA-04 | データベース形式の更新前にバックアップを作成する。 |
| NFR-DATA-05 | ネットワーク保存先が一時的に利用できない場合も通常のFAQ利用を妨げず、バックアップ失敗を明示する。 |

### 10.4 セキュリティ・プライバシー

| ID | 要件 |
|---|---|
| NFR-SEC-01 | 通常の検索、閲覧、手動編集では外部サーバーへデータを送信しない。Codex連携は、利用者が会話で明示的に提供・送信を許可した内容だけを対象とする。 |
| NFR-SEC-02 | アプリに分析用通信や広告用通信を組み込まない。 |
| NFR-SEC-03 | リッチテキストは許可要素だけを保存・表示し、スクリプト、イベント属性、iframeなどを除去する。 |
| NFR-SEC-04 | KnowledgeAppは外部資料本体やコピー用パスを読み込み・実行せず、外部資料のJavaScriptや外部リソースをアプリ内で動作させない。HTTP/HTTPS参考URLを既定ブラウザーで開いた後は、会社のブラウザー・ネットワーク・アクセス制御の管理範囲とする。 |
| NFR-SEC-05 | 検索履歴を含む全データをローカルに保存し、利用者が削除できるようにする。 |
| NFR-SEC-06 | パスワード、秘密鍵、個人情報をFAQへ保存しない旨をFAQ作成・編集画面へ小さい補足文として表示し、共通メニューには表示しない。 |
| NFR-SEC-07 | クリック可能な参考URLはHTTP/HTTPSだけを許可し、利用者の確認操作後にWindowsの既定ブラウザーで開く。通常テキスト内のURL文字列はリンクとして解釈・実行しない。 |
| NFR-SEC-08 | ソース管理の除外設定と自動検査により、利用者がローカル環境で作成したFAQを含むデータベース、添付画像、検索・閲覧履歴、バックアップをGitHub等へ誤登録しない。会社管理の外部資料もリポジトリへ複製しない。 |
| NFR-SEC-09 | リポジトリの公開範囲は会社の規定に従い、規定確認までは非公開を推奨する。 |
| NFR-SEC-10 | Codexへ公開する分類カタログは分類ID・名前・説明・階層に限定し、FAQ本文、件数、履歴、添付、外部資料の参照先・内容を含めない。 |
| NFR-SEC-11 | Codex提案は1MB上限、厳格なJSON形式、許可リッチテキスト、HTTP/HTTPSリンク、最大5階層、同名分類、親分類存在をRust側で再検証する。 |
| NFR-SEC-12 | Codex提案箱、分類カタログ、処理途中ファイルをリポジトリ外へ置き、Git除外とコミット前・CI検査の対象にする。 |
| NFR-SEC-13 | 既存FAQをCodexへ渡す場合は、利用者がアプリ上で明示的に選択した1～10件だけを委譲ファイルへ出力し、DB、履歴、未選択FAQ、添付画像本体を含めない。 |
| NFR-SEC-14 | Codex提案の元FAQ IDと更新日時が委譲ファイルおよび現在DBと一致することをRust側で検証し、対象差替えや古い内容の上書きを防ぐ。 |
| NFR-SEC-15 | コピー用テキスト枠にはクリップボードへのプレーンテキスト書き込みだけを許可し、クリップボード読取、HTML・画像書込、自動オープン、自動実行の権限を与えない。 |

### 10.5 保守性

| ID | 要件 |
|---|---|
| NFR-MNT-01 | データベースのスキーマバージョンを管理する。 |
| NFR-MNT-02 | 検索の重み、除外語、同義語をプログラム全体の改修なしに調整しやすくする。 |
| NFR-MNT-03 | エラー記録には機密情報やFAQ本文全体を不要に含めない。 |
| NFR-MNT-04 | 将来SQLiteから共有型データベースへ移行しやすいよう、画面処理とデータ処理を分離する。 |
| NFR-MNT-05 | 画面、業務処理、データアクセス、検索処理を分離し、自動テスト可能な構成にする。 |
| NFR-MNT-06 | GitHub等のリリース単位でアプリのバージョン、変更内容、データベース移行内容を記録する。 |
| NFR-MNT-07 | 開発時は要件定義書と基本設計書への影響を必ず確認し、仕様・画面・データ・処理・運用が変わる場合は実装と同じ作業内で文書を更新する。 |
| NFR-MNT-08 | 実装が設計書と異なる場合は、差異の理由と影響を設計書へ反映し、文書と実装を一致させる。 |
| NFR-MNT-09 | ソースコード、設定、SQL、設計文書、PowerShellスクリプトをUTF-8（BOMなし）・LFで管理し、コミット前とCIで不正な文字コード、BOM、置換文字、NUL文字、異なる改行コードを検出する。 |
| NFR-MNT-10 | JSONなどの文字列ファイルを生成・読取する処理はOS既定文字コードへ依存せずUTF-8を明示し、日本語を保存・再読込しても同じ内容を維持する。 |

## 11. 推奨システム構成

Windows 11での使い心地、配布サイズ、オフライン動作、リッチテキスト編集、今後の保守性を考慮し、次の構成を採用候補とする。

| 層 | 推奨内容 |
|---|---|
| アプリ形式 | Windows 11 64ビット向けデスクトップアプリ |
| デスクトップ化 | Tauri 2 |
| 画面 | React＋TypeScript＋Vite＋HTML＋CSS |
| リッチテキスト | Tiptap。保存形式は構造化JSONを正本とし、表示時に許可要素だけを描画する。コピー用テキスト枠は文字列属性を持つ許可ノードとして扱う。 |
| バックエンド | Rust。データベース、ファイル、バックアップ、パス検証を担当する。 |
| データ | SQLite。階層取得は再帰クエリ、文字列検索は正規化データとFTS5 trigram等を組み合わせる。 |
| 外部資料参照 | ローカルパス・UNCパスはコピー用テキスト、Web上の資料はHTTP/HTTPS参考URL。資料本体は会社管理の既存ディレクトリまたはWebサーバーに置く。 |
| FAQ内画像 | 利用者別アプリデータフォルダ内の添付ファイル。データベースには相対パスを保存する。 |
| 検索 | ローカル全文検索＋日本語の文字列分割＋同義語辞書＋項目別の重み付け |
| バックアップ・一括編集 | Excel編集用CSV、MVP必須の検証付きJSON入出力、データベース・添付画像・利用者情報を含む完全バックアップ。外部資料本体は含めない。 |
| 配布 | GitHub等のリリースから取得するNSIS形式の利用者単位インストーラーを基本とする。 |
| テスト配布 | 必要に応じてポータブルZIP版を併用する。 |
| Codex連携 | リポジトリで管理するKnowledgeApp専用Codexプラグイン（スキル＋固定用途PowerShellコマンド）と、appLocalDataDir内の分類カタログ・明示委譲ファイル・提案箱・提案履歴。従量課金を避けるためアプリ内へAPIキーや生成AIモデルを組み込まず、定額利用のCodexへ必要時に委譲する。 |

### 11.1 この構成を推奨する理由

- Tauri 2はWindowsでWebView2を利用し、Windows 11にはWebView2がOSの一部として配布されるため、Electronのようにブラウザー本体をアプリごとに同梱する構成より軽量にしやすい。
- NSISインストーラーは標準で現在の利用者向けにインストールでき、管理者権限なしで導入する構成を選べる。
- React＋TypeScriptは、分類ツリー、検索結果、複数入力欄、リッチテキストなど状態の多い画面を部品化し、型検査で変更時の不整合を検出しやすい。
- TiptapはReactへ統合でき、表と画像の拡張が用意されている。保存形式を構造化JSONに限定することで、任意HTMLを直接保存するより検証しやすい。
- SQLiteはサーバーを必要とせず、一人利用、検索履歴、関連FAQ、5階層の再帰取得、バックアップに適する。
- SQLite FTS5のtrigram tokenizerは部分文字列検索を扱えるため、日本語の表記揺れ・同義語処理と組み合わせる候補になる。実データで検索精度と速度を評価して最終方式を決める。
- Rust側へデータベースとファイル操作を集約すると、画面から任意ファイルや任意SQLを直接操作させず、将来のデータ構成変更も一か所へまとめやすい。

### 11.3 C#移行の将来検討

- 現行のTauri＋React版は、FAQ編集表とリッチテキストの視認性を維持したまま個人利用と会社デモへ使用する。
- 会社標準に合わせる必要が確定した場合は、`.aspx`・C#・CSSによるASP.NET Web Forms系Webアプリ、または.NET Framework 4.8のC#デスクトップ`.exe`を候補とする。
- 移行可否はあるが、画面を機械的に置き換えず、会社デモ後に配布方式、IIS・DB・認証の社内標準、複数人同時利用の要否を確認して判断する。
- 移行時はCSV形式と業務ルールを互換境界にできるが、Tiptap本文、画像、表、SQLite FTS5検索、ローカルパス安全性は個別の再設計・移行テストを要する。視認性が現行より劣る場合は移行を後回しにする。

### 11.2 インストールと更新方針

1. 個人PCで開発・テストし、Windows向けリリースを作成する。
2. ソースコードとリリース成果物をGitHub等で管理する。
3. 自宅PCと会社PCへNSIS形式の利用者単位インストーラーから導入する。通常は管理者権限を要求しない設定とする。
4. FAQ、添付画像、履歴は、インストール先ではなく利用者別アプリデータフォルダへ保存する。会社管理の外部資料本体は元のディレクトリまたはWebサーバーで管理する。
5. 更新前にフルバックアップを作成し、新しいインストーラーで上書き更新する。
6. 自動更新機能はMVPに含めず、GitHub等のリリースを確認して手動更新する。

ポータブルZIP版はインストーラーを使えない場合や試用時の補助とする。通常運用は、ショートカット、アンインストール、データ保存場所を明確に管理しやすいインストーラー版を推奨する。

### 11.3 技術上の注意

- 外部ライブラリはバージョンを固定し、ロックファイルをソース管理する。
- Tauriのファイルアクセス権限は、利用者が選択したバックアップ先とアプリ管理フォルダに限定する。
- リッチテキスト画像をBase64でデータベースへ埋め込まず、管理フォルダ内の実ファイルとして扱う。
- ローカルパス・UNCパスはコピー専用とし、KnowledgeAppから自動オープン・自動実行しない。HTTP/HTTPS参考URLは接続先確認後に既定ブラウザーへ委譲し、ブラウザーで表示する内容の安全性は会社のブラウザー・ネットワーク管理に従う。
- GitHub等のリポジトリには除外設定を設け、実データが含まれていないことをリリース前に自動確認する。

### 11.4 参照した公式資料

- [Tauri 2：Windowsの事前準備](https://v2.tauri.app/start/prerequisites/)
- [Tauri 2：Windowsインストーラー](https://v2.tauri.app/distribute/windows-installer/)
- [Tauri 2：ファイルシステムとアクセス権限](https://v2.tauri.app/plugin/file-system/)
- [Tiptap：Reactへの導入](https://tiptap.dev/docs/editor/getting-started/install/react)
- [Tiptap：表](https://tiptap.dev/docs/editor/extensions/nodes/table)
- [Tiptap：画像](https://tiptap.dev/docs/editor/extensions/nodes/image)
- [Tauri 2：クリップボード](https://v2.tauri.app/plugin/clipboard/)
- [SQLite：FTS5とtrigram tokenizer](https://www.sqlite.org/fts5.html)

## 12. 受入条件

### 12.1 分類

1. 1～5階層の分類を作成できる。
2. 5階層目の下へ分類を追加しようとすると拒否され、理由が表示される。
3. 分類を移動して配下の最深部が6階層になる場合、移動が拒否される。
4. 分類を自分の子孫へ移動できない。
5. FAQまたは子分類が残る分類を誤って削除できない。

### 12.2 FAQ

1. 必須項目を入力してFAQを保存し、アプリ再起動後も表示できる。
2. FAQを編集、複製、下書き化、公開、廃止、削除できる。
3. 下書きと廃止FAQは通常検索結果に表示されない。
4. 関連FAQを選択すると正しいFAQ詳細を開ける。
5. 簡易回答に画像、表、見出し、箇条書き、クリック可能な参考URLを登録できる。
6. FAQを再度編集しても、リッチテキストの構造と添付画像が維持される。
7. 危険なURL形式、スクリプト、iframeを貼り付けても実行されない。危険なURL形式を通常テキストとして入力した場合は、文字列を失わずFAQを保存できる。
8. FAQ保存が失敗した場合、画面下部の保存操作位置から原因・対処の通知位置へ自動的に移動するか、同等のポップアップが表示される。
9. 新着・更新フラグを個別または同時に設定できる。空欄のフラグをONにすると端末の現在日付から7日後が表示終了日の初期値になり、利用者が変更できる。指定した終了日当日までは表示され、翌日から表示されない。
10. 非表示をONにした公開FAQは通常検索に表示されず、FAQ管理画面から詳細確認と編集ができる。
11. 回答欄で連続して段落を入力しても、段落間に過大な空白が入らず読みやすい。
12. 従来の「新しいFAQ」から一から作成できる。
13. Codex提案に既存分類候補がある場合、候補と理由を確認し、候補外も含む現在の分類から所属先を選べる。
14. 適切な既存分類がない場合、Codexが提案した新規分類1件を利用者の承認後に作成し、FAQ下書きと同じトランザクションで登録できる。
15. Codex提案から取り込んだFAQは公開されず下書きになり、同じ受付番号を再度取り込んでも重複FAQが作成されない。
16. 提案後に親分類が削除された場合、6階層化する場合、または同名分類が作成済みの場合は取込を拒否し、分類を選び直す方法を表示する。
17. 不明な項目、1MB超過、危険なリンク、任意HTML、新規・統合時の画像、または修正時に変更された画像参照を含むCodex提案を拒否し、DBを変更しない。
18. 既存FAQ1件を委譲して修正案を作り、現在内容と案を確認して承認すると、分類・状態・表示設定・画像を維持したまま本文関連項目だけが更新される。
19. 委譲後に元FAQを編集した場合、古い修正案は反映されず、現在のFAQから再委譲する案内が表示される。
20. 2～10件のFAQを統合委譲し、承認すると新しい下書きが作成され、元FAQは変更・削除されない。
21. 提案を却下しても履歴から内容を確認でき、同じ依頼系列の最新却下案だけを再検討へ戻せる。
22. 最新案より古い却下案は再承認できず、閲覧専用である理由を表示する。
23. Codexが作成したFAQで、概要の結論、番号付き手順、該当する注意・備考、本文末尾の参考リンクが標準順序で表示され、不要な空見出しや未確認情報が含まれない。
24. FAQ編集画面で基本情報、回答、公開・表示設定を同時に確認でき、上部の入力ナビゲーションから各区分へ移動できる。
25. 下書き・公開・廃止と、新着・更新・非表示が別の設定として判別でき、現在の保存状態と保存可否を画面下部で確認できる。
26. 公開に必要な概要または回答が不足している場合、画面上部の一覧と該当入力欄の両方に不足理由が表示される。
27. 回答ツールバーから段落、見出し、リスト、表、画像、参考URL、元に戻す・やり直すを用途別に選択でき、表内では行・列の追加と削除を実行できる。
28. Codexが画像・スクリーンショットを必要と判断した新規・統合FAQでは、提案JSONに画像を含めず、送信後に追加対象の手順、画像内容、挿入位置、代替テキスト案、著作権・機密情報の確認事項が案内される。画像が不要な場合または権利・機密性を確認できない場合は、無理に画像追加を求めず文章だけで完結する。
29. Codexが作成する新規・統合FAQのタイトル本体に`【分類名】`が固定保存されず、修正案でも表示設定を理由に分類接頭辞が追加・削除されない。
30. 回答内のネットワークパスを選択してコピー用テキスト枠へ変換し、保存後のFAQ詳細で「コピー」を選ぶと、円記号、空白、日本語、長いファイル名を含む文字列が一字も変わらずクリップボードへ書き込まれる。コピー操作だけではファイルやフォルダが開かず、クリップボードの既存内容を読み取らない。
31. FAQ作成・編集画面にパスワード、秘密鍵、個人情報を登録しない旨が小さい補足文で表示され、メイン画面の共通メニューには同じ注意枠が表示されない。
32. Codex提案画面の見出し部分に「Codexからの提案」と「新規下書き、既存FAQの推敲・修正、複数FAQの統合案を確認できます。」が表示され、「内容を確認してから反映する」とDBを直接変更しない旨の補足文は表示されない。
33. Codex提案画面に「Codexへの委譲方針」のバナーと、従量課金API・定額利用・明示委譲を説明するバナー本文が表示されない。
34. 確認待ちの提案が0件の場合は「確認待ちの提案はありません」だけが空表示の文言として表示され、新規依頼・修正委譲・統合委譲の導線説明は表示されない。
35. FAQ作成・編集画面の回答欄直下に、画像の保存場所、参考URLのリンク化条件、コピー用テキスト枠の内部動作を説明する3つの補助枠が表示されない。
36. Codex統合案の下書きを公開へ変更すると新着がONになり、表示終了日に端末の現在日付から7日後が入る。新着期限なしでは画面とRust側の両方で公開保存できない。
37. 統合FAQの公開成功後に確認が表示され、承認すると元FAQが「統合済み」として通常検索から除外され、管理・詳細画面から統合先へ移動できる。確認を取り消した場合は元FAQを変更しない。
38. 統合元のいずれかが委譲後に変更・削除されている場合は全件の統合済み化を中止し、部分的な関係を残さない。統合解除後は本文・状態・非表示を変更せず、元の設定に従って検索対象へ戻る。統合関係が残る統合先を非表示・下書き・廃止・削除にはできない。
39. FAQ作成・編集画面の基本情報で、タイトル、所属分類、重要度、概要が入力単位ごとの薄い背景と枠で分かれ、必須・任意・公開時必須を文字で判別できる。新規FAQの所属分類は「選択してください」で始まり、選択するまで保存できない。未入力、キーボードフォーカス、入力エラーは色だけに依存せず、既存のラベル・バッジ・プレースホルダー・エラー文と併用して項目単位で確認できる。
40. FAQ作成・編集画面の回答区分に、結論を簡潔に示してから手順や詳細を記載する書き方の案内が表示され、旧案内文は表示されない。
41. FAQ管理一覧の初期状態では作成者・更新者列を省略し、「詳細」へ切り替えると作成者と更新者が読める幅で表示される。狭い画面ではカード形式へ切り替わり、編集・CSV取込後は更新者が現在利用者へ変わる。
42. CSVで回答本文を変更しない行は表・画像等を保持し、変更した行だけプレーンテキスト段落へ置換される。
43. 書出し後に更新されたFAQを含むCSVでも警告確認後はCSV内容で上書きでき、エラー行がある場合は1件も反映されない。
44. 設定画面で空パスワード許可をOFFにすると、以後の利用者追加とパスワード再設定では空欄を拒否する一方、切替前から空パスワードの利用者は引き続きログインできる。利用者追加画面には空欄許可を説明する補足文を表示しない。
45. FAQ管理一覧の主要操作は「編集」を常設し、「複製」「統合を解除」「削除」を行単位の「その他の操作」メニューへまとめる。表示幅が狭くてもFAQタイトル、状態、最終更新、操作が互いに1文字幅まで圧縮されない。
46. CSVへ内部UUIDが出力されず、既存FAQは`FAQ-00001`形式、分類は`CAT-00001`形式の管理IDで識別できる。管理ID空欄の行を新規登録すると次の未使用番号が割り当てられ、削除済み番号は再利用されない。

### 12.2.1 認証・利用者管理

1. 初回起動後、ログインID`0000`と空欄パスワードでログインできる。
2. 未ログイン状態ではFAQ・分類・CSV・バックアップ等の保護対象コマンドを実行できない。
3. 管理者は利用者を追加し、利用停止・再開、パスワード再設定を行える。空パスワード許可がONなら空欄を使用でき、OFFなら新規追加と再設定で空欄を拒否する。
4. 一般利用者は利用者管理画面・利用者管理コマンドを使用できない。
5. ログイン中の本人と最後の有効な管理者を利用停止にできず、利用停止中の利用者はログインできない。
6. 空パスワード許可をOFFにしても、すでに空パスワードの利用者の認証情報は書き換えず、空欄のままログインできる。

### 12.3 検索

1. 「現在地以下」で検索すると、現在地と子孫分類のFAQだけが結果に出る。
2. 「現在地のみ」では子孫分類のFAQが結果に出ない。
3. 「すべて」では別の枝のFAQも検索対象になる。
4. 「PC」「パソコン」など登録済み同義語のどちらでも同じFAQ候補を得られる。
5. 「Windowsの画面が真っ暗でスタートが開かない」のような文章で、該当語を持つFAQが上位に出る。
6. エラーコードの完全一致が一般的な本文一致より上位に出る。
7. 結果0件の検索が履歴画面で確認できる。
8. 1280×720以上では、共通メニュー、分類絞り込み、検索欄、主要操作、FAQ一覧を同一画面で確認でき、FAQカードが1列の横長一覧として表示される。各カードの分類・状態、タイトル・概要、更新日・重要度が同じ位置に揃い、カード全体から詳細へ移動できる。
9. 分類を選択した場合、選択分類とその配下が検索範囲であることを画面上で確認でき、「すべての分類」へ戻せる。
10. キーワード・分類・下書き表示を指定した検索結果からFAQ詳細を開き、「一覧へ戻る」を選ぶと同じ検索条件と結果一覧が自動的に復元され、可能な範囲で元の表示位置へ戻る。
11. 「条件を初期化」を選ぶと、キーワードは空、分類は「すべての分類」、下書き・廃止を含める設定はオフへ一括で戻り、初期状態ではボタンが無効表示になる。
12. 共通ヘッダーに「Knowledge Base」、検索画面に「FAQを探す」と「必要な情報をすばやく検索」が表示され、「自分の知識を、すぐ見つかる形に」「FAQナレッジ」「知りたいことを探す」の旧文言は表示されない。
13. 検索結果が51件ある場合、1ページ目に最大50件、2ページ目に残り1件が表示され、総件数、表示範囲、現在ページを確認できる。検索条件変更時は1ページ目へ戻り、FAQ詳細から戻る場合は元のページを復元する。
14. 子分類を持つ分類を個別に閉じるとその子孫だけが隠れ、「すべて閉じる」「すべて開く」で全枝を切り替えられる。分類名の選択は開閉を変えず、開閉状態はアプリ終了後の端末設定やFAQデータへ残らない。
15. 並び順を変更すると、現在の検索条件に一致する全件が選択した更新日または重要度の順で並び替えられ、1ページ目へ戻る。FAQ詳細から一覧へ戻る場合は並び順とページが復元され、「条件を初期化」で更新日（新しい順）へ戻る。
16. 検索パネルと入力欄に背景グラデーションがなく、白の単色背景と明確な実線枠によって、緑・青のどちらのテーマでも入力位置を判別できる。

### 12.4 外部資料参照

1. `C:\...`、割り当て済みネットワークドライブ、`\\server\share\...`などの参照先をコピー用テキスト枠へ保存し、改行、空白、円記号を変えずにコピーできる。
2. コピー用テキスト枠を選択しても、KnowledgeAppがファイルやフォルダを自動オープン・自動実行せず、存在確認もしない。
3. HTTP/HTTPS参考URLを選択すると、表示文字、接続先ホスト名、完全なURLを確認した後に既定ブラウザーで開ける。
4. `file:`、`javascript:`、`data:`などをクリック可能な参考URLとして保存・実行できない。
5. 外部資料本体の内容はFAQ検索結果へ影響せず、KnowledgeAppのバックアップにも含まれない。
6. FAQをバックアップ・復元すると、回答内の参照先文字列とHTTP/HTTPS参考URLが維持される。

### 12.5 バックアップ

1. JSONエクスポート後、新しいデータ環境へFAQと分類を取り込める。
2. 完全バックアップから、FAQ、分類、設定、画像を復元でき、FAQ本文内の外部資料参照も維持される。外部資料本体は復元対象に含まれない。
3. 復元前の状態が自動退避される。
4. インターネット接続がない状態でも、主要機能を利用できる。
5. 任意名を付けたフルバックアップを、選択したローカルフォルダまたはネットワークドライブへ保存できる。
6. 完全バックアップを別PCへ復元できる。
7. ネットワークドライブが利用できない場合、元データを損なわずに失敗理由を表示する。

### 12.6 配布と更新

1. Windows 11 64ビット環境でインストーラーから導入し、ショートカットから起動できる。
2. インターネットに接続しない状態でも、参考URLを開く機能以外の主要機能を利用できる。
3. アプリを更新しても、利用者別フォルダのFAQ、画像、履歴が維持され、FAQ本文内の外部資料参照も失われない。
4. GitHub等のソースとリリース成果物に実データ、履歴、添付画像、バックアップ、会社管理の外部資料が含まれない。
5. 日本語を含むソース、設計文書、SQL、JSON、PowerShellスクリプトがUTF-8のまま検査・ビルドされ、画面文言と生成JSONに文字化けがない。

### 12.7 表示設定

1. 設定画面でブルーを選ぶと、主要ボタン、リンク、ナビゲーション、選択状態、フォーカス、カード、FAQ編集画面の強調色が青へ切り替わる。
2. グリーンを選ぶと従来の緑基調へ戻せる。
3. アプリを終了して再起動しても、最後に選択したテーマが維持される。
4. テーマを切り替えても、公開・成功、注意、エラーの状態色と文字・アイコンによる説明が維持される。
5. テーマ設定の保存に失敗した場合は元のテーマへ戻し、原因と対処を日本語で表示する。
6. トップ分類名表示をONにすると、下位分類に所属するFAQも現在のトップ分類名を`【トップ分類名】`として検索結果タイトルの先頭へ表示する。分類名変更・FAQ移動後は自動追従し、OFFにすると保存済みタイトルを変更せず接頭辞だけが消える。設定は再起動後も維持される。
7. 全画面でFAQマスコットが左共通メニュー下部の空きスペースへ固定表示され、主要な検索・編集・保存操作とダイアログを妨げない。
8. マスコットはクリックされるまで静止画のまま変化せず、クリックまたはキーボードで実行した場合だけ、手振り、ジャンプ、見回し、作業中、考え中、眠る、ムギが遊びに来るのいずれかが直前と重複せず再生され、完了後に静止画へ戻る。
9. ムギが遊びに来た後は2体の静止画になり、クリックするとお茶、ハイタッチ、すり寄り、遊び、読書、帰宅のいずれかを再生する。帰宅後はフクロウだけの静止画へ戻る。
10. OSの動きを減らす設定が有効な場合はクリック時の連続コマ送りを抑制し、マスコットの操作名を読み上げで確認できる。
11. 設定画面でマスコット表示をOFFにすると全画面から消え、ONに戻すと左下へ再表示される。選択値は再起動、フルバックアップ、復元後も維持される。
12. 表示中のマスコットを右クリックすると非表示操作を選べ、設定保存に失敗した場合はマスコットを残して再操作を案内する。
13. ムギを含む静止画と各モーションは約88×95ピクセルで元スプライトセルの縦横比を維持し、2体が横方向または縦方向へ引き伸ばされない。
14. 「Codex連携用の保存先」は設定画面にだけ表示され、分類一覧と提案箱の実際の固定パスを確認できる。入力欄、選択操作、変更ボタンがなく、Codex提案画面には同じ保存先情報が表示されない。

## 13. 開発段階案

### 第1段階：基盤とFAQ管理

- デスクトップアプリの起動
- SQLiteとデータ移行機構
- 分類ツリー
- FAQの登録、編集、複製、削除
- 画像、表、参考URLに対応するリッチテキストエディター
- 添付画像管理と安全な表示
- FAQ一覧・詳細

### 第2段階：階層検索

- 現在地以下、現在地のみ、すべての検索範囲
- 項目横断検索
- 検索順位と一致理由

### 第3段階：日本語検索

- 正規化
- 日本語の語分割またはn-gram
- 同義語辞書
- 表記揺れ
- 実データによる順位調整

### 第4段階：外部資料参照

- コピー用テキスト枠によるローカルパス・UNCパスの正確な保存とコピー
- パスの自動オープン・自動実行を行わない権限制御
- HTTP/HTTPS参考URLの確認付き既定ブラウザー表示
- 外部資料本体を検索・バックアップ・ソース管理へ取り込まない境界確認

### 第5段階：データ保護

- JSONエクスポート・インポート
- 完全バックアップ・復元
- 任意名と任意保存先に対応するフルバックアップ
- 完全バックアップを使用した別PCへの移行確認

### 第6段階：運用改善

- 検索履歴
- 結果0件の分析
- 閲覧数、お気に入り、解決状況などの候補機能

### 第7段階：Codexによる下書き支援

- 分類説明と分類カタログ
- KnowledgeApp専用Codexプラグインと提案形式
- 確認待ち提案一覧、本文プレビュー、分類の再選択
- 新規分類とFAQ下書きの同時登録、重複取込防止
- 提案箱・分類カタログのGit混入防止
- 既存FAQの明示委譲、推敲・修正案の版競合確認と承認反映
- 2～10件の非破壊統合案と元FAQ保持
- 統合FAQの新着公開、公開後確認による統合元の「統合済み」関係、通常検索除外、統合解除
- 承認・却下履歴、依頼系列ごとの最新却下案の再検討
- 結論、手順、注意、備考、参考リンクによる簡潔なFAQ標準構成
- 必要な場合だけ画像・スクリーンショットの追加位置を案内する判断基準と、著作権・機密情報・代替テキストの確認
- Codex提案のタイトル本体へ分類接頭辞を含めず、検索結果で現在のトップ分類名を表示設定により動的表示する制御

### 第8段階：CSV一括編集とローカル利用者管理

- DB第6版、初期管理者、ログインセッション、Argon2パスワードハッシュ
- 利用者追加・利用停止・再開・パスワード再設定
- FAQ作成者・更新者の記録と管理一覧表示
- Excel向けCSV書出し、全行プレビュー、本文ハッシュによる構造保持、旧版上書き確認
- 取込直前の完全バックアップと一括トランザクション
- DB第7版、FAQ・分類の接頭辞付き管理ID、CSV形式第2版への切り替え

### 今後の拡張段階：本格運用

- 一般利用者権限の細分化と承認フロー
- 管理者限定の検索・閲覧分析
- 検索キーワード傾向、人気FAQ、FAQ未登録ワードの可視化
- 自動バックアップのスケジュールと保持世代管理
- 複数人利用に適した共有データベースへの移行検討
- FAQまたは分類の管理ID採番値が9万件へ達した場合の、桁数・表示幅・CSV一括処理・運用ルールの再検討。`99999`超でも連番は6桁以上へ自動拡張するため、再設計まで採番を停止しない
- 会社デモ後のC#（ASP.NETまたは.NET Framework 4.8）移行判断

## 14. Q1～Q10の確定結果

2026-08-08の回答により、次の内容を確定した。

| No. | 確定内容 |
|---:|---|
| Q1 | 個人PCで開発し、GitHub等を通じて会社PCへ配布する。自宅で作るFAQは生成AI活用事例が中心になる場合があり、会社ではPCトラブルなどの業務FAQを利用するが、アプリは題材を限定しない。 |
| Q2 | Windows実行ファイルを利用できる。Tauri 2による利用者単位インストーラーを基本とし、必要に応じてポータブル版を補助利用する。 |
| Q3 | 対象OSはWindows 11 64ビットとする。 |
| Q4 | 外部資料の本文はKnowledgeAppへ取り込まず、検索対象に含めない。 |
| Q5 | KnowledgeApp内では外部HTMLを実行しない。HTTP/HTTPS参考URLを既定ブラウザーで開いた後は、会社のブラウザー・ネットワーク・アクセス制御に従う。 |
| Q6 | FAQは1分類に所属し、複数の観点はタグで表現する。 |
| Q7 | 簡易回答は画像、表、クリック可能な参考URLを含むリッチテキストに対応する。 |
| Q8 | 既存データは取り込まず、新規登録から開始する。 |
| Q9 | MVPでは任意名・任意保存先のフルバックアップを手動実行する。自動スケジュールは将来検討とする。 |
| Q10 | 検索・閲覧履歴は期間制限なく保持する。将来の本格運用では、分析情報を管理者だけが閲覧できるようにする。 |

### 14.1 2026-08-16 外部資料参照への変更

会社では既存ディレクトリ内に多数のHTML手順書その他の業務資料があり、KnowledgeAppへ複製して専用管理する必要性が低いことを確認した。このため、従来の`FR-MAN-01`～`FR-MAN-10`による取り込み、専用ビューアー、存在確認、リンク切れ検査、資料本体のバックアップ要件を廃止し、`FR-REF-01`～`FR-REF-07`の外部資料参照へ置き換える。

ローカルパス・UNCパスはコピー用テキストとして利用者が明示操作でコピーし、KnowledgeAppは自動で開かない。Web上の資料はHTTP/HTTPS参考URLに限定し、接続先を確認して既定ブラウザーで開く。外部資料本体の配置、更新、権限、安全性、可用性、バックアップは会社側の管理対象とする。

### 14.2 本格利用候補版0.3.3のリリース範囲

本格利用候補版`0.3.3`は、FAQ・分類・タグ・関連FAQ、ローカル利用者認証、監査情報、同義語を含む日本語検索、検索範囲・順位・一致理由・50件ページング、検索・閲覧履歴、CSV・JSON入出力、フルバックアップ・復元、Codexへの明示的委譲、外部資料参照をMVPの対象とする。利用者データはWindows利用者別のローカルデータフォルダへ保存し、単一起動、DB移行前安全バックアップ、移行失敗時の起動中止、Git・配布物への混入検査をリリース条件とする。

通常配布物はWindows 11 64ビット向けの利用者単位NSISインストーラーとする。新規インストール、旧版からの上書き更新、アンインストール後のデータ保持、表示倍率、実Excel、会社ネットワークのバックアップ先、コード署名その他の会社規定は、実際の配布候補と専用のWindows利用者環境で最終確認する。この実機確認と会社判断が未完了の間は「本格利用候補」であり、会社向け正式リリース可とは判定しない。

専用HTML手順書管理、外部資料本文の検索、外部資料の存在確認・リンク切れ検査・バックアップは対象外とする。外部資料は会社管理のディレクトリまたはWebサーバーに置き、KnowledgeAppにはコピー用参照先またはHTTP/HTTPS参考URLだけを保存する。

## 15. 残る調整事項

主要な機能要件と技術構成は確定した。次の事項は実装開始を妨げないため、プロトタイプの設計・試用を通じて決める。

1. アプリの正式名称と正式アイコン（メイン画面は白基調とし、緑・青のカラーテーマを選択可能）
2. GitHubリポジトリを公開または非公開のどちらで運用するか（会社規定確認までは非公開を推奨）
3. FAQ内画像1件あたり10MBという初期上限の調整
4. フルバックアップファイルの拡張子と命名規則
5. 実データによる検索順位、除外語、同義語の調整
6. 会社での配布時にコード署名が必要か
