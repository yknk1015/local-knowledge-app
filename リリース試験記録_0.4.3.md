# KnowledgeApp 0.4.3 リリース試験記録

## 1. 候補情報

| 項目 | 結果 |
|---|---|
| 試験日 | 2026-08-16 |
| 候補版 | 0.4.3 |
| 対象コミット | 未固定。画像保存診断・FAQ編集・タグマスター実装作業ツリー |
| OS | Windows 11 64ビット |
| 生成物 | `KnowledgeApp_0.4.3_x64-setup.exe` |
| サイズ | 8,437,002 bytes |
| SHA-256 | `6E3AA7508D511C1A6477C421982185FF3D447E731278D67CA5D16FA880271ABD` |
| Authenticode | `NotSigned` |
| 現時点の判定 | ローカル自動試験とSurface手動試験は良好との利用者報告あり。記入済み結果票、GitHub Actions、会社規定の確認待ち |

正式候補コミットから再生成した場合は、Surfaceへ渡す前に本記録、テスト計画、手順、結果票、検証スクリプト、マニフェスト、ZIP外側ハッシュを同じ成果物の値へ更新する。

## 2. 0.4.3機能回帰

| 対象 | 結果 |
|---|---|
| FAQ編集4区分 | 合格。基本情報、回答、タグ、公開・表示設定を表示し、検索情報・関連FAQ入力を表示しない画面試験を実施 |
| タグマスター | 合格。追加、正規化重複拒否、名称変更、検索索引更新、使用中削除拒否、未使用削除をDB・画面試験で確認 |
| タグ選択 | 合格。マスターのプルダウンから複数選択・解除できる画面試験を実施 |
| 旧データ互換 | 合格。旧検索情報と関連FAQを持つFAQの編集保存要求で旧値・関係IDを保持する画面試験を実施 |
| `ATT-004` | 合格。保存前に画像ノードを許可4属性へ正規化し、Rust側の厳格検証を維持した。0.4.2診断版でファイル選択・貼り付け・保存後表示に成功し、利用者が元の失敗条件による再保存でもエラーなしを確認した |
| 画像診断情報 | 合格。12分岐の固定コード、固定説明、安全化した属性名、文字数だけを返し、FAQ本文、属性値、画像、パス、添付IDを出力しない単体試験を実施 |
| `AUTH-001` | 合格。パスワード再設定後のDB再オープンでも新値だけを有効にする試験を確認 |

## 3. 自動検査

| 検査 | 結果 |
|---|---|
| `npm test` | 合格。20ファイル、91テスト |
| `npm run build` | 合格 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | 合格 |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | 合格 |
| `cargo test --manifest-path src-tauri/Cargo.toml --locked` | 合格。82件合格、性能試験1件は通常実行では意図的に除外 |
| `scripts/test-release-performance.ps1` | 合格。性能試験1件をreleaseモードで実行 |
| `scripts/check-text-encoding.ps1` | 合格 |
| `scripts/check-no-runtime-data.ps1` | 合格。`dist`、`target/release`、`target/release/bundle`も個別に検査 |
| `scripts/test-codex-plugin.ps1` | 合格 |
| `scripts/check-release-bundle-config.ps1` | 合格 |
| `npm run tauri build` | 合格。利用者単位NSISを生成 |

## 4. 性能結果

OS一時フォルダにFAQ 10,000件、分類1,000件、検索履歴1,000,000件、0件検索100,000件を生成した。会社FAQと利用者データは使用していない。

| 対象 | 結果 |
|---|---|
| 合成データ生成 | 1,164 ms |
| 分類1,000件取得 | 8 ms |
| 日本語検索、10,000件一致、50件取得 | 56 ms |
| FAQ詳細 | 1 ms未満 |
| 履歴100万件のページ・条件付き件数 | 671 ms |
| CSV 10,000件 | 53 ms、3,400,241 bytes |
| DBバックアップ | 4,485 ms、199,389,184 bytes |

## 5. 配布物検査

- Tauriのbundle targetがNSIS、install modeが`currentUser`である。
- `bundle.resources`は空、`externalBin`は未設定である。
- DB、WAL、SHM、FAQ、履歴、画像、CSV、JSON、バックアップ、Codex提案・委譲、会社管理の外部資料を同梱しない。
- 実行ファイルとインストーラーは未署名である。署名要否は会社規定で判定する。
- Surface検証ZIP `KnowledgeApp_Surface_Rehearsal_0.4.3.zip`を生成した。15ファイル、20,179,635 bytes、SHA-256は`FEFBDD5945D05BC6F7CC18F9CD19D313A0A48AAF515A39C1B1AFCBE00B945AF1`である。
- ZIPを別の一時フォルダーへ再展開し、マニフェストの全記載ファイル、未記載ファイルなし、想定外サブフォルダーなし、3インストーラーの固定SHA-256、3件の`NotSigned`を再検証してPASSした。

## 6. 残る手動確認

利用者から0.4.3のSurface手動試験は良好との報告を受けた。正式記録として確定する場合は、記入済み`Surface実機試験結果_0.4.3.md`で対象項目、担当、日時を補完する。0.4.4では認証処理だけを重点再確認する。
