# KnowledgeApp 0.4.3 リリースノート

## 位置づけ

`0.4.3`は、0.4.0・0.4.1のSurface試験で検出した画像付きFAQの保存エラーを修正し、0.4.2診断版で元の操作条件を含む手動保存に成功した処理を取り込む本格利用候補版である。0.4.0のFAQ編集簡素化・タグマスターと、0.3.4までの画像保存・パスワード再設定修正を含む。ローカル自動検査と画像の個別実機確認は合格したが、0.4.3の全P0、GitHub Actions、会社規定の確認が終わるまでは会社向け正式リリース可と判定しない。

## 変更内容

- SurfaceのWebView2／Tiptap実行条件で画像ノードへ`width`、`height`、`style`等の表示用属性が残っても、保存前に設計上許可した`src`、`alt`、`title`、`attachmentId`だけへ正規化する。
- Rust側の画像参照検証は緩和せず、不正な添付ID、保存用マーカー、代替テキストは従来どおり拒否する。
- 許可外の画像属性を含むSurface相当データの回帰テストを追加した。
- `ATT-004`を属性形式、許可外属性、添付ID、保存用参照、代替テキスト・タイトルの12分岐へ細分化した。表示するのは固定コード、固定説明、安全化した属性名、文字数だけで、FAQ本文、属性値、画像、ファイルパス、添付IDは含めない。
- 0.4.2診断版でファイル選択・貼り付け・元の失敗条件を手動確認し、保存・保存後表示とも成功した。0.4.2は診断専用であり正式配布しない。
- 共通メニューへ「タグマスター」を追加した。タグの追加、名称変更、FAQ使用件数の確認、未使用タグの削除を行える。
- FAQ作成・編集画面を「基本情報」「回答」「タグ」「公開・表示設定」の4区分へ変更した。
- FAQのタグは自由入力せず、タグマスターのプルダウンから複数選択し、選択済みタグを個別に外す方式へ変更した。
- タグ名変更時はタグIDを維持し、使用中の全FAQと検索索引へ同一トランザクションで反映する。
- FAQで使用中のタグは削除せず、使用件数と先にFAQから外す必要があることを表示する。
- 個別の症状、原因、対象、エラーコード、対応手順、注意事項、検索用語を入力する「検索情報」をFAQ編集画面から削除した。
- FAQ同士を直接選ぶ関連FAQ編集を現行画面から削除し、必要性を確認して別リリースで再設計する方針へ変更した。

## データ形式と互換性

- アプリ版：`0.4.3`
- DBスキーマ版：`7`（変更なし）
- CSV形式版：`2`（変更なし）
- JSON形式版：`1`（変更なし）
- リッチテキスト形式版：`2`（変更なし）

タグマスターには初期DBから存在する`tags`と`article_tags`を使用するため、DBマイグレーションは追加していない。旧版で保存した検索情報と関連FAQはDB・JSON・完全バックアップ内に保持し、既存FAQを他項目だけ編集しても消去しない。保存済みの関連FAQは読取互換として詳細画面に表示できるが、現行画面から新規追加・変更は行わない。

## 会社配布前の確認

- 0.3.4から0.4.3へ上書き更新し、既存タグ、旧検索情報、既存関連FAQ、画像、パスワードを保持できることをSurfaceで確認する。
- タグマスターの追加・名称変更・使用中削除拒否と、FAQでの複数タグ選択・解除を確認する。
- Surface再試験用に固定した0.4.3 NSISインストーラーは8,437,002 bytes、SHA-256は`6E3AA7508D511C1A6477C421982185FF3D447E731278D67CA5D16FA880271ABD`である。本書、検証スクリプト、ZIP内マニフェストで同じ値を使用する。署名状態は`NotSigned`であり、会社規定に反して警告や実行制限を回避しない。
- GitHub Actionsの全工程、Surface実機試験、コード署名要否、会社の配布経路・実行制限を確認する。
- 会社FAQ、パスワード、個人情報、会社の機密パスを試験記録や配布物へ含めない。
