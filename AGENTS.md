# AGENTS.md

## 適用範囲

このファイルは、リポジトリ全体の開発・テスト・文書更新に適用する。

## 正式な参照文書

正式な正本は次の3文書。作業開始時に各文書の冒頭の現行版・優先指定を確認し、その後は変更対象に関係する章を読む。継続作業では前回確認からの変更点を確認し、毎回全文を読み直さない。

1. `FAQシステム要件定義書.md`：要求、利用者向け動作、受入条件。
2. `FAQシステム基本設計書.md`：UI、検索、保存、認証、バックアップ、配布など、変更する機能の設計。
3. `ローカルFAQデータ・Git除外詳細設計書.md`：データ・パス・Git除外・ログ・配布・Codex委譲に触れる場合の境界と検査。

| 作業 | 追加で読む範囲 |
|---|---|
| 文書・指示の整理 | 変更する説明に対応する正式仕様と `文書整理ガイド.md`。アプリ仕様への影響を確認。 |
| UI・検索・編集 | 要件と基本設計の該当機能。データや外部資料に触れる場合はデータ詳細の該当章も読む。 |
| 認証・DB・保存先・バックアップ・配布・委譲 | 正式3文書の該当章を相互確認。安全境界を既存実装だけから推測しない。 |
| FAQ案の制作 | 下記のFAQスキルとその本文書式・提案形式。実DBや未委譲データを資料探しに使わない。 |

判断の優先順位は、利用者の最新指示、要件定義書、基本設計書、詳細設計書、既存実装の順とする。文書と実装が食い違う場合、既存実装だけを根拠に仕様を決めない。

現在の進行管理は`今後の課題.md`、文書の役割と旧版履歴の保管先は`文書整理ガイド.md`を参照する。旧計画・旧試験書は履歴であり、現在の起動手順や未完了判定に転用しない。古い段階の「試作」「本番切替禁止」より、正式3文書冒頭のC#正式版指定と現在の運用手順を優先する。

## 文書と実装の同期

- 開発作業では、コードだけでなく要件定義書・基本設計書への影響を必ず確認する。
- 機能、画面、データ項目、DB、検索、ファイル構成、バックアップ、セキュリティ、配布方法、運用方法を変更する場合、該当する設計書を同じ作業内で更新する。
- 利用者の要求範囲が変わる場合は要件定義書を更新し、その実現方法が変わる場合は基本設計書も更新する。
- 実装上の都合で設計から外れる必要がある場合、黙って変更せず、理由・影響・採用案を文書へ反映してから、または実装と同時に変更する。
- 不具合修正や内部整理で外部仕様が変わらない場合も、作業完了前に設計書への影響がないことを確認する。
- 作業報告では、変更した設計書、または「設計変更なし」と判断した理由を明示する。

## FAQ内容に関する前提

- アプリのテーマは「日常から仕事まで、セキュアにFAQナレッジを活用できる」とする。利用場所で対象者やFAQの題材を分けず、同じローカル保存・認証・権限・明示委譲の安全境界を適用する。
- FAQの題材は利用場所で分けず、日常の疑問、学習、生成AIの活用事例などを扱える。
- PCトラブル、PC設定、ネットワーク、プログラム修正などの業務知識も、同じ仕組みで扱える。
- これは登録されるFAQの題材に関する説明であり、アプリを生成AI専用にする要件ではない。
- FAQ内容が生成AI中心でも、生成AI専用の分類、画面、検索、API連携などを推測して追加しない。
- アプリへ従量課金の生成AI APIを組み込まない。これはAPI利用量による費用増大を避けるためであり、生成AI連携そのものを避ける方針ではない。利用者が定額利用するCodexへの明示的な委譲は積極的に活用する。

## CodexによるFAQ下書き

- 利用者が「この内容のFAQを作ってください」「KnowledgeAppへFAQ案を追加してください」「委譲番号のFAQを推敲・修正してください」「選択したFAQを統合してください」などと明示した場合は、`codex-plugins/knowledgeapp-faq/skills/knowledgeapp-faq/SKILL.md`を全文読み、その手順に従う。
- 利用者が「メール委譲番号からFAQ案を作ってください」と明示した場合も同じスキルを使う。メール委譲番号に一致する専用JSONだけを読み、指定フォルダ、Outlookプロファイル、`.pst`、`.msg`、添付、未選択メールをCodexから直接探索・読取しない。
- 新規FAQでは分類カタログだけを読む。既存FAQでは、利用者がKnowledgeApp画面で明示的に選び、発行した委譲番号のファイルだけを読む。SQLite、WAL、SHM、未委譲FAQ、添付画像本体、外部資料本体、検索・閲覧履歴を直接読取・変更しない。委譲されたFAQ本文に外部資料の参照先文字列が含まれていても、Codexからその参照先を自動で開かない。
- Codexは確認待ち提案だけを作成する。修正案は版競合確認と利用者承認後に既存FAQへ反映し、統合案は元FAQを変更・削除せず新規下書きにする。公開・削除を自動実行しない。
- 却下提案は履歴へ残し、同じ依頼系列では最新の却下案だけを再検討へ戻せる。古い案を再送・再承認する抜け道を作らない。
- 機密情報をCodexへ提示できるかは利用環境の規定を優先し、利用者が会話で明示した内容以外を自動収集しない。

FAQの本文書式・画像・リンクの規則は、同スキルの [FAQ本文の標準構成](codex-plugins/knowledgeapp-faq/skills/knowledgeapp-faq/references/faq-writing.md) を正本とする。FAQを新規作成・修正・統合するときに読む。開発作業でFAQ本文を作らない場合は読み込み不要。

## ローカルFAQデータとソースコードの分離

- 実際のFAQ・データベース・パスワードなどの利用者データや認証情報は、GitHubを含むGitリポジトリへ絶対にコミットしない。公開・非公開を問わず、APIキー、トークン、秘密鍵、緊急認証情報も対象とし、文書・ソース・画像へ転記する形でも含めない。
- DB、検索・閲覧履歴、添付画像、バックアップ、JSONエクスポート、診断ログ、一時ファイル、アプリ外で管理する外部資料、旧版互換用`manuals`データをソースコードに含めない。
- GitHub等で管理するのは、プログラム、設計文書、設定例、開発用に明示して作成したテストフィクスチャーとする。
- FAQの保存先はリポジトリ外の利用者データフォルダとし、開発環境でもリポジトリ配下へ保存しない。
- 保存先、Git除外、コミット前・CI検査は`ローカルFAQデータ・Git除外詳細設計書.md`に従う。

## 採用する基本構成

- Windows 11 64ビット
- .NET 10 / C# / WPF / WebView2（0.5.0以降の正式主系）
- React＋TypeScript＋Vite
- Tiptap
- SQLite＋FTS5
- インストール不要の単一exe配布（通常配布）。NSIS形式の利用者単位インストーラーは補助方式として維持

別方式へ変更する場合は、実装前または実装と同時に要件定義書と基本設計書を更新する。

今後の機能改修はC#へ実装する。Tauri 2 / Rustは旧版の互換回帰用として残す。C#本番は`LocalApplicationData/jp.local.webknowledgesystem.csharp`、検証起動は`--rehearsal`と固定の`.csharp-rehearsal`を使用する。旧Tauriデータを自動探索・変更・同期せず、引継ぎは利用者が選択するフルバックアップ復元に限る。詳細は要件1.17、基本設計4.6.13、データ詳細2.0.8を優先する。

## 実装上の原則

- 旧Tauriソースは外部保管とGit履歴で維持する。通常のC#ビルドは`src-tauri`へ依存させない。互換試験は`KNOWLEDGEAPP_LEGACY_SOURCE`で開発者がソースを明示した場合だけ行う。この変数をアプリやプラグインの利用者データルートに転用しない。

- 画面から任意SQL、任意シェル、無制限のファイル操作を実行できるAPIを公開しない。
- DB、検索、ファイル、バックアップ、パス検証はC#側へ集約する。Rustは旧版互換の確認に限る。
- SQLite DBは実行ホストのローカルに置く。共有版はWindows 11のHTTPSサービス経由とし、NASはバックアップ先にする。JSON/CSVの明示入出力先も用途別パス検証を経てネットワーク先を許可する。DBのネットワーク直接共有は許可しない。
- すべてのSQLをパラメーター化し、外部キーとトランザクションを使用する。
- 分類は最大5階層とし、循環・6階層化・同一親内の同名を画面側と保存側で防ぐ。
- FAQは1分類に所属し、複数観点はタグで表す。
- アプリ外で管理する外部資料本体はKnowledgeAppへ取り込まず、検索・存在確認・リンク切れ検査・バックアップの対象に含めない。
- ローカルパス、割り当て済みネットワークドライブ、UNCパスはコピー用テキストとして扱い、自動オープン、自動実行、パス補完を行わない。
- Web上の外部資料はHTTP/HTTPS参考URLだけを許可し、表示文字、接続先ホスト名、完全なURLを確認後に既定ブラウザーで開く。ブラウザーへ委譲後のJavaScript、外部リソース、認証、ダウンロードは利用環境のブラウザー・ネットワーク管理に従う。
- リッチテキストは許可ノード方式とし、任意HTMLを保存・実行しない。
- Codexが開発中にHTMLの扱いを判断する場合、利用者がHTMLとしての作成・編集・利用を明示していなければ、安全な通常テキストまたは許可済みの構造として扱う。利用者がHTMLファイル、HTMLコード、CSSの作成・編集・利用を明示した場合はその限りではなく、依頼された範囲で実際のHTML・CSSソースとして扱ってよい。
- 利用者によるHTML利用の指定は、HTMLソースを作成・編集する許可として扱うが、KnowledgeAppへHTML本体を取り込む許可や、ローカルパスを自動オープンする許可とはみなさない。外部HTMLはアプリ外で管理する資料としてコピー用パスまたはHTTP/HTTPS参考URLで案内する。アプリの安全境界を変更する依頼の場合は、リスク・影響・代替案を説明し、要件定義書・基本設計書・詳細設計書と実装を同じ作業内で更新する。
- URL文字列はクリック機能を持たない通常テキストとして保存できるようにする。クリック可能な外部URLはHTTP/HTTPSだけを許可し、確認後に既定ブラウザーで開く。
- FAQ保存に失敗した場合は、利用者が見落とさない位置へ原因と対処を日本語で表示する。
- 削除は要件で定めた論理削除・確認手順を守り、利用者データを不用意に消さない。

## テストと品質確認

- 変更箇所に応じた単体・統合・画面テストを追加または更新する。
- 5階層制限、階層検索、同義語、日本語検索、FAQ状態、リッチテキスト安全性、外部資料参照のコピー専用境界、HTTP/HTTPS確認、バックアップ復元を重点的に確認する。
- 保存形式やDBを変更する場合はマイグレーションと旧データ復元を確認する。
- バックアップ処理は失敗時に元データと既存バックアップを損なわないことを確認する。
- 実装完了前に、変更対象の型検査、テスト、ビルド、設計書との整合確認を行う。検証コマンドの正本は `package.json` と `.github/workflows/quality.yml`。

| 変更対象 | 検証 |
|---|---|
| 文書・指示だけ | 参照先・仕様整合・`git diff --check`、`./scripts/check-text-encoding.ps1`、`./scripts/check-no-runtime-data.ps1`。製品の再ビルドは不要。 |
| React/TypeScript | 対象のテスト（`npm test -- <対象テスト>`）、`npm run build`。UI動作を変えた場合は対象画面も確認。 |
| C#・DB・認証・保存境界 | `dotnet build src-csharp/KnowledgeApp.CSharp/KnowledgeApp.CSharp.csproj --configuration Release` と、品質ワークフロー内の変更対象に対応するCheckプロジェクト。合成データを使い実FAQを検査材料にしない。 |
| FAQプラグインのコマンド・提案形式 | `./scripts/test-codex-plugin.ps1` と変更に関係するC#側の委譲・提案検査。書式説明だけの整理は文書検証で確認。 |
| 配布・共通基盤・複数層にまたがる変更 | 影響する検証を組み合わせ、配布時は品質ワークフローと正式配布手順の必須検査を完了する。 |

関連する検証の合格後は、新しい変更・失敗・未解決の懸念がなければ同じ検証を繰り返さない。未実行の項目と理由は報告する。

## 文字コード

- ソースコード、設定、SQL、設計文書、PowerShellスクリプトはUTF-8（BOMなし）、改行コードはLFで保存する。
- 日本語を含むファイルの読み書きではOS既定文字コードへ依存せず、Rust、PowerShellその他の処理でUTF-8を明示する。
- `Out-File`、`Set-Content`、リダイレクトなど、実行環境によって既定文字コードが変わる書き込み方法を文字列ファイルへ無指定で使用しない。
- コミット前とCIで`scripts/check-text-encoding.ps1`を実行し、UTF-8違反、BOM、置換文字、NUL文字、LF以外の改行を拒否する。

## Git・配布

- DB、WAL、SHM、添付画像、検索・閲覧履歴、バックアップ、診断ログ、一時ファイル、アプリ外で管理する外部資料、旧版互換用`manuals`データをコミットしない。
- `.gitignore`とリリース前検査を維持する。
- FAQ CRUDを変更する際も、C#専用LocalApplicationDataへの保存とGit除外検査を維持する。
- 利用者の既存変更を尊重し、関係のないファイルを巻き戻さない。
- アプリ更新で利用者データを上書き・削除しない。
- 正式配布時の公開範囲、コード署名、保存場所は利用環境の規定を優先する。

## 作業完了チェック

- 要求した機能が実装・検証されている。
- 要件定義書と基本設計書を確認し、必要な更新を同じ作業内で行っている。
- 利用者がローカルで作成したFAQデータがソース管理やリリースへ混入していない。
- 変更内容、確認結果、文書更新の有無を利用者へ報告している。
