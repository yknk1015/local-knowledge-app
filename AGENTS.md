# AGENTS.md

## 適用範囲

このファイルは、リポジトリ全体の開発・テスト・文書更新に適用する。

## 正式な参照文書

開発前に必ず次を読むこと。

1. `FAQシステム要件定義書.md`
2. `FAQシステム基本設計書.md`
3. `ローカルFAQデータ・Git除外詳細設計書.md`

判断の優先順位は、利用者の最新指示、要件定義書、基本設計書、詳細設計書、既存実装の順とする。文書と実装が食い違う場合、既存実装だけを根拠に仕様を決めない。

## 文書と実装の同期

- 開発作業では、コードだけでなく要件定義書・基本設計書への影響を必ず確認する。
- 機能、画面、データ項目、DB、検索、ファイル構成、バックアップ、セキュリティ、配布方法、運用方法を変更する場合、該当する設計書を同じ作業内で更新する。
- 利用者の要求範囲が変わる場合は要件定義書を更新し、その実現方法が変わる場合は基本設計書も更新する。
- 実装上の都合で設計から外れる必要がある場合、黙って変更せず、理由・影響・採用案を文書へ反映してから、または実装と同時に変更する。
- 不具合修正や内部整理で外部仕様が変わらない場合も、作業完了前に設計書への影響がないことを確認する。
- 作業報告では、変更した設計書、または「設計変更なし」と判断した理由を明示する。

## FAQ内容に関する前提

- 利用者が自宅で作成するFAQは、生成AIの活用事例を中心とする場合がある。
- 会社で作成するFAQは、PCトラブル、PC設定、ネットワーク、プログラム修正などの会社業務を中心とする場合がある。
- これは登録されるFAQの題材に関する説明であり、アプリを生成AI専用にする要件ではない。
- FAQ内容が生成AI中心でも、生成AI専用の分類、画面、検索、API連携などを推測して追加しない。
- アプリへ従量課金の生成AI APIを組み込まない。これはAPI利用量による費用増大を避けるためであり、生成AI連携そのものを避ける方針ではない。利用者が定額利用するCodexへの明示的な委譲は積極的に活用する。

## CodexによるFAQ下書き

- 利用者が「この内容のFAQを作ってください」「KnowledgeAppへFAQ案を追加してください」「委譲番号のFAQを推敲・修正してください」「選択したFAQを統合してください」などと明示した場合は、`codex-plugins/knowledgeapp-faq/skills/knowledgeapp-faq/SKILL.md`を全文読み、その手順に従う。
- 新規FAQでは分類カタログだけを読む。既存FAQでは、利用者がKnowledgeApp画面で明示的に選び、発行した委譲番号のファイルだけを読む。SQLite、WAL、SHM、未委譲FAQ、添付画像本体、手順書、検索・閲覧履歴を直接読取・変更しない。
- Codexは確認待ち提案だけを作成する。修正案は版競合確認と利用者承認後に既存FAQへ反映し、統合案は元FAQを変更・削除せず新規下書きにする。公開・削除を自動実行しない。
- 却下提案は履歴へ残し、同じ依頼系列では最新の却下案だけを再検討へ戻せる。古い案を再送・再承認する抜け道を作らない。
- 会社情報をCodexへ提示できるかは会社規定を優先し、利用者が会話で明示した内容以外を自動収集しない。

### Codexが作成するFAQの標準構成

- FAQのタイトル本体は検索しやすい質問文とし、`【分類名】`の接頭辞を含めない。検索結果では、表示設定がONの場合にアプリが現在のトップ分類名を`【トップ分類名】`として動的に付け、OFFの場合はタイトル本体だけを表示する。分類名の変更やFAQの移動へ追従させるため、Codex提案や保存済みタイトルへ接頭辞を固定保存しない。
- 利用者が最初に答えを把握できるよう、概要を結論として原則1文で記載する。前置きや背景説明から始めない。ただし、誤解防止や安全確保に不可欠な条件は省略しない。
- 本文は原則として`■手順`から始め、実行順の番号付きリストにする。1項目には1操作を書き、画面名・ボタン名などは実際の表記に合わせる。操作を伴わないFAQでは架空の手順を作らず、`■確認方法`や`■対応`など内容に合う見出しを使う。
- 注意事項は関係する手順の直前または直後へ置き、`※注意`と明記する。必要に応じて太字を併用するが、色だけで注意を伝えない。注意がなければ見出しだけを作らない。
- 補足情報は必要な場合だけ`■備考`へ短い箇条書きで記載し、結論や手順を繰り返さない。
- 補足となるリンクは本文の最後に`■参考リンク`として記載する。利用者が提示した、または依頼の範囲で確認できたリンクだけを使い、表示名と参照目的を添える。該当リンクがなければ空の見出しや推測したURLを作らない。
- 文章だけでは操作箇所や状態の違いが伝わりにくく、視認性・理解を実質的に高める場合だけ、関係する手順の直後へ画像またはスクリーンショットを用いる。装飾目的や、文章だけで十分に伝わる場合は追加しない。新規・統合提案は画像を直接含められないため、必要な画像の内容、挿入位置、代替テキスト案を送信後に利用者へ案内し、下書き取込後に追加してもらう。修正案では委譲元画像を一字も変えず維持し、追加・削除・並べ替えをしない。
- 利用者自身が作成・提供した画像、または利用条件を確認できた画像だけを使用する。著作権侵害のおそれや利用条件の不明確さがある場合は無理に使用せず、文章で補う。個人情報、機密情報、認証情報は写り込まないよう除去またはマスクし、追加する画像には内容が分かる代替テキストを付ける。
- 1つのFAQは1つの質問・目的に絞り、手順は必要最小限かつ原則7項目以内にする。長くなる場合はFAQの分割または外部手順書への切り分けを提案する。
- 簡潔さより事実の正確性、安全性、利用者が作業を完了できることを優先する。新規・修正・統合のいずれでも、確認できない前提、操作、リンクを補完しない。

## ローカルFAQデータとソースコードの分離

- 利用者がアプリ上で作成したFAQ内容をGitHub等のソース管理へ登録しない。
- DB、検索・閲覧履歴、手順書、添付画像、バックアップ、JSONエクスポート、診断ログ、一時ファイルをソースコードに含めない。
- GitHub等で管理するのは、プログラム、設計文書、設定例、開発用に明示して作成したテストフィクスチャーとする。
- FAQの保存先はリポジトリ外の利用者データフォルダとし、開発環境でもリポジトリ配下へ保存しない。
- 保存先、Git除外、コミット前・CI検査は`ローカルFAQデータ・Git除外詳細設計書.md`に従う。

## 採用する基本構成

- Windows 11 64ビット
- Tauri 2
- React＋TypeScript＋Vite
- Tiptap
- Rust
- SQLite＋FTS5
- NSIS形式の利用者単位インストーラー

別方式へ変更する場合は、実装前または実装と同時に要件定義書と基本設計書を更新する。

## 実装上の原則

- 画面から任意SQL、任意シェル、無制限のファイル操作を実行できるAPIを公開しない。
- DB、検索、ファイル、バックアップ、パス検証はRust側へ集約する。
- SQLite DBはローカルに置き、ネットワークドライブはバックアップ先としてだけ扱う。
- すべてのSQLをパラメーター化し、外部キーとトランザクションを使用する。
- 分類は最大5階層とし、循環・6階層化・同一親内の同名を画面側と保存側で防ぐ。
- FAQは1分類に所属し、複数観点はタグで表す。
- 外部HTML手順書の本文は検索対象に含めない。
- HTML手順書ではJavaScript、iframe、外部リソースを実行・自動読込しない。
- リッチテキストは許可ノード方式とし、任意HTMLを保存・実行しない。
- Codexが開発中にHTMLの扱いを判断する場合、利用者がHTMLとしての作成・編集・利用を明示していなければ、安全な通常テキストまたは許可済みの構造として扱う。利用者がHTMLファイル、HTMLコード、CSSの作成・編集・利用を明示した場合はその限りではなく、依頼された範囲で実際のHTML・CSSソースとして扱ってよい。
- 利用者によるHTML利用の指定は、HTMLソースを作成・編集する許可として扱い、KnowledgeAppへ取り込んだHTMLでJavaScript、iframe、外部リソースなどを実行する許可とはみなさない。アプリの安全境界を変更する依頼の場合は、リスク・影響・代替案を説明し、要件定義書・基本設計書・詳細設計書と実装を同じ作業内で更新する。
- URL文字列はクリック機能を持たない通常テキストとして保存できるようにする。クリック可能な外部URLはHTTP/HTTPSだけを許可し、確認後に既定ブラウザーで開く。
- FAQ保存に失敗した場合は、利用者が見落とさない位置へ原因と対処を日本語で表示する。
- 削除は要件で定めた論理削除・確認手順を守り、利用者データを不用意に消さない。

## テストと品質確認

- 変更箇所に応じた単体・統合・画面テストを追加または更新する。
- 5階層制限、階層検索、同義語、日本語検索、FAQ状態、リッチテキスト安全性、手順書安全性、バックアップ復元を重点的に確認する。
- 保存形式やDBを変更する場合はマイグレーションと旧データ復元を確認する。
- バックアップ処理は失敗時に元データと既存バックアップを損なわないことを確認する。
- 実装完了前に、型検査、テスト、ビルド、設計書との整合確認を行う。

## 文字コード

- ソースコード、設定、SQL、設計文書、PowerShellスクリプトはUTF-8（BOMなし）、改行コードはLFで保存する。
- 日本語を含むファイルの読み書きではOS既定文字コードへ依存せず、Rust、PowerShellその他の処理でUTF-8を明示する。
- `Out-File`、`Set-Content`、リダイレクトなど、実行環境によって既定文字コードが変わる書き込み方法を文字列ファイルへ無指定で使用しない。
- コミット前とCIで`scripts/check-text-encoding.ps1`を実行し、UTF-8違反、BOM、置換文字、NUL文字、LF以外の改行を拒否する。

## Git・配布

- DB、WAL、SHM、手順書、添付画像、検索・閲覧履歴、バックアップ、診断ログ、一時ファイルをコミットしない。
- `.gitignore`とリリース前検査を維持する。
- FAQ CRUDの実装前に、TauriのappLocalDataDirへの保存とGit除外検査を完成させる。
- 利用者の既存変更を尊重し、関係のないファイルを巻き戻さない。
- アプリ更新で利用者データを上書き・削除しない。
- 会社配布時の公開範囲、コード署名、保存場所は会社規定を優先する。

## 作業完了チェック

- 要求した機能が実装・検証されている。
- 要件定義書と基本設計書を確認し、必要な更新を同じ作業内で行っている。
- 利用者がローカルで作成したFAQデータがソース管理やリリースへ混入していない。
- 変更内容、確認結果、文書更新の有無を利用者へ報告している。
