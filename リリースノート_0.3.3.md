# KnowledgeApp 0.3.3 リリースノート

## 位置づけ

`0.3.3`は会社での本格利用に向けたリリース候補版である。自動テスト、リリースモード性能試験、Windows 11でのNSIS生成と配布物検査は完了している。専用Windows利用者環境での新規導入・旧版更新・アンインストール、表示倍率、実Excel、会社ネットワーク、コード署名・配布規定の確認が完了するまでは、会社向け正式リリース可とは判定しない。

## 主な機能

- FAQ・分類・タグ・関連FAQ、下書き・公開・廃止・非表示・論理削除・復元
- FAQ検索情報、日本語正規化、trigram検索、同義語、分類範囲、順位・一致理由、50件ページング、並び替え
- 検索・閲覧履歴の絞り込み、ページング、削除
- 安全なリッチテキスト、表、管理対象画像、HTTP/HTTPS参考URL、コピー用テキスト枠
- ローカル利用者認証、管理者・一般利用者、FAQ作成者・更新者の監査
- Excel編集用CSV形式第2版と、形式版付きJSON入出力
- 任意保存先のフルバックアップ、復元前・CSV/JSON取込前・DB移行前の安全バックアップ
- Codexによる確認待ちの新規・修正・統合提案。自動公開・自動削除・SQLite直接操作は行わない。
- 単一起動、起動失敗時の日本語ダイアログ、利用者データと配布物の分離検査

## データ形式と移行

- アプリ版：`0.3.3`
- DBスキーマ版：`7`
- CSV形式版：`2`
- JSON形式版：`1`
- リッチテキスト形式版：`2`

DB第1～6版は、書込みを始める前に整合性と版を確認し、利用者データフォルダ内の`safety-backups`へ移行前フルバックアップを作成してから第7版へ段階移行する。バックアップ作成または移行に失敗した場合は通常起動を中止し、旧DBを変更しない。

旧版互換用の`manuals`テーブルとデータ領域は自動削除しないが、現行画面では専用HTML手順書管理に使用しない。会社管理資料はコピー用参照先またはHTTP/HTTPS参考URLで案内する。

## 互換性と運用上の注意

- 対象OSはWindows 11 64ビットで、通常配布は利用者単位NSISインストーラーである。
- 更新で利用者データを上書き・削除しない。更新前にはフルバックアップを作成する。
- SQLite DBをネットワークドライブへ置かない。ネットワークドライブはバックアップ先としてだけ扱う。
- CSV形式第1版は取り込まない。`0.3.3`から最新CSVを書き出して編集する。
- JSONは添付画像、利用者、検索・閲覧履歴を含む完全移行形式ではない。端末全体の移行にはフルバックアップを使う。
- 外部資料本体はKnowledgeAppの検索・リンク切れ検査・バックアップ・復元の対象外である。
- FAQ、履歴、画像、CSV、JSON、バックアップ、診断ログ、Codex提案・委譲をGitや配布物へ含めない。

## 確認済み

- Reactテスト83件、Rust通常テスト79件、Rustリリース性能試験1件
- TypeScriptビルド、Rust整形、Clippy、文字コード、利用者データ混入、Codexプラグイン、NSIS設定・成果物検査
- FAQ 10,000件、分類1,000件、履歴100万件のOS一時データによる検索・詳細・履歴・CSV・DBバックアップ計測
- Windows 11上で`KnowledgeApp_0.3.3_x64-setup.exe`を生成し、SHA-256を記録

詳細結果は`リリース試験記録_0.3.3.md`と`リリース性能試験記録.md`を参照する。

## 正式リリース判定までの未確認事項

- FAQ実データから隔離した専用Windows利用者または別PCでの新規インストール、旧版上書き更新、アンインストール、再インストール、ショートカット起動
- 1280×720、100%・125%・150%、緑・青テーマ、キーボード、フォーカス、読み上げ名の実機確認
- 実際のExcelによるCSV往復、会社で許可されたローカル・UNC・HTTP/HTTPS参照先、ネットワークバックアップ・復元
- コード署名の要否、証明書、署名担当、GitHub公開範囲、配布経路、WebView2・実行制限・ウイルス対策などの会社判断

現在生成した候補インストーラーは未署名である。会社配布に署名が必要な場合、EXEとインストーラーの署名・検証を正式工程へ追加する。
