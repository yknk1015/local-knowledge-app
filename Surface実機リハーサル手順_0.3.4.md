# KnowledgeApp 0.3.4 Surface実機リハーサル手順

> 0.3.3は画像保存とパスワード再設定の高重要度不具合によりリリース不可とした。本手順では修正版0.3.4の全項目を最初から再実施し、0.3.3の部分結果を流用しない。

## 1. 目的と安全条件

この手順は、会社FAQや既存DBを一切使わず、Surface上の合成データだけで新規導入、旧版更新、検索、CSV、外部資料参照、バックアップ、表示倍率、アンインストールを確認する。

次の条件を満たさない場合は開始しない。

- Windows 11 64ビットのSurfaceである。
- KnowledgeAppの既存利用者データがない専用Windows利用者、または初期化可能な試験専用PCである。
- 未署名インストーラーの実行が試験環境の規則で許可されている。許可されない場合は警告を回避せず中止する。
- 会社FAQ、会社の認証情報、個人情報、秘密鍵をSurfaceへコピーしない。
- 画面記録にはこの手順で指定した合成データだけを表示する。

## 2. パッケージ確認

1. `KnowledgeApp_Surface_Rehearsal_0.3.4`フォルダをSurfaceのローカルフォルダへ展開する。ネットワーク上から直接インストーラーを実行しない。
2. PowerShellで展開先へ移動し、次を実行する。

   ```powershell
   ./verify-surface-release-package.ps1
   ```

3. 実行ポリシーでスクリプトが許可されない場合は回避せず、次の2ファイルを右クリックしてプロパティとハッシュを個別に確認する。

   ```powershell
   Get-FileHash ./KnowledgeApp_0.2.0_x64-setup.exe -Algorithm SHA256
   Get-FileHash ./KnowledgeApp_0.3.4_x64-setup.exe -Algorithm SHA256
   ```

4. 期待値は次のとおり。1文字でも異なる場合は実行しない。

| 用途 | ファイル | SHA-256 |
|---|---|---|
| 更新試験専用旧版 | `KnowledgeApp_0.2.0_x64-setup.exe` | `AB2451B297B79C89C12C3196CEBA0302AD925C02764D2B6A53FE66B3BE178455` |
| 本格利用候補版 | `KnowledgeApp_0.3.4_x64-setup.exe` | `9194B31560CF076F9E244836816E12BB54EDF75A90ECA165F73837C0400CDD03` |

両方とも未署名である。`0.2.0`はコミット`5f99114e`からクリーンビルドした更新試験専用品であり、正式配布しない。`0.3.4`の対象ソースは修正コミット確定後に記入する。

## 3. 旧版0.2.0の導入と検証データ作成

1. Windowsの表示倍率を100%にする。
2. `KnowledgeApp_0.2.0_x64-setup.exe`を実行し、利用者単位でインストールする。
3. スタートメニューまたはショートカットからKnowledgeAppを起動する。コマンド操作なしで検索画面が表示されることを確認する。
4. 「分類の管理」でトップ分類`Surface検証`を作り、その下へ`ネットワーク`を作る。
5. 「新しいFAQ」で次を登録する。

| 項目 | 合成データ |
|---|---|
| 分類 | `Surface検証 > ネットワーク` |
| タイトル | `Surfaceでネットワーク接続を確認するには？` |
| 概要 | `Windowsの設定画面で接続状態を確認します。` |
| 回答 | `■確認方法`、`1. Windowsの設定を開きます。`、`2. ネットワークとインターネットを確認します。` |
| 状態 | 公開 |
| 重要度 | 2 |

6. 回答へ`knowledgeapp-test-image.png`を追加し、代替テキストを`KnowledgeApp試験用アイコン`とする。
7. FAQを保存し、検索結果と詳細画面でタイトル、概要、回答、画像が表示されることを確認する。
8. 「設定・情報」で`Surface-0.2.0-before-update.faqbackup`をSurface内の試験用バックアップフォルダへ作成する。
9. KnowledgeAppを終了する。

## 4. 0.3.4への上書き更新

1. `KnowledgeApp_0.3.4_x64-setup.exe`を実行して上書き更新する。
2. ショートカットから起動する。DB移行前安全バックアップの作成または移行に失敗した場合は、繰り返し操作せずエラーを記録して中止する。
3. ログインID`0000`、パスワード空欄でログインする。
4. 旧版で作成した分類、FAQ、画像、公開状態が保持されていることを確認する。
5. FAQの管理IDと分類の管理IDが追加され、FAQの作成者・更新者が初期管理者で補完されていることを確認する。
6. 「利用者の管理」で初期管理者のパスワードを試験用の値へ変更する。結果票へパスワードを書かない。
7. 「設定・情報」で「空欄のパスワードを許可する」をOFFにする。
8. KnowledgeAppを終了して再起動し、変更後パスワードでログインできることを確認する。

## 5. 検索・関連FAQ・外部資料参照

1. 旧版FAQを編集し、検索情報へ症状`接続できない`、対象`Surface`、検索用語`ネットワーク確認`、タグ`ネットワーク`を追加する。
2. 2件目の公開FAQ`SurfaceでWi-Fiを再接続するには？`を作成し、旧版FAQの関連FAQへ設定する。
3. 「同義語の管理」で代表語`パソコン`、同義語`PC`を登録する。
4. 検索語`PC ネットワーク`で検索し、対象FAQ、一致理由、タグ、重要度が表示されることを確認する。
5. 検索範囲を「現在地以下」「現在地のみ」「すべて」で切り替え、50件単位のページ表示と更新日・重要度の並び替えを確認する。
6. FAQ回答へ`external-reference-test.html`の絶対パスを入力して選択し、「コピー用」でコピー専用枠へ変換して保存する。
7. 詳細画面でコピーし、メモ帳へ貼り付けて元のパスと一字ずつ一致することを確認する。KnowledgeAppが自動でファイルを開かないことも確認する。
8. コピーしたパスを利用者が明示的に既定ブラウザーへ貼り付け、外部HTMLが表示されることを確認する。
9. 回答の表示文字`外部サイト試験`へ`https://example.com/`を参考URLとして設定する。詳細画面で開くと、表示文字・接続先・完全URLの確認ダイアログが出ることを確認してから開く。
10. `https://example.invalid/`でも確認ダイアログ後にブラウザー側で失敗し、KnowledgeAppのFAQ閲覧を継続できることを確認する。
11. 「検索・閲覧履歴」で検索履歴、0件検索、閲覧履歴、期間・件数絞り込み、ページングを確認する。削除は対象と件数を確認してから試験する。

## 6. 6桁管理IDとJSON

1. 合成フィクスチャーをKnowledgeAppが受け付ける試験用ファイル名へコピーする。元の`.fixture.json`は変更しない。

   ```powershell
   Copy-Item ./six-digit-management-id.fixture.json ./six-digit-management-id.knowledge-export.json
   ```

2. 「FAQの管理」→「JSON入出力」→「JSONインポート」で、作成した`six-digit-management-id.knowledge-export.json`を選ぶ。
3. 分類1件、FAQ1件が新規、エラー0件であることを確認して取り込む。
4. 「FAQの管理」を詳細表示へ切り替え、`FAQ-100000`が省略・重なり・横切れなく読めることを確認する。
5. 「分類の管理」で`CAT-100000`が読めることを確認する。
6. 画面幅を900px以下にしてカード表示へ切り替わっても管理IDと操作が判別できることを確認する。

## 7. Excel CSVと10,000件処理

1. 「FAQの管理」→「CSVエクスポート」で`Surface-base.knowledge-faq.csv`を書き出す。
2. Excelで開き、UTF-8の日本語、17列、管理IDが崩れていないことを確認する。
3. 旧版から移行したFAQの概要だけを変更してCSV形式のまま保存する。
4. 「CSVインポート」でプレビューし、変更対象、警告、エラーを確認して反映する。取込前安全バックアップのパスを結果票へ記録する。
5. PowerShellで次を実行し、同じ分類へ追加する合成FAQ 10,000件のCSVを作る。

   ```powershell
   ./New-KnowledgeAppLargeCsvFixture.ps1 `
     -BaseCsv ./Surface-base.knowledge-faq.csv `
     -DestinationCsv ./Surface-10000.knowledge-faq.csv `
     -RowCount 10000
   ```

6. `Surface-10000.knowledge-faq.csv`を選択し、プレビュー中と一括取込中にウィンドウが白く固まらず、処理中表示が分かることを確認する。開始時刻、終了時刻、件数を記録する。
7. 取込後、検索・FAQ管理の50件ページング、更新日・重要度並び替え、詳細表示の応答を確認する。
8. 10,000件を含むフルバックアップをローカルへ作成し、処理中表示、時間、ファイルサイズを記録する。

合成CSVと取込後DBは試験専用である。Git、メール、正式配布物へ含めない。

## 8. 表示・キーボード・単一起動

次を100%、125%、150%の各表示倍率で確認する。倍率変更後はKnowledgeAppを開き直す。

- 1280×720以上でメインメニュー、検索、FAQカード、新規FAQ操作が見える。
- 緑・青テーマで主要操作、選択、フォーカス、成功・注意・エラーが色だけに依存せず判別できる。
- FAQ管理の通常表示、詳細表示、900px以下のカード表示に欠けや重なりがない。
- Tab／Shift+Tabでフォーカスが見え、Enterでボタン・リンク、Spaceでチェックボックスとマスコットを操作できる。
- エラー通知へ自動移動し、項目名、原因、対処が読める。
- Windowsのナレーターを使用できる場合、検索、分類、FAQ編集、設定の主要操作名が読み上げられる。機密内容は読み上げさせない。

単一起動は次の順で確認する。

1. FAQ詳細を開いた状態でショートカットをもう一度実行し、別ウィンドウが増えず既存ウィンドウが前面へ戻る。
2. 設定画面でも同じ結果になる。
3. 10,000件バックアップの処理中にもショートカットを実行し、別プロセスからDBやバックアップが競合しない。
4. タスクマネージャーでKnowledgeAppが同時に複数常駐していないことを確認する。

## 9. バックアップ・復元・アンインストール

1. 10,000件を含むフルバックアップをローカルへ作成する。
2. 会社規定で許可されたネットワーク保存先がある場合は、同じ内容をネットワーク先へ作成する。接続、書込権限、空き容量、切断時の失敗を確認する。許可先がなければ未実施として残す。
3. 試験用FAQを1件変更し、ローカルバックアップから復元する。復元前安全バックアップが作成され、変更前の内容へ戻ることを確認する。
4. KnowledgeAppをアンインストールし、利用者データが不用意に削除されないことを確認する。データフォルダ自体を手動削除しない。
5. 同じ`0.3.4`を再インストールし、変更後パスワードでログインでき、分類、FAQ、画像、10,000件、設定が保持されていることを確認する。
6. ショートカットからコマンド操作なしで起動できることを確認する。

## 10. 結果の返却

`Surface実機試験結果_0.3.4.md`の各欄を合格／不合格／未実施で記入する。パスワード、会社FAQ、個人情報、会社の機密パスは書かない。不合格時は合成データだけが見える画面、操作順、時刻、エラー番号を記録する。

結果票と、会社規定上共有してよい合成データだけの画面記録を開発PCへ戻す。バックアップ、CSV、JSON、DB、検索・閲覧履歴は戻さない。
