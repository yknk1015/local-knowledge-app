# KnowledgeApp 0.4.1 リリース試験記録

## 1. 候補情報

| 項目 | 結果 |
|---|---|
| 試験日 | 2026-08-16 |
| 候補版 | 0.4.1 |
| 対象コミット | 未固定。FAQ編集・タグマスター実装作業ツリー |
| OS | Windows 11 64ビット |
| 生成物 | `KnowledgeApp_0.4.1_x64-setup.exe` |
| サイズ | 8,431,294 bytes |
| SHA-256 | `F098756BDAFEB53982FAD725A6534F6F58FC0885AA1A03131F8A380EC04923A6` |
| Authenticode | `NotSigned` |
| 現時点の判定 | ローカル自動試験合格。Surface全項目、GitHub Actions、会社規定の確認待ち |

正式候補コミットから再生成した場合は、Surfaceへ渡す前に本記録、テスト計画、手順、結果票、検証スクリプト、マニフェスト、ZIP外側ハッシュを同じ成果物の値へ更新する。

## 2. 0.4.1機能回帰

| 対象 | 結果 |
|---|---|
| FAQ編集4区分 | 合格。基本情報、回答、タグ、公開・表示設定を表示し、検索情報・関連FAQ入力を表示しない画面試験を実施 |
| タグマスター | 合格。追加、正規化重複拒否、名称変更、検索索引更新、使用中削除拒否、未使用削除をDB・画面試験で確認 |
| タグ選択 | 合格。マスターのプルダウンから複数選択・解除できる画面試験を実施 |
| 旧データ互換 | 合格。旧検索情報と関連FAQを持つFAQの編集保存要求で旧値・関係IDを保持する画面試験を実施 |
| `ATT-004` | 合格。`width`、`height`、`style`を含むSurface相当の画像ノードを保存前に許可4属性へ正規化し、Rust側の厳格検証を維持する回帰試験を確認 |
| `AUTH-001` | 合格。パスワード再設定後のDB再オープンでも新値だけを有効にする試験を確認 |

## 3. 自動検査

| 検査 | 結果 |
|---|---|
| `npm test` | 合格。20ファイル、91テスト |
| `npm run build` | 合格 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | 合格 |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | 合格 |
| `cargo test --manifest-path src-tauri/Cargo.toml --locked` | 合格。81件合格、性能試験1件は通常実行では意図的に除外 |
| `scripts/test-release-performance.ps1` | 合格。性能試験1件をreleaseモードで実行 |
| `scripts/check-text-encoding.ps1` | 合格 |
| `scripts/check-no-runtime-data.ps1` | 合格 |
| `scripts/test-codex-plugin.ps1` | 合格 |
| `scripts/check-release-bundle-config.ps1` | 合格 |
| `npm run tauri build` | 合格。利用者単位NSISを生成 |

## 4. 性能結果

OS一時フォルダにFAQ 10,000件、分類1,000件、検索履歴1,000,000件、0件検索100,000件を生成した。会社FAQと利用者データは使用していない。

| 対象 | 結果 |
|---|---|
| 合成データ生成 | 1,222 ms |
| 分類1,000件取得 | 9 ms |
| 日本語検索、10,000件一致、50件取得 | 59 ms |
| FAQ詳細 | 1 ms未満 |
| 履歴100万件のページ・条件付き件数 | 590 ms |
| CSV 10,000件 | 49 ms、3,400,241 bytes |
| DBバックアップ | 4,687 ms、199,389,184 bytes |

## 5. 配布物検査

- Tauriのbundle targetがNSIS、install modeが`currentUser`である。
- `bundle.resources`は空、`externalBin`は未設定である。
- DB、WAL、SHM、FAQ、履歴、画像、CSV、JSON、バックアップ、Codex提案・委譲、会社管理の外部資料を同梱しない。
- 実行ファイルとインストーラーは未署名である。署名要否は会社規定で判定する。
- Surface検証ZIP `KnowledgeApp_Surface_Rehearsal_0.4.1.zip`を生成した。15ファイル、20,173,532 bytes、SHA-256は`445D9B9EE69D37DC15D9DDB5408B5A9E3AC12985F7EE24DA872CB81D84549B56`である。
- ZIPを別の一時フォルダへ再展開し、マニフェストの全記載ファイル、未記載ファイルなし、想定外サブフォルダなし、3インストーラーの固定SHA-256、3件の`NotSigned`を再検証してPASSした。

## 6. 残る手動確認

`Surface実機テスト計画書_0.4.1.md`と`Surface実機リハーサル手順_0.4.1.md`に従い、0.2.0→0.3.4→0.4.1の更新、旧互換データ保持、タグマスター、画像、パスワード、CSV、検索、バックアップ・復元、表示倍率、アンインストール・再インストールを実施する。P0不合格時は中止し、修正版でP0を先頭から再実施する。
