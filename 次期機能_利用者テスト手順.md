# KnowledgeApp 次期機能 利用者テスト手順

## 利用者テスト手順の管理形式

2026-08-30以降、利用者へ実施を依頼する段階テストは、判定を選択できるExcelチェックシートを正本として作成する。シートには操作、入力例・条件、期待結果、判定、エラーコード、実施日、実施者、備考を設け、判定は「未実施／合格／不合格／対象外」から選択する。概要シートでは合格・不合格・未実施・対象外と完了率を数式で集計する。

チェックシートと記入済み結果はリポジトリ外の`%USERPROFILE%\Documents\KnowledgeApp_Test\outputs\{フェーズ名}`へ保存する。本書は仕様・注意事項・回帰手順の履歴として維持し、今後の個別実施依頼は対応するチェックシートを案内する。

## 1. テスト前の注意

- 最初は会社FAQ、本番DB、実メールを使わず、合成FAQと自分で作成した合成`.msg`だけを使用する。
- 合成`.msg`はリポジトリ外の専用フォルダへ置く。`.pst`、添付、暗号化メールは初期テストに使わない。
- パスワード、秘密鍵、個人情報、会社の実在名、実アドレス、実ファイルパスを結果票や画面キャプチャへ残さない。
- C#版はまだ試作であり、既存の本番DBを開かない。現行Tauri版をアンインストールまたは置換しない。

## 2. FAQタブ（現行Tauri版）

2026-08-30に、11件目の拒否、10件の均等縮小、FAQ一覧・FAQ別のスクロール位置保持を含む一連の利用者テストが合格済みです。次の手順は将来の回帰確認用に保持します。

1. 合成FAQを11件以上用意する。
2. 検索結果から異なるFAQを10件開き、画面上部へ10タブが表示されることを確認する。
3. 10件の途中で同じFAQをもう一度開き、タブ数が増えず既存タブが選ばれることを確認する。
4. 10件を開いた状態で11件目を選び、次の文言が表示され、既存タブが閉じないことを確認する。

   `開いているFAQタブが10件あります。既存のタブを閉じてからもう一度開いてください。`

5. 表示中タブを閉じ、右隣、右隣がなければ左隣へ移ることを確認する。
6. 最後のタブを閉じ、FAQ検索へ戻ることを確認する。
7. 10件を開いた状態で横スクロールバーが表示されず、10件すべてのタブと閉じるボタンが画面幅内に表示されることを確認する。省略されたタイトルは、ポインターを置くと完全なタイトルを確認できることも確認する。
8. FAQ検索・一覧を任意の位置までスクロールしてFAQ①を開き、FAQ①を別の位置までスクロールする。左メニューの「FAQを探す」で一覧へ戻り、一覧が元の位置へ戻ることを確認する。
9. FAQ①とFAQ②をそれぞれ異なる位置までスクロールし、タブを数回切り替えても各FAQがそれぞれ最後の位置へ戻ることを確認する。可能であれば10件すべてで繰り返す。
10. 1280×720、Windows表示倍率100%・125%・150%、緑・青テーマで、タブのタイトル、選択状態、閉じるボタン、11件目メッセージを判読できることを確認する。
11. キーボードのTabキーでタブ選択と閉じるボタンへ移動でき、フォーカス枠が見えることを確認する。

## 3. C#互換ホスト

2026-08-30に、チェックシートの14項目すべてが利用者テストで合格しました。概要の記録は「合格14、不合格0、未実施0、完了率100%、次段階へ進行可」です。次の手順は将来の認証回帰確認用に保持します。

この段階で使用するDBは起動ごとにOS一時フォルダへ作成され、ウィンドウを閉じると破棄されます。本番DB、現行Tauri版のFAQ、バックアップは開きません。PowerShell 7で次を実行します。

```powershell
npm run build
dotnet run --project src-csharp/KnowledgeApp.CSharp/KnowledgeApp.CSharp.csproj
```

1. 上部に「C#移行試作 — 本番切替禁止」と、下部に「認証・分類・検索・FAQ表示用合成DB接続／本番データ未接続」と表示されることを確認する。
2. 初期表示のログインID`0000`に、任意の誤った合成パスワードを入力する。`AUTH-001`で拒否され、入力した文字列がエラー文へ表示されないことを確認する。
3. ログインID`0000`、パスワード空欄でログインする。「利用者の管理」が開き、初期管理者が「管理者」「利用中」と表示されることを確認する。
4. 「利用者を追加」で、ログインID`csharp-user`、表示名`合成一般利用者`、パスワード`Synthetic\*Pass!2026`、権限「一般利用者」を登録する。実際に使用しているパスワードは使わない。
5. 同じログインIDを全角または大文字小文字違いにして追加し、正規化後の重複として拒否されることを確認する。
6. `合成一般利用者`の「パスワード再設定」を開く。新しいパスワードを`Synthetic\*Reset!2026`、確認欄を`Synthetic\*Reset!2025`にすると`USR-001`で拒否され、両方を`Synthetic\*Reset!2026`にすると成功することを確認する。
7. 初期管理者自身の「利用停止」が押せないことを確認する。次に`合成一般利用者`を利用停止し、ログアウト後に同利用者でログインすると`AUTH-001`で拒否されることを確認する。
8. 初期管理者で再ログインして同利用者を「利用再開」し、パスワード`Synthetic\*Reset!2026`で一般利用者としてログインできることを確認する。一般利用者には左メニューの「利用者の管理」が表示されないことも確認する。
9. ログアウトし、別管理されている初期管理者の緊急認証情報でログインできることを確認する。緊急認証情報は本書、結果報告、画面キャプチャ、チャットへ記載しない。
10. ウィンドウを閉じて同じコマンドで再起動し、追加した利用者が消え、初期管理者`0000`へパスワード空欄で再びログインできることを確認する。これは合成DB破棄の正常動作である。
11. 現行Tauri版のFAQ、DB、バックアップが変更されていないことを確認する。

### 3.1 C#分類・検索・FTS5段階

2026-08-30にC#実装、合成DB自動試験、利用者手動試験26項目が合格した。現行React画面の見た目、分類CRUD、5階層制約、同義語、検索3範囲、NFKC、短語、50件ページング、並び替え、一致理由、再起動時の合成DB破棄を確認済みである。

当初のチェックシートNo.12・13は、6階層化または循環する親を選んで`CAT-003`・`CAT-006`を確認する手順だったが、実画面は設計どおり無効な親候補をプルダウンから除外した。今後の画面回帰では「5階層目が新規分類の親候補へ表示されないこと」「移動元自身と子孫が移動先候補へ表示されないこと」を合格条件とする。`CAT-003`・`CAT-006`は画面を迂回した直接要求に対する保存側防御として、C#合成DB自動試験で確認する。

### 3.2 C# FAQ詳細・閲覧履歴・最大10タブ段階

2026-08-30にC#実装、合成DB自動試験、利用者手動試験18項目がすべて合格した。現行React/Tiptap画面を再利用し、FAQ詳細、監査情報、旧検索情報、タグ、関連FAQ、削除・統合情報、閲覧記録、異なる10件の詳細読取、最大10タブ、重複防止、11件目拒否、一覧・FAQ別スクロール、タブ終了、再起動時破棄を確認済みである。画像・表・参考URL・コピー用テキスト・添付は後続ゲートのため本段階の判定へ含めない。本番DB、会社FAQ、実ファイルは使用していない。

## 4. Outlookメールの明示委譲

2026-08-30に、`.msg`の表示、明示走査、選択・マスク、委譲、破棄までの一連操作が利用者テストで合格済みです。次の手順は将来の回帰確認用に保持します。

1. リポジトリ外の専用フォルダへ、個人情報を含まない合成`.msg`を2件置く。
2. C#互換ホストの「メール履歴を確認」を開く。まだフォルダを選んでいない状態では「② .msgを確認」が無効で、メール一覧が空のまま自動走査されないことを確認する。
3. 「① フォルダを選択」で専用フォルダを選ぶ。選んだだけでは走査されず、フォーカスされた「② .msgを確認」を押した場合だけ2件が一覧化されることを確認する。
4. 以前「内容を安全に読み取れないため除外しました」となった`.msg`を再確認する。表示できた場合、一覧から選択できること、件名・送信者・宛先・本文の空欄や文字化けがないことを確認する。`MAIL-READ-PARTIAL`が出る、または空欄・文字化けがある場合は委譲しない。
5. なお除外される場合は、メール本文やアドレスを報告せず、下部の`MAIL-READ-...`原因コードだけを記録する。
6. 1件だけ「使用」をONにし、右側で件名、送信者、宛先、本文を編集して架空の伏字へ変える。
7. 「選択したメールをCodexへ委譲」を押し、最終確認で「はい」を選ぶ。委譲番号とCodexへ送る依頼文が表示されることを確認する。
8. 選択しなかった1件の内容や元ファイルの絶対パスが、画面に表示されたCodex依頼文へ含まれないことを確認する。委譲JSON本体は機密データになり得るため、結果票やGitへ貼らない。
9. 合成データだけでCodexへ依頼文を送り、KnowledgeApp専用スキルが確認待ちFAQ案を作り、元メールやフォルダを追加で開こうとしないことを確認する。
10. 「作成した委譲を破棄」を押し、対象の委譲番号を破棄できることを確認する。

## 5. 結果の返し方

次の項目だけを報告する。会社情報やメール本文は記載しない。

| 項目 | 記録例 |
|---|---|
| FAQタブ | 合格／不合格、表示倍率、テーマ、問題が起きた操作番号 |
| C#認証・権限 | 手順2～10の合格／不合格、問題が起きた操作番号とエラーコード |
| メール明示走査 | 合格／不合格、自動走査がなかったか |
| メール選択・マスク | 合格／不合格、未選択メールが含まれなかったか |
| 委譲・破棄 | 合格／不合格、エラー文またはエラーコード |

会社PCでの通常・緊急認証、利用者データルート、ローカルフルバックアップ・復元は、従来の`会社環境検証手順_0.4.4.md`と`会社環境検証結果_0.4.4.md`で別に実施する。
