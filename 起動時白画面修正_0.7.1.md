# 起動時の白画面・異常終了修正（0.7.1）

## 原因と修正

2026-09-06、インストール済み0.7.0が白画面のまま終了するとの報告を受けた。対象アプリに限定してWindowsの障害イベントを確認し、`CoreWebView2Deferral.Dispose()`から`Complete()`を呼んだ際の`COMException (0x8000000E)`を特定した。実FAQ・DB・認証情報は読取・変更していない。

`MainWindow.ConfigureHostSecurity`の資源イベントは`using var deferral`と`finally { deferral.Complete(); }`を併用していた。`Dispose`も完了通知するため、後者を削除し、正常・403の双方で1回だけ完了させる。これは画面ホスト内部の修正であり、データ変換・リセット・復元は行わない。

Microsoftの[WebView2スレッドモデル・Deferralの説明](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/threading-model#deferrals)も、`using`による完了を説明している。

## 再現・検証

`KnowledgeApp.StartupCheck`を追加し、本番と同じWPF資源イベント、実WebView2、ビルド済みReact画面で確認する。テスト専用の未認証応答を使い、DBを開かない。プロファイルは毎回専用のOS一時フォルダーで作成・終了確認後に削除する。

- 修正前：同じ起動試験が`0x8000000E`で失敗し、利用者の障害イベントと一致。
- 修正後：ログイン画面・CSSの表示、2回の再読込、欠落スクリプトへの403応答、ブラウザー終了が合格。
- React：31ファイル・240試験合格。型検査・UIビルド合格。
- ホスト信頼境界：157試験合格。
- 配布パッケージの実DLL・UIを専用一時配置へ複製した実WebView2起動試験も合格。利用者DBや実インストールは開いていない。
- NSIS：合成隔離環境の51試験合格。更新・実行中拒否・アンインストール・再導入・所有外データ保持を確認した。
- パッケージ回帰：47試験合格。版・同梱資源・データ混入・不正な配布情報の拒否を確認した。

従来の純粋なポリシー試験ではCOMイベントの二重完了を検出できなかったため、実WebView2試験をCIへ追加した。制限付き開発サンドボックスではWebView2初期化が進まなかったため、合成データだけを扱う同じ試験を通常Windows権限で実行した。利用者による修正版の通常起動確認は、自動試験の合格と区別する。

## 更新方法とデータ保護

アプリを閉じて0.7.1のインストーラーを実行する。事前アンインストールやFAQ・DB・パスワードの再登録は不要。C#専用保存先、DB第9版、バックアップ形式、共有プロトコルは維持する。実FAQ・DB・パスワード・復旧キーをGitHubを含むGitへ絶対にコミットしない。

要件定義書、基本設計書、データ分離詳細設計書へ修正と試験境界を反映した。Gitへのcommit/push、利用者の実インストール変更、共有サービスの登録は実施していない。

## 配布物

- インストーラー：`KnowledgeApp-CSharp-0.7.1-setup.exe`
- SHA-256：`4eb4a98011390085f2dda08103fc76ed2d221730b6f23c151c7aa19bf366bc41`
- ポータブルZIP：`KnowledgeApp-CSharp-0.7.1-win-x64.zip`
- ZIP SHA-256：`b2d35c3310764cd4f357404e6ceb260506147e74b8cc440e19bb59721714d9bd`

パッケージは165ファイル、うちUIは31ファイル。DB・実FAQ・認証情報は含めない。
