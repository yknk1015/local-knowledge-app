# KnowledgeApp 0.3.4 リリースノート

## 位置づけ

`0.3.4`は、Surface実機試験でリリース不可とした`0.3.3`の修正版である。`0.3.3`で検出した画像保存とパスワード再設定の高重要度不具合を修正し、自動回帰試験を追加した。自動検査とWindows用インストーラー検査を完了しても、`Surface実機試験結果_0.3.4.md`の全必須項目と会社規定の確認が終わるまでは会社向け正式リリース可と判定しない。

## 修正内容

- 画像を追加したFAQの保存時に`ATT-004`となる問題を修正した。Tiptapが自動で付加する表示用の`width`・`height`を保存JSONへ含めず、Rustが許可する管理画像属性だけを送信する。
- パスワード再設定をブラウザー組込みの入力欄からアプリ内ダイアログへ変更した。新しいパスワードを2回入力して一致を確認し、失敗時は原因と対処をダイアログ内へ表示する。
- パスワード再設定後とDB再オープン後に新しいパスワードで認証でき、以前の空パスワードを拒否するRust回帰試験を追加した。
- 実際のTiptapエディターから生成される画像ノードが、Rust保存境界で許可する4属性だけを持つReact回帰試験を追加した。

## データ形式と互換性

- アプリ版：`0.3.4`
- DBスキーマ版：`7`（変更なし）
- CSV形式版：`2`（変更なし）
- JSON形式版：`1`（変更なし）
- リッチテキスト形式版：`2`（変更なし）

DB列、マイグレーション、FAQ添付画像の保存先、バックアップ形式、Git除外対象は変更していない。`0.3.3`以前の利用者データを上書き・削除しないが、更新前にはフルバックアップを作成する。

## 0.3.3実機試験の扱い

`Surface実機試験結果_0.3.3.md`は、`ATT-004`と`AUTH-001`を検出してリリース不可とした履歴として保持する。0.3.3で合格した部分結果は0.3.4の正式判定へ流用せず、画像を含むFAQの登録、パスワード再設定・再起動後認証を含む全項目を`Surface実機リハーサル手順_0.3.4.md`に従って再実施する。

## 会社配布前の確認

- コード署名の要否、配布経路、WebView2・アプリ実行制限、ウイルス対策などは会社規定に従う。
- 未署名インストーラーを実機試験に使う場合も、許可された試験環境に限定し、ハッシュを照合する。
- 会社FAQ、パスワード、個人情報、会社の機密パスを試験記録や配布物へ含めない。
