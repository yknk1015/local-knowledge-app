# KnowledgeApp MVP要件実装対応表

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-08-16 |
| 基準コミット | `86eccce`（2026-08-29の再検査開始点） |
| 対象 | `FAQシステム要件定義書.md`で必須としたMVP要件 |
| 判定 | 実装済み、部分実装、未実装、運用確認待ち |
| 位置づけ | リリース前の追跡表。要件の正本は要件定義書、実現方法の正本は基本設計書とする。 |

本表の「実装済み」は、基準コミットのソースと自動テストで主要経路を確認できることを示す。Windows実機、会社規定、実データ相当件数による確認が別途必要な項目は「運用確認待ち」とする。

## 2. 機能要件

現在の正式版はC# 0.5.0である（2026-09-06、利用者による今回のPCの復元・確認完了を受領）。本表の基準コミットと旧版の実装判定は過去の追跡として残し、現在の構成は要件定義書v1.52・基本設計書v1.73、第17段階切替記録を参照する。未実施の会社環境確認は完了扱いにしない。

| 要件群 | 判定 | 実装済みの範囲 | 未完了または追加確認が必要な必須要件 |
|---|---|---|---|
| `FR-CAT` 分類 | 現行版・C#段階移植合格 | `FR-CAT-01`～`12`。1～5階層、移動、循環・6階層・同一親同名の拒否、同一親内の上下並べ替え、空分類の削除、件数、説明とCodex分類カタログを実装済み。C#版も同じ型付き契約とSQLite形式へ移植し、自動・手動試験が合格した。無効な親は画面で候補から除外し、保存側でも拒否する。 | C#分類段階の残件なし。全面切替は他ゲート合格後。 |
| `FR-FAQ` FAQ管理 | 現行版実装済み・C#編集・添付段階合格 | CRUD、論理削除、状態、添付、表示フラグ、タグマスター選択、旧検索情報・関連FAQの互換保持、未保存離脱警告、Codex連携、コピー用テキスト枠を現行版で実装済み。C#版のCRUD・管理一覧・監査・FTS5再構築、画像選択・貼り付け・確定・除去、添付付き独立複製、論理削除・復元保持は自動・利用者手動試験に合格した。 | C#履歴以降のゲート確認待ち。全面切替は全ゲート合格後。 |
| `FR-TAG` タグマスター | 実装済み | タグ一覧・追加・名称変更・使用件数、正規化重複拒否、使用中削除拒否、FAQ画面の複数選択、名称変更時の検索索引更新を実装済み。 | 0.4.0のWindows実機で追加・変更・FAQ選択・削除拒否を確認する。 |
| `FR-SRCH` 検索 | 現行版・C#段階移植合格 | 3検索範囲、NFKC等の正規化、日本語分解、FTS5 trigram・短語補助、同義語展開、全検索項目の重み付き順位、タグ・一致理由、50件ページング、URL・戻り状態復元、検索ログを実装済み。C#版も自動・手動試験で検索品質と除外条件へ合格した。 | 10,000件相当の性能比較待ち。全面切替は他ゲート合格後。 |
| `FR-SYN` 同義語 | 現行版・C#段階移植合格 | 代表語と複数同義語のCRUD、同一グループ正規化重複の統合、別グループ競合確認、保存直後の双方向検索反映、全体JSONへの収録を実装済み。C#版のCRUD・競合・検索展開も自動・手動試験で確認した。 | C#同義語段階の残件なし。全面切替は他ゲート合格後。 |
| `FR-VIEW` 詳細 | 現行版実装・タブ・C#詳細・添付・履歴段階合格 | パンくず、旧検索情報、タグ、リッチテキスト、画像・表、参考URL確認、コピー用テキスト、関連FAQ、削除・統合表示、閲覧履歴、最大10件タブを現行版で実装済み。C#詳細・タブ、画像専用仮想ホスト、表、URL確認後のC#再検証・既定ブラウザー委譲、コピー専用書込、閲覧履歴一覧・削除は自動・利用者手動試験に合格した。 | C#入出力以降のゲート確認待ち。 |
| `FR-MAIL` Outlook履歴 | `.msg`先行実装・手動合格 | C#画面からの明示的な1回走査、ローカルプレビュー、選択、件名・送信者・宛先・本文のマスク、固定メール委譲、明示破棄、Codex固定読取コマンドを実装した。原本パス・ファイル名・添付を委譲しない検査と、一連の利用者画面試験が合格した。 | 会社規定と`.pst`対応可否の別ゲート確認。 |
| `FR-REF` 外部資料参照 | 実装済み | `FR-REF-01`～`07`。パスはコピーのみ、HTTP/HTTPSは接続先確認後に既定ブラウザーで開き、外部資料本体は読取・検索・バックアップしない。 | 会社管理の実URL、割当ドライブ、UNCパスによる実機確認。 |
| `FR-EDIT` 編集 | 現行版実装済み・C#編集・添付段階合格 | 基本情報、回答、タグ、公開・表示設定の4区分を現行Reactで共用する。C#編集・監査、許可Tiptap構造、画像のマジックバイト・10MB・SHA-256検証、保存時確定・失敗時巻戻し、本文除去後削除、添付付き独立複製、外部URL再検証は自動・利用者手動試験に合格した。旧検索情報と関連FAQはC#更新でも保持する。 | C#履歴以降のゲート確認待ち。 |
| `FR-LOG` 履歴 | 現行版実装済み・C#履歴段階合格 | 検索・閲覧記録、遷移元検索、検索／0件検索／閲覧の3タブ、期間・文字列・50件ページング、削除対象件数確認、期間・全件削除、フルバックアップ復元を現行版で実装済み。C#版へ記録、一覧、NFKC・日付・0件絞り込み、50件ページング、期間・全件削除を移植し、合成DB自動試験と利用者手動試験20項目に合格した。 | C#バックアップ復元ゲートで履歴を含む復元を再確認する。 |
| `FR-BK` 入出力・バックアップ | 部分実装 | `FR-BK-01`～`10`、`13`～`19`。検証付きJSON入出力、フルバックアップ・復元、保存先事前検査、CSVプレビュー・取込前退避、管理IDを実装済み。 | `FR-BK-06`のネットワーク経路と`FR-BK-11`の別PCは実機確認待ち。 |
| `FR-SET` 設定 | 実装済み | `FR-SET-01`～`09`、`11`～`14`。テーマ、分類接頭辞、マスコット、Codex保存先表示、空パスワード方針を実装済み。 | 必須要件の残件なし。推奨の`FR-SET-10`も実装済み。 |
| `FR-AUTH` 認証 | 現行版実装済み・C#段階移植合格 | `FR-AUTH-01`～`03`。現行Rust版に加え、C#版へ未認証コマンド拒否、初期管理者、Argon2id、通常・緊急認証、空パスワード方針を移植し、合成DB自動試験と利用者手動試験が合格した。 | 全面切替は分類・検索以降の他ゲート合格後。 |
| `FR-USR` 利用者 | 現行版実装済み・C#段階移植合格 | `FR-USR-01`～`03`。C#版にも追加、停止・再開、管理者・一般利用者境界、自己停止・最後の管理者保護、パスワード再設定を移植し、合成DB自動試験と利用者手動試験が合格した。 | C#認証段階の残件なし。全面切替は他ゲート合格後。 |
| `FR-AUD` 監査 | 現行版・C#編集段階合格 | `FR-AUD-01`、`02`。C#合成DBと利用者手動試験23項目で新規・更新・複製・削除・復元の作成者・更新者と管理一覧の表示名を確認した。 | C#編集段階の残件なし。全面切替は他ゲート合格後。 |

## 3. 非機能要件

| 要件群 | 判定 | 未完了または追加確認が必要な要件 |
|---|---|---|
| `NFR-USE` 操作性 | 部分実装 | 主要な画面・日本語エラー・確認・キーボード操作は実装済み。`NFR-USE-05`の1280×720、テーマ判別性、読み上げをWindows実機で確認する。 |
| `NFR-PERF` 性能 | 運用確認待ち | 50件ページングは実装済み。FAQ 10,000件・分類1,000件のフィクスチャーで検索1秒、分類・詳細0.5秒、画面無停止を測定する。 |
| `NFR-DATA` データ保護 | 部分実装 | SQLiteトランザクション、WAL、DB移行前・復元前・CSV取込前の検証済み退避、既存バックアップ保護を実装済み。ネットワーク保存先の切断試験を完了する。 |
| `NFR-SEC` セキュリティ | 部分実装 | ローカル保存、許可リッチテキスト、URL制限、Codex境界、Git混入防止、履歴削除、JSONの危険本文・Git配下拒否を実装・検査済み。`NFR-SEC-09`の公開範囲は会社規定を確認する。 |
| `NFR-MNT` 保守性 | 部分実装 | スキーマ版、層分離、文書同期、UTF-8・Git検査を実装済み。`NFR-MNT-02`は検索重み・除外語・同義語実装時に調整可能な構成とし、`NFR-MNT-06`は正式リリース時に移行内容を記録する。 |

## 4. MVP区分の確定

- JSONエクスポート・検証付きインポート（`FR-BK-01`、`FR-BK-02`）は、要件定義書どおりMVP必須として実装済みである。
- 同義語辞書全体のJSON入出力（`FR-SYN-04`）は、MVP必須の全体JSONへ同義語データを含める方式で実装済みである。
- 自動バックアップのスケジュール、管理者向け集計分析、お気に入り、解決率、複数人同時利用は将来候補のままとする。C#移行は2026-08-29付で必須へ変更したが、非デグレ同等性ゲートに未合格の間は現行Tauri版を主系とし、切替優先度を下げる。
- 要件をリリース対象外へ変更していないため、今回の整理に伴う利用者承認を要する要件縮小はない。

## 5. 基準環境の再現確認

2026-08-29に現行`0.4.4`を再確認し、次を実行してすべて成功した。利用者DB、会社FAQ、メール原本は使用していない。

- Reactテスト 98件
- C#認証・権限、分類CRUD、同義語、日本語検索、FTS5、50件ページング、FAQ詳細・関連FAQ・最大10件分の読取、FAQ編集・監査、リッチテキスト、画像確定・独立複製・除去・削除復元、URL・コピー境界、検索・閲覧履歴、DB第1～7版の合成DB試験
- Rustテスト 84件（別に明示的な無視1件）
- TypeScript型検査とViteビルド
- UTF-8（BOMなし）・LF検査
- 利用者データ・実FAQ・バックアップ等のGit混入防止検査
- KnowledgeApp専用Codexプラグインの安全境界検査

PowerShellスクリプトは、UTF-8（BOMなし）の日本語を確実に解釈するため`pwsh -NoProfile -File ...`で実行した。Windows PowerShell 5.1から直接`-File`実行する方法は基準手順にしない。GitフックはPowerShell 7を優先し、5.1しかない場合もUTF-8で明示読取する既存フォールバックを使用する。

自動テストはOS一時フォルダ内の生成フィクスチャーだけを使用し、本番候補DB、会社FAQ、外部資料本体を読み取っていない。本番候補データを使うリリースリハーサルでは、作業開始直前に画面から検証済みフルバックアップを作成し、保存先・作成日時・復元検証結果をリリース試験記録へ残す。
