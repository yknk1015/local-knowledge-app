# KnowledgeApp 0.3.4 リリース試験記録

## 1. 候補情報

| 項目 | 結果 |
|---|---|
| 試験日 | 2026-08-16 |
| 候補版 | 0.3.4 |
| 対象コミット | 未固定。修正作業ツリーで自動検査と仮成果物生成を実施 |
| OS | Windows 11 Home 10.0.26200、64ビット |
| 生成物 | `src-tauri/target/release/bundle/nsis/KnowledgeApp_0.3.4_x64-setup.exe` |
| サイズ | 8,433,098 bytes |
| SHA-256 | `9194B31560CF076F9E244836816E12BB54EDF75A90ECA165F73837C0400CDD03` |
| Authenticode | `NotSigned` |
| 現時点の判定 | 0.3.3実機不具合のコード修正・自動回帰試験は合格。Surface全項目と会社規定の再確認待ち |

このハッシュは修正内容を確認するために現在の作業ツリーから生成した仮成果物へ固定する。正式候補コミットから再生成した場合は、Surfaceへ渡す前に本記録、リリース手順、結果票のハッシュを同じ新しい値へ更新する。

## 2. 不具合修正の回帰試験

| 対象 | 結果 |
|---|---|
| `ATT-004` | 合格。実際のTiptapエディターで画像を追加し、Rust保存境界が許可する`src`、`alt`、`title`、`attachmentId`だけが出力されることを確認 |
| `AUTH-001` | 合格。初期管理者のパスワード再設定後とDB再オープン後に新パスワードで認証でき、旧空パスワードを拒否することを確認 |
| パスワード確認入力 | 合格。一致時だけRust APIへ正確な値を送り、不一致時はダイアログ内エラーとしてAPIを呼ばないことを確認 |

## 3. 自動検査結果

| 検査 | 結果 |
|---|---|
| `npm test` | 合格。19ファイル、87テスト |
| `npm run build` | 合格 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | 合格 |
| `cargo clippy --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings` | 合格 |
| `cargo test --jobs 1 --manifest-path src-tauri/Cargo.toml` | 合格。80件合格、性能試験1件は通常実行では意図的に除外 |
| `scripts/test-release-performance.ps1` | 合格。除外した性能試験1件をreleaseモードで実行 |
| `scripts/check-text-encoding.ps1` | 合格 |
| `scripts/check-no-runtime-data.ps1` | 合格 |
| `scripts/test-codex-plugin.ps1` | 合格 |
| `scripts/check-release-bundle-config.ps1` | 合格 |
| `npm run tauri build` | 合格。利用者単位NSISを生成 |

通常の並列`cargo test`は、Windowsリンカーが同名のテスト出力を同時に開く`LNK1104`で1回失敗した。テストコードの失敗ではなく、`--jobs 1`で同じライブラリ・バイナリ・文書試験を再実行して全件合格した。CIまたはクリーンな正式候補環境では通常コマンドも再確認する。

## 4. 性能結果

OS一時フォルダだけにFAQ 10,000件、分類1,000件、検索履歴1,000,000件、0件検索100,000件を生成した。利用者データと会社FAQは使用していない。

| 対象 | 結果 |
|---|---|
| 分類1,000件取得 | 15 ms |
| 同義語を含む日本語検索、10,000件一致、重要度順、50件取得 | 103 ms |
| FAQ詳細 | 1 ms未満 |
| 履歴100万件のページ・条件付き件数 | 598 ms |
| CSV 10,000件 | 52 ms、3,400,241 bytes |
| DBバックアップ | 4,626 ms、199,389,184 bytes |

通常検索1秒以内、FAQ詳細0.5秒以内、分類取得0.5秒以内の基準に合格した。

## 5. 配布物検査

- Tauriのbundle targetがNSIS、install modeが`currentUser`であることを確認した。
- `bundle.resources`が空、`externalBin`が未設定であることを確認した。
- 0.3.4のNSIS成果物が生成され、配布フォルダにDB、WAL、SHM、FAQ、履歴、添付画像、CSV、JSON、バックアップ、診断ログ、Codex提案・委譲が含まれないことを確認した。
- 実行ファイルとインストーラーは未署名である。実名を埋め込む対応は行わず、署名要否は会社規定の判断事項として維持する。
- Surface検証ZIP `KnowledgeApp_Surface_Rehearsal_0.3.4.zip`を生成し、12ファイル、11,751,456 bytes、SHA-256 `301E05D2206F804181C5280C0917F933A24547DACD62DC4BDF41D3D457B714A1`を確認した。パッケージ内の旧版・0.3.4インストーラー、全ファイルマニフェスト、署名状態の検証は合格した。

## 6. 残る手動確認

`Surface実機試験結果_0.3.3.md`は高重要度2件によりリリース不可である。0.3.4では合格済み部分も流用せず、`Surface実機リハーサル手順_0.3.4.md`に従い、新規インストール、旧版更新、画像保存、パスワード再設定・再起動後認証、CSV、検索、バックアップ・復元、表示倍率、アンインストール・再インストールを全件再実施する。会社FAQ、パスワード、個人情報、会社の機密パスは記録しない。
