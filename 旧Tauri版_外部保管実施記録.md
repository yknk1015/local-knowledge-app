# 旧Tauri版の外部保管実施記録

実施日：2026-09-06。旧ソースの整理であり、FAQデータの再移行ではない。

## 保管先と復旧基準

利用者確認済みの保管先：

`C:\Users\takow\Documents\game_development\program\old\KnowledgeApp_Archive\Tauri_0.4.4_20260906`

- 基準コミット：`277f38c2a374889d21cb61798ab072dfb16e98be`
- ローカルタグ：`legacy-tauri-final`、`csharp-0.5.0`
- `source-baseline-277f38c.zip`：移行完了時のソース336ファイル。旧版・C#・共通UI・設計・ビルド定義を含む。
- `source/`：ZIPの展開済みソース。全336ファイルが元ソースとSHA-256一致。
- `release/KnowledgeApp_0.4.4_x64-setup.exe`：旧版配布物。コピー前後のSHA-256一致。再ビルドしたものではない。
- `retired-worktree/`：現行作業ツリーから移した旧`src-tauri`（旧ビルドキャッシュ等を含む）と旧専用スクリプト6本。復旧の正本は上記ZIPであり、キャッシュからの再現を前提にしない。

| 対象 | SHA-256 |
|---|---|
| ソースZIP | `84744bf87b2fd099a6f56bade3a128b81152251453d5d7cb5314e8bf37c030e0` |
| 旧0.4.4セットアップ | `507f5b323389f61fd1e5e1ffeb0ea2253295dcd51603a0214a0458cef65b3b43` |

タグとコミットはローカルに作成した。GitHubへのpush、履歴全体の書換え、新規リポジトリ作成は行っていない。旧版は通常更新しない。

## 整理内容

- DB第1～7版のSQLを`src-csharp/KnowledgeApp.Data/Migrations`へ内容不変で移し、C#で埋め込む。`Test-CSharpSourceIndependence.ps1`が固定ハッシュを検査する。
- 通常CIからRust/Tauriビルドを分離する。独立NSISをPATHまたは開発用の明示パスで使用する。
- Rust相互試験は固定コミットを用いる手動CIと、明示的な旧ソース指定によるローカル試験に残す。未実行を合格と数えない。
- 共通React/Tiptap UIと互換API分岐は維持する。Tauri CLIだけを通常のpackage設定から外す。
- 旧版/C#の本番DB、WAL、SHM、FAQ画像、利用者バックアップ、導入済みアプリにはアクセス・変更しない。

## 検証

整理前：React 230件、型検査・フロントエンドビルド、C#合成DBの全機能・第1～7版移行、新しい出力先へのC#ホストビルドが合格。

整理後は、旧ソース・既存node_modules・C#ビルド出力を含まない新しい一時ソース環境で、npm ci、React 230件、型検査・UIビルド、C#ホストの新規ビルドが合格した。SQL7ファイルの保存バイトと埋込み資源名・資源バイトの同一性も合格。

DataCheck全体、旧版DB1～7復元716件、Codex72件、通常FinalInterop117件、復元確認112件、安全境界157件、本番分離93件、復元中断351件、継続保存74件、本文互換271件、旧版起動ガード42件、導入ロック10件、単一起動37件、UI同梱13件、MailCheck、MigrationGateが合格した。いずれも合成データのみ。

外部の旧ソースを明示して実RustのCodex JSON相互試験とバックアップ往復を実行し、後者を含むFinalInterop136件が合格した。未指定ソース、相対ソース、不正引数の拒否4パターンも確認した。移動済みRustキャッシュの絶対パスが無効になったため、新しい`interop-build-cache/`へ再ビルドした。続くリンク時の一時的なLNK1104は同条件再試行で解消し、元キャッシュは削除していない。

C#配布ZIPとNSISを生成できることを確認し、GUID専用領域だけを用いたNSISの導入・更新・削除回帰51件が合格した。NSISコンパイラーは既存のローカルコンパイラーの絶対パスを明示し、Tauriビルドによる取得は行っていない。導入済みアプリは更新していない。GitHub Actions上でのNSIS準備・ワークフロー実行はpush後の別確認とする。

クリーンな依存復元の前後で同梱UI30ファイルのSHA-256がすべて一致し、表示コード・素材は変わっていない。既存の通常ビルド先に残っていた旧構成のUI成果物は、Git除外済みの`src-csharp/KnowledgeApp.CSharp/bin/archive-stale-ui-20260906`へ退避して再生成する。インストール済みアプリのUIではない。

互換ビルド後にも外部`source/`の336ファイルと元ZIPの一致を再確認した。ソースZIP・旧インストーラーのハッシュは不変。通常利用の手動再試験や再インストールは不要であり、対象環境・実Codex会話等の既存未実施項目とは区別する。

## 旧版の参照・復旧

2026-09-06の文書整理で、旧版試験書31件と計画/追跡文書6件の原文を同じ保管ルートの`document-cleanup-20260906/`へ新規コピーし、37件すべてのSHA-256一致を確認した。基準ZIPと異なる更新があった保管計画書も、今回のコピーへ含めた。元ZIP・展開済みソースは上書きしていない。現行リポジトリの文書削除は利用者が行い、未完了事項は[今後の課題](今後の課題.md)、削除対象と参照方法は[文書整理ガイド](文書整理ガイド.md)で追跡する。削除・commit・push完了を意味しない。

1. ソースZIPのハッシュを上記と照合し、新しい空のフォルダーへ展開する。現行C#作業ツリーへ上書き展開しない。
2. 旧コードの確認には展開した`source`相当を使う。ビルドする場合はその場所でNode依存を復元し、Rust MSVC・C++ Build Tools・WebView2を用意する。合成データの試験を優先し、旧本番アプリを自動起動しない。
3. インストーラーの再利用が必要な場合はSHA-256と0.4.4であることを確認する。アプリの更新・再導入・FAQの復元は別の明示操作とする。

ソースZIPはFAQバックアップではない。旧利用者データは元の`LocalApplicationData/jp.local.webknowledgesystem`、C#の正本は`LocalApplicationData/jp.local.webknowledgesystem.csharp`に残る。現行C#のデータを旧版へ自動同期しない。
