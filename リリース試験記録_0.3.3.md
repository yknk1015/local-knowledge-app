# KnowledgeApp 0.3.3 リリース試験記録

## 1. 候補情報

| 項目 | 結果 |
|---|---|
| 試験日 | 2026-08-16 |
| 候補版 | 0.3.3 |
| 対象コミット | 未固定（作業中のため、候補コミット作成後に追記する） |
| OS | Windows 11 Home 10.0.26200、64ビット |
| 生成物 | `src-tauri/target/release/bundle/nsis/KnowledgeApp_0.3.3_x64-setup.exe` |
| サイズ | 8,433,180 bytes |
| SHA-256 | `8063FE644E65F0C1E550A52B958E5275FB94CB15A7EEE5C49F80C7E2CB0E8471` |
| Authenticode | `NotSigned` |
| 現時点の判定 | 正式リリース保留。自動検査合格、専用実機・会社規定確認待ち |

## 2. 自動検査結果

| 検査 | 結果 |
|---|---|
| `npm test` | 合格。19ファイル、83テスト |
| `npm run build` | 合格 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --check` | 合格 |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | 合格 |
| `cargo test --manifest-path src-tauri/Cargo.toml --locked` | 合格。78件合格、性能試験1件は通常実行では意図的に除外 |
| `scripts/test-release-performance.ps1` | 合格。除外した性能試験1件をreleaseモードで実行 |
| `scripts/check-text-encoding.ps1` | 合格 |
| `scripts/check-no-runtime-data.ps1` | 合格 |
| `scripts/test-codex-plugin.ps1` | 合格 |
| `scripts/check-release-bundle-config.ps1` | 合格 |
| `git diff --check` | 合格 |
| `npm run tauri build` | 合格。利用者単位NSISを生成 |

Rust試験時にMSVCリンカーがDLL用の`.lib`と`.exp`を作成した旨を標準出力へ表示した。これはWindowsの動的ライブラリー生成に伴う通常メッセージであり、警告・失敗ではないことを確認した。

## 3. 性能結果

OS一時フォルダだけにFAQ 10,000件、分類1,000件、検索履歴1,000,000件、0件検索100,000件を生成した。利用者データと会社FAQは使用していない。

| 対象 | 結果 |
|---|---|
| 分類1,000件取得 | 8 ms |
| 同義語を含む日本語検索、10,000件一致、重要度順、50件取得 | 56 ms |
| FAQ詳細 | 1 ms未満 |
| 履歴100万件のページ・条件付き件数 | 582 ms |
| CSV 10,000件 | 50 ms、3,400,241 bytes |
| DBバックアップ | 4,596 ms、199,389,184 bytes |

通常検索1秒以内、FAQ詳細0.5秒以内、分類取得0.5秒以内の基準に合格した。詳細な環境とフィクスチャー生成時間は`リリース性能試験記録.md`を参照する。

## 4. 配布物検査

- Tauriのbundle targetがNSIS、install modeが`currentUser`であることを確認した。
- `bundle.resources`が空、`externalBin`が未設定であることを確認した。
- 現行版`0.3.3`のNSIS成果物が1件であることを確認した。過去版のローカルビルド成果物は検査対象の現行版と区別し、利用者の成果物として削除していない。
- 配布フォルダにDB、WAL、SHM、FAQ、履歴、添付画像、`manuals`データ、CSV、JSON、バックアップ、診断ログ、Codex提案・委譲が含まれないことを検査した。
- 候補インストーラーは未署名である。署名要否は会社規定の確認事項とする。

## 5. 手動試験の取扱い

通常のWindows利用者環境で候補アプリを起動したところ、プロセス固有に指定した一時`LOCALAPPDATA`ではなく、Windows/Tauriが通常の利用者データフォルダを解決することを確認した。実データへ影響させないため、FAQの編集・保存その他の操作を行わず直ちに試験を中止した。

以後の手動試験は、実データから隔離した専用Windows利用者または別PCで行う。試験用データはその環境で新規作成し、会社FAQや既存DBを複製しない。

## 6. 未実施・担当者確認事項

| 項目 | 状態・必要条件 |
|---|---|
| 新規インストール・空DB・初回ログイン | 専用Windows利用者または別PCが必要 |
| 旧版上書き更新・データ保持 | 専用環境に試験用旧版DBを作成して実施 |
| アンインストール・再インストール・データ保持 | 専用環境で実施 |
| ショートカット起動・単一起動の実表示 | 専用環境で実施 |
| 1280×720、100%・125%・150% | 専用環境で記録 |
| 緑・青テーマ、通常・詳細・900px以下、キーボード、フォーカス、読み上げ名 | 専用環境で記録。`prefers-reduced-motion`の自動試験は合格済み |
| 実Excel CSV往復 | 会社と同等のExcel環境が必要 |
| ローカル・UNC・HTTP/HTTPS外部資料参照 | 会社が許可した試験先が必要 |
| ネットワークバックアップ・別PC復元 | 会社が許可した保存先と専用環境が必要 |
| コード署名 | 要否、証明書、署名担当、更新手順の会社判断が必要 |
| GitHub・配布 | 公開範囲、会社情報、配布経路の会社判断が必要 |
| 会社PC制約 | WebView2、アプリ実行制限、ウイルス対策、保存場所、UNCアクセスの確認が必要 |

## 7. 次回記録する内容

専用環境の試験では、実施者、日時、Windows利用者またはPCの識別方法、旧版と更新後版、各表示幅・倍率、バックアップ名と復元結果、インストーラーのSHA-256、既知事項を記録する。FAQ本文、パスワード、会社の機密パスは記録へ転記しない。
