# 管理者パスワード復旧 実装計画

作成日：2026-09-06。状態：C# 0.6.0へ実装。以下は採用仕様と検証記録。実利用環境へのインストール・キー発行は未実施。

## 1. 確定した方針

利用者の追加回答により、メール方式からオフライン復旧キー方式へ変更する。復旧後は空欄へ初期化せず、新しいパスワードを設定する。メールアドレス登録、確認メール、送信サービス、秘密の質問は今回実装しない。

キーの仕様は読みやすさと安全性を両立するため、英数字16文字を4文字ずつ区切る設計とする。ハイフン込みの表示は19文字。現行の通常パスワードのArgon2idハッシュ化を維持し、固定共通の緊急認証は新方式の提供と同時に廃止する。

既存FAQ、利用者、パスワード、権限、空欄許可方針は保持する。既存管理者の復旧キーは未発行のまま移行し、管理者の設定から発行できるようにする。

## 2. 現行実装との対応

| 現行の場所 | 確認事項・改修対象 |
|---|---|
| `src-csharp/KnowledgeApp.Data/Argon2PasswordCodec.cs` | 通常パスワードはすでにArgon2id。ランダム16バイトsalt、32バイト出力、メモリ19MiB・反復2・並列度1を維持する |
| `src-csharp/KnowledgeApp.Data/KnowledgeDatabase.cs` | 固定緊急ハッシュの定数・初期管理者向け追加照合を削除する。実際の秘密値は文書や試験へ転記しない |
| `src-csharp/KnowledgeApp.Data/AuthenticationService.cs` | 本人の通常パスワード再確認、キー発行・照合・再設定・セッション失効を追加する |
| `src-csharp/KnowledgeApp.CSharp/AuthenticationCommandDispatcher.cs` | 用途を限定した型付き契約だけを追加する。任意SQL・任意ファイル操作は追加しない |
| `src/pages/LoginPage.tsx` | 紛失案内、管理者ID・復旧キー、新パスワード入力を追加する |
| `src/pages/SettingsPage.tsx` | 管理者本人のキー発行・再発行、発行状態、別保管の案内を追加する |
| `src/api/knowledgeApi.ts`・型定義・`AuthContext.tsx` | 固定契約と通常認証／復旧だけの権限の分離を反映する |
| C#マイグレーション・バックアップ検査 | 新しい認証補助情報と旧DB復元の互換性を追加する |

## 3. 復旧キーの形式と保護

| 項目 | 設計 |
|---|---|
| 長さ | 英数字16文字。4文字×4組、区切りハイフン込み19文字 |
| 文字集合 | `23456789ABCDEFGHJKLMNPQRSTUVWXYZ`の32種類。0・1・I・Oを使わない |
| 生成 | C#の暗号学的乱数で各文字を偏りなく選択。32種類の16乗＝80ビットの組合せ |
| 入力 | ASCII英小文字は大文字へ正規化し、表示用ハイフン・空白を除いて照合する。その他の文字を曖昧に推測・補完しない。入力長にも上限を設ける |
| 保存 | 管理者本人のIDへ結び付け、ランダムsalt付きArgon2idの検証用ハッシュだけをDBへ保存する |
| 平文表示 | 発行・再発行・復旧成功時の一度だけ。再表示はせず、紛失時は通常ログイン後に再発行する |
| 保管 | パスワード管理アプリや紙などで別保管。FAQ本文、Git、ログへ保存する案内はしない |
| 有効性 | 現在のDBで一回限り。使用成功・再発行で旧キー失効。保管用キーに10分等の短い期限は付けない |
| 照合後の権限 | 新パスワード設定だけを許す5分の権限をC#の起動中メモリに保持。通常ログイン状態は作らない |
| 試行制限 | 5回連続失敗で復旧照合だけを15分停止する初期値。回数・停止期限をDBへ保持し、再起動で解除しない。通常パスワードのログインをロックしない |

16文字・32種類の80ビットは本アプリの設計値である。NISTは保存型復旧コードについて64ビット以上の乱数、ハッシュ保存、試行制限、使用後の失効・新規発行を定めており、長さを決める参考にした。本アプリ全体がNISTの認証保証レベルに適合すると主張するものではない。[NIST SP 800-63B-4：Saved Recovery Codes](https://pages.nist.gov/800-63-4/sp800-63b.html#saved-recovery-codes)

本計画には実際に使うキーやパスワードを記載しない。通常パスワードと復旧キーは用途を分け、復旧キーをログイン欄へ入力しても通常認証の代わりにしない。

## 4. 利用者の操作

### 4.1 新規利用時

1. 新規の初期セットアップで、初期管理者のローカル認証後に復旧キー作成の案内を表示する。既存管理者の設定を未認証で変更できる入口は作らない。
2. 「復旧キーを作成する」と「後で設定する」を用意する。作成したキーを一度だけ表示し、別保管を案内する。
3. スキップ時は「復旧キーを作成しない場合、パスワードを忘れると、ご自身で再設定できません。他の有効な管理者にも依頼できない場合、アクセスを復旧できなくなることがあります。」と表示する。
4. 案内状態を保存し、毎回の起動で強制表示しない。設定画面では未発行状態を確認できる。

新規の初期管理者が空欄パスワードの間は、本人再確認も空欄認証と同じ強さである。初回に通常パスワードの設定を案内するが、今回の移行で既存利用者のパスワードを勝手に変更しない。

### 4.2 管理者設定

有効な管理者は自分のキーだけを発行・再発行できる。現在の通常パスワードを再入力し、C#側で再認証する。再発行の確定までは以前のキーを保持し、保存成功時に新ハッシュへ置き換える。一般利用者には発行APIを許可しない。

別の管理者が利用者の通常パスワードを再設定する既存機能は維持する。ただし他人の復旧キーを閲覧・発行する機能は追加しない。他の管理者によるパスワード再設定時は対象の旧キーと未完了の復旧権限を失効させ、本人へ新規発行を案内する。

### 4.3 パスワードを忘れた場合

1. ログイン画面の「パスワードを忘れた場合」を開く。
2. 一般利用者には「管理者にパスワードの再設定を依頼してください。」と案内する。
3. 管理者はログインIDと保管していた復旧キーを入力する。
4. C#が有効な管理者・現在のキー・試行制限を検査する。成功時は新しいパスワードと確認用入力へ進む。
5. 新しい非空パスワードを保存し、使用したキーを無効にするとともに、新しいキーを発行・一度だけ表示する。関連する認証・復旧セッションを失効させる。
6. 新しいキーを別保管し、通常ログイン画面から新しいパスワードでログインする。

原案の「空欄へ戻す」は採用しない。既存の空欄許可設定がONでも、復旧経由では空欄を拒否する。パスワードの最大長・Unicodeの扱いは現行仕様を維持する。最低長の引上げは別の全体パスワード方針変更として検討し、既存ユーザーへ無断適用しない。

未発行の管理者には「復旧キーが作成されていません。」と表示する。存在しない利用者・一般利用者・利用停止者の詳細や管理者一覧は公開しない。登録状態の表示に伴う情報開示はローカル画面だけの範囲に限定する。

## 5. C#側の認証境界

- キー照合成功をReactの真偽値やURLパラメーターで表現して信用しない。C#が十分な乱数の一時権限を生成し、対象ID・キーの版・期限へ結び付ける。
- 照合だけではキーを消費しない。キャンセル・新パスワードの保存失敗で復旧手段を失わない。再設定確定時に有効性を再検査する。
- パスワードハッシュ更新、旧キー消費、新キーのハッシュ保存、世代更新、失敗回数初期化を同一トランザクションで確定する。並行要求の成功は一度だけとする。
- 通常の認証サービスと同期し、復旧完了、本人または別管理者によるパスワード変更、利用停止、キー再発行、DB復元、アプリ終了で古い復旧権限を失効させる。
- 新しいキーの表示前にアプリが停止しても、新パスワードでログインして再発行できる。平文キーを再表示するための保存は行わない。
- キー照合済みでも、FAQ閲覧、設定保存、バックアップ、利用者追加等を許可しない。通常セッションの権限検査を迂回させない。
- 復旧画面には新パスワード入力2回と明示的な保存操作を設ける。エラーは日本語で原因と対処を示し、入力値・秘密をログや例外へ出力しない。
- 失敗回数・停止期限はC#側で検査する。アプリ再起動や要求の作り直しで制限を解除しない。OS時計・DBを改変できる相手への完全な制限維持は保証しない。

実装した用途別契約は`get_recovery_key_status`、`issue_recovery_key`、`skip_recovery_setup`、`verify_recovery_key`、`complete_password_recovery`、`cancel_password_recovery`。未認証で呼べる契約は紛失手続きに必要な固定対象だけとし、管理者の一覧や秘密の検証値は返さない。

## 6. データ・移行・バックアップ

| 項目 | 方針 |
|---|---|
| 新しい保存情報 | 管理者IDを外部キーとするキーの検証ハッシュ、世代、発行日時、案内状態、連続失敗数、停止期限。DB第8版。通常認証失効用auth_versionも保持する |
| 既存移行 | 新しいキーはNULL／未発行。新規セットアップと既存移行を区別し、既存FAQ・ID・認証ハッシュ・役割・空欄方針を保持する |
| パスワード | 既存Argon2idを継続。平文保存からの変換処理は不要。新キーもsalt付きArgon2idで検証する |
| フルバックアップ | キーのハッシュ・世代・状態は認証情報としてDBスナップショットに含める。平文キー・一時再設定権限は含めない |
| CSV／JSON・Codex | 復旧情報をエクスポート、分類カタログ、委譲へ追加しない |
| 復元時 | 起動中の通常・復旧セッションを破棄し、復元された認証状態を使用する。旧DB版から復元した場合はキー未発行へ移行する |
| マイグレーション失敗 | 移行前安全バックアップ、トランザクション、元データ保持を維持する。認証失敗を空DB再作成で隠さない |
| Git・配布 | FAQ、DB、パスワード、キー、認証ハッシュなど実際の利用者データを絶対にコミット・同梱しない。検証は合成データだけで行う |

古いDBバックアップを復元すると、当時のキーの有効状態やパスワードへ戻る。「現在のDBで一回限り」と「過去のバックアップまで永久に再使用不可」は異なる。完全ローカル方式のため前者を採用し、バックアップは認証情報と同様に厳重に保管する。復元後は必要に応じてキーを再発行する。鍵の漏えい対応で古いバックアップへ戻す場合は、通常パスワードとキーを再設定するまで安全が回復したと判断しない。

同じWindows利用者権限でDB・アプリを改変できる相手や、旧実行物への巻戻しまでメールなしのキーだけで防げるとは説明しない。現行SQLiteを暗号化する機能は今回追加しない。新しいDB版の旧アプリ利用は保証せず、切戻しは対応版と移行前バックアップの組で判断する。

## 7. 固定緊急認証の廃止

新しい復旧方式を提供するリリースで、固定緊急ハッシュの定数と追加照合分岐をC#製品コードから削除する。設定、別コマンド、環境変数などから再有効化できる抜け道を残さない。現行の通常パスワードは変更しない。

移行後の既存管理者はキー未発行であるため、ログイン可能な間に発行するよう設定画面・更新案内で知らせる。スキップは可能とし、キーも別の管理者も使えないときに認証なしで初期化する救済は作らない。

固定緊急認証の廃止は過去に配布された実行物を無効化するものではない。また、同じ文字列を利用者が通常パスワードとして自分で設定済みなら、通常ハッシュと一致してログインすることまで特別に禁止するものではない。廃止するのはDBの通常パスワードと無関係に通る追加認証経路である。

## 8. 実装順序・試験・文書更新

| 段階 | 主な作業 | 完了条件 |
|---|---|---|
| 1. データ基盤 | 非破壊マイグレーション、キー生成・正規化・ハッシュ、状態管理 | 既存DBを壊さず16文字キーが管理者ごとに照合できる |
| 2. C#の復旧処理 | 本人再認証、発行・再発行、試行制限、限定権限、再設定・失効のトランザクション | 通常権限へ迂回できず、二重使用・部分保存がない |
| 3. UI・API | 新規案内、スキップ、設定、紛失案内、キー入力、新パスワード、新キー表示 | キーボード操作・狭い画面・エラー・保存中操作が確認できる |
| 4. 移行・復元 | 旧版からの未発行移行、新版バックアップ、既存認証保持、キー状態と一時権限の復元境界 | FAQ保持と旧バックアップ互換、復元前権限の失効が確認できる |
| 5. 固定認証廃止 | 固定照合と定数の削除、既存試験・説明の更新 | 通常パスワードのみの認証となり、旧DB復元でも追加経路が復活しない |
| 6. 配布確認 | 型検査、C#・Reactテスト、ビルド、NSIS、文字コード・データ混入検査 | 合成環境の一連復旧が成功し、秘密が配布物へ混入しない |

重点試験：新規案内／スキップ、既存未発行、16文字と80ビット生成設計、区切り・大小文字・不正文字、一般利用者・停止管理者・他管理者からの拒否、通常パスワード再確認、期限、誤キーと回数制限、再起動、キャンセル、保存失敗、並行照合・二重確定、再発行後の旧キー拒否、パスワード変更・利用停止・復元後の権限失効、復旧限定状態でのFAQ／設定API拒否、平文非保存、既存FAQ・バックアップ保持、固定認証分岐の廃止。

正本は要件定義書、基本設計書、データ分離詳細設計書。現行の暫定固定認証に関する記載を置換し、履歴には当時の仕様であることを明示する。README・運用手順・C#切替案内・Git公開方針も、新しい利用手順とキーの保護・バックアップ制約へ同期する。

本改修はC#とReactへ実装した。実DB変更、実利用者のキー発行、インストール、コミット・pushは行っていない。人間による対象端末の表示・操作確認と、自動試験の結果を区別し、本番データや実際のキーの提示を求めない。

## 9. 採用しない案と根拠

- 秘密の質問を単独の復旧手段にする案：答えが推測・調査されやすく、ハッシュ化しても回答自体の弱さは改善しない。[OWASPの復旧指針](https://cheatsheetseries.owasp.org/cheatsheets/Forgot_Password_Cheat_Sheet.html#security-questions)
- 空欄パスワードへの初期化：本人確認後に新パスワード設定へ直接進む。通常権限は再ログイン後に付与する。
- メール送信：今回の選択で不要になった。宛先、SMTP、メールAPI、外部送信の設定を追加しない。
- 固定共通パスワード、固定コード、秘密の質問の答えを使った別の緊急経路：作らない。
- パスワードの平文・復号可能な保存：既存Argon2idを継続する。[OWASPの保存指針](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)

## 10. 実装・自動検証記録（2026-09-06）

対象はC# 0.6.0 / DB第8版。メール機能は追加せず、採用した復旧キー方式を実装した。既存通常パスワードはすでにArgon2idであり、移行時の再ハッシュ・初期化を行わない。

| 検証 | 結果 |
|---|---|
| React / Vitest | 30ファイル・236件合格。復旧の画面遷移、照合後フォーカス、確認不一致、期限切れ、二重送信抑止、一度だけのキー表示、保管確認、初回スキップ、一般利用者の案内対象外を追加確認 |
| 型検査・UIビルド | `npm run build`（tsc + Vite）合格 |
| DataCheck | 復旧の発行・本人再認証・通常権限拒否、再起動後の15分停止、5分期限、使い捨て、競合する確認の失効、保存失敗時巻戻し、新パスワード認証、再発行・変更・利用停止・復元時の失効、必須トリガー改変拒否を合成DBで確認 |
| 実ホストdispatcher | 対象利用者の偽装・型不正入力を拒否。復旧権限だけではタグ・設定を取得できず、確定後も通常ログインが必要 |
| 旧版バックアップ | LegacyBackupCheck 774項目合格。第1～8版の合成アーカイブ、既存FAQ・監査・認証・画像・設定保持、不正時の原本と現状保持 |
| 永続起動・移行 | ProductionCheck 93項目、RehearsalCheck 75項目合格。第7版更新前の実フルバックアップと第8版移行、バックアップ失敗時の非変更、既存未発行・通常パスワード保持を確認 |
| バックアップ確認・中断回復 | BackupConfirmationCheck 112項目、RestoreCrashCheck 351項目合格 |
| その他の関連回帰 | SecurityCheck、CodexCheck 72項目、RichCompatibilityCheck 271項目合格 |
| NSIS | 隔離したGUID環境の導入・更新・起動中拒否・アンインストール・データ保持など51項目合格。実アプリ起動・本番データ接続なし。最後の版番号表示修正後は再ビルド・パッケージ検査を実施（インストーラーの制御処理は変更なし） |
| 配布・文字コード | 許可リストに沿ったC# Release publish、ZIP、NSIS生成。UTF-8 BOMなし・LF、Git差分空白、実行時データ・配布禁止ファイル混入検査に合格 |
| 旧SQL・依存分離 | 第1～7版SQLと埋込リソースのバイト一致、C#通常ビルドの旧ソース非依存を確認 |

検証は明示生成した合成データとOS一時フォルダーだけで行った。実DB・実FAQ・実メールは開いていない。画面の自動試験はDOM・イベントの検証であり、対象端末でのWPF実画面、倍率、支援機能の総当たりを実施済みとはしない。通常の対象端末確認とGitHub Actionsの実行結果は別に追跡する。

更新文書：要件定義書1.57、基本設計書1.78、データ分離詳細設計書0.80、README、運用手順、C# README・正式切替ガイド、今後の課題、文書整理ガイド、GitHub公開記録、配布README。機能・画面・DB・認証・バックアップ仕様が変わるため設計書も同時更新した。

### 最終成果物

- インストーラー：`src-csharp/KnowledgeApp.CSharp/bin/pkg-d4f385ed1fd8/installer-a38795342f9c46d89f258192b013931b/KnowledgeApp-CSharp-0.6.0-setup.exe`
- インストーラーSHA-256：`5c2aad66cfa0fc859d0c8978729d18163c6b3ab1603982de4ffac66599a63a39`
- ZIP：`src-csharp/KnowledgeApp.CSharp/bin/pkg-d4f385ed1fd8/KnowledgeApp-CSharp-0.6.0-win-x64.zip`
- ZIP SHA-256：`4bbc9ff723fa3d940d2a0be734919bc70ca7453a163b8bb3294001ba5a37d091`

生成物はGit除外対象のビルド領域にあり、実利用者データを含まない。通常アプリのインストール・起動は行っていない。既存利用者は更新後、ログイン可能な間に「設定・情報」から自分の復旧キーを発行し、別の安全な場所に保管する。
