# 保存先設定・複数人共有 実装計画・記録

## 0. 0.7.0の実装結果（2026-09-06）

依頼を受けてC#本体・React画面・専用プラグイン・Windows共有サービスを実装した。以下の第1節以降は実装前に作成した計画を残したもので、現時点の仕様・差分・確認範囲は本節と正式3文書の冒頭を優先する。

| 段階 | 今回の到達点 |
|---|---|
| P：3役割 | admin/editor/viewer、旧user移行、公開FAQだけの閲覧、バックエンド権限、最後の管理者保護、利用者別履歴・表示設定を実装 |
| A：用途別保存先 | 端末のバックアップ/JSON/CSVの書出・取込初期フォルダー、共有サーバーのバックアップ退避先、標準復帰、Git拒否、確認プロセスの時間制限を実装 |
| B：Codex連携先 | 単独利用の親変更、専用領域・環境/世代・ACL・切替ロック、元履歴保持、プラグイン4コマンドを更新。共有では利用者別の固定交換領域とAPI連携を実装 |
| C：正本移設 | 任意追加段階のため未着手。DBのネットワーク直置きは引き続き禁止 |
| D：共有サービス | Windows 11用HTTPSサーバー、接続方式選択、セッション、版競合、再送照合、画像・転送の所有権、検証済みバックアップ、サービス登録スクリプト、配布ZIPを実装 |

### 実装判断と制限

- NASはI-O DATA製で型番不明。Windows PCを共有ホスト、NASをバックアップ先とする。サービスアカウント、証明書、LAN範囲は導入時に設定する。実機へ無断で登録しない。
- 編集者は公開まで可能、マスター・削除/復元・一括入出力は管理者のみ。新規アカウントの画面初期値は閲覧のみとした。
- Codexの共有端末側フォルダーは環境/利用者/世代で分離し、初回共有版では親変更を設けない。単独利用の任意親変更と共通化すると異なる共有先や端末間の未処理委譲を混在させるため、追加設計・試験の対象とする。CodexのNAS直接利用も同様。通常のJSON/CSV・バックアップの保存先変更は共有接続でも使用できる。
- 共有サーバーへの直接ファイルパス入力でDBや任意ファイルを取得できるAPIは設けない。取込は端末で選んだファイルをアップロードし、接続所有の番号でプレビュー・確認・実行する。
- 起動・復元前の安全退避と明示バックアップのサーバー内検証済みコピーはローカルに保持する。転送失敗時は部分ファイルを完成扱いせず、既存バックアップを変更しない。自動削除・定期バックアップは今回追加しない。
- 15秒は未開始の業務操作の待機上限、10秒は保存先の接続確認上限。実際のNASへの大容量コピー時間やOSのSMBタイムアウトを10秒に制限するものではない。

### 合成データによる確認

| 確認 | 結果 |
|---|---|
| TypeScript・React本番ビルド | 合格 |
| 画面試験 | 31ファイル・240項目合格（閲覧権限と保存先の成功/拒否/標準復帰を含む） |
| C# DataCheck | 認証・復旧・権限・FAQ・検索・画像・履歴・入出力・復元・Git境界を合成DBで合格 |
| 旧バックアップ移行 | LegacyBackupCheck 774項目合格。旧版1～8のID・ハッシュ・FAQ・画像・監査・履歴の保持を確認 |
| 本番起動の合成試験 | ProductionCheck 93項目合格。固定領域・排他・破損拒否・元データ保持を確認 |
| 共有機能 | SharedCheckの157項目と実際のHTTPS経路試験に合格。3役割・競合・再送・個人履歴・転送の所有権・復元後失効・Codex保存先を確認 |
| 50合成アカウント | 同時ログインと閲覧のサービス試験に合格。実データ量・実ネットワーク・実PCの性能保証ではない |
| 専用プラグイン | 設定先・ACL・世代・切替ロック・後からGit化された先の拒否を合成JSONで確認。C#との往復はPowerShell 7/5.1で117項目合格 |
| 復元中断回復 | RestoreCrashCheck 351項目合格。合成プロセスの強制終了後、元のDB・画像・設定・履歴を検証して復帰 |
| 配布検査 | パッケージ改変/混入等47項目、GUID隔離NSIS導入・更新・削除51項目合格。実アプリと実DBは未起動 |
| ソース境界 | 旧移行SQL7件のバイト一致、UTF-8/LF、Git差分検査、実データ混入検査に合格 |

### 導入時に残る確認

実NASの切断・権限・容量不足、別Windows端末からの接続、実アカウントでのサービス起動/再起動、証明書運用、実FAQ件数での応答性能、導入済みプラグイン更新後の明示FAQ往復は未実施。実際のFAQ・DB・パスワードを探索して代用しない。GitHubへのcommit/pushや実アプリ更新も行っていない。

利用者向けの具体的な手順は[共有サーバー導入手順](共有サーバー導入手順.md)。名称・exe名の日本語化は合意どおり別段階で計画する。


作成日：2026-09-06。状態：要求を受領し、現行C# 0.6.0のソースと正式3設計書を確認した計画案。今回はアプリ実装、設定変更、データ移動、共有先への接続を行わない。追加回答により、第二事業部の約50名が利用する可能性、管理者・FAQ編集者によるFAQ追加・編集と新設する「閲覧のみ」の3役割、常時起動PCまたはNASが利用可能という前提を確認した。実際の同時利用数、管理者数、ホスト機種・OSは未確定とする。

## 1. 方針と変更範囲

管理者が「設定・情報」から用途別のフォルダーと既定値を管理する機能を計画する。リポジトリ内部を許可する設定や例外スイッチは追加しない。実FAQ・DB・パスワード・復旧キー・認証ハッシュ・委譲内容は、公開/非公開を問わずGitHubを含むGitへ絶対にコミットしない。

「入出力ダイアログの既定フォルダー」「アプリが継続的に読み書きするCodex連携フォルダー」「DB・画像等の正本の移設」は異なる処理として実装する。既定フォルダーの変更では既存ファイルを移動しない。実データ移設が必要な場合は対象・件数・移設先・バックアップを確認する専用操作にする。

推奨順序はP（管理者・FAQ編集者・閲覧のみの3役割への変更）→A（バックアップ/CSV/JSON設定）→B（Codex連携先変更）→D（複数人共有）とする。C（ローカル正本移設）は必要な場合だけ独立して見積もり、共有化の前提作業にはしない。ネットワーク上のSQLiteファイルを各PCが直接開く仕様は推奨しない。

### 追加回答で確定した前提

| 項目 | 前提 |
|---|---|
| 利用規模 | 第二事業部の約50名が利用できる可能性を想定する。50名登録と50名同時利用は区別する |
| 権限 | 管理者、FAQ編集者、閲覧のみ（新設）の3役割。管理者とFAQ編集者が追加・編集可能 |
| ホスト | 常時起動PCまたはNASを利用可能。実機の型番・OS・性能は未確認 |
| 推奨初期構成 | 常時起動するWindows PCにC#共有サービス、DB・画像はそのPCのローカル、NASは検証済みバックアップの保管先 |
| DB方式 | SQLite継続を第一候補として負荷検証。50名という登録人数だけでサーバー用DBが必須とも、性能が十分とも断定しない |

### 管理者・FAQ編集者・閲覧のみの権限案

利用者の最新訂正により「管理者のみ編集・一般利用者閲覧」の2役割案を置き換える。役割は管理者、FAQ編集者、閲覧のみ（新設）の3つとする。以下の追加・編集/閲覧の区分は要求事項、公開・削除・マスター・一括操作の細分化は実装前に確定する推奨案として扱う。

| 操作 | 管理者 | FAQ編集者 | 閲覧のみ |
|---|---|---|---|
| 許可された公開FAQの検索・閲覧・画像参照 | 可 | 可 | 可 |
| FAQ追加・編集 | 可 | 可 | 不可 |
| FAQ複製・画像登録・既存分類/タグの選択 | 可 | 可 | 不可 |
| 下書き閲覧・公開状態変更 | 可 | 可とする案 | 不可 |
| FAQ削除・復元 | 可 | 初版は管理者に限定する案 | 不可 |
| 分類・タグ・同義語マスターの変更 | 可 | 初版は管理者に限定する案 | 不可 |
| Codexへの明示委譲・提案確認/反映 | 可 | 本人の権限内で可とする案 | 不可とする案 |
| CSV/JSON一括取込・書出、フルバックアップ/復元 | 可 | 管理者に限定する案 | 不可 |
| 利用者の追加・権限変更、保存先・システム設定 | 可 | 不可 | 不可 |
| 本人の表示設定・本人の履歴 | 可 | 可 | 可とする案 |

FAQ編集権限と利用者管理権限を分離し、編集のために全員へ管理者権限を渡す必要をなくす。閲覧のみでも通常操作に伴うサーバー側の履歴記録は発生し得るため、権限制御上の閲覧専用とDB接続の読取専用を混同しない。

現行`ArticleEditingService`の保存・複製・削除・復元・画像準備等は`RequireUser()`であり、現行版をそのまま配布しても新しい閲覧専用権限は成立しない。新しい`RequireEditor`等の用途別権限判定をC#へ追加し、UIにも同じ区分を反映する。`RequireUser()`で守る全APIを見直し、画像・取込・提案承認等を経由した更新や、非公開FAQのID指定取得も確認する。ボタンを隠すだけにはしない。

既存の管理者は管理者、既存の一般利用者（現在FAQ編集可能）はFAQ編集者へ引き継ぐ案を推奨する。内部役割値は`admin`・`editor`・`viewer`の案とし、既存`user`はマイグレーション時に`editor`へ対応付ける。利用者ID・認証ハッシュ・FAQ監査履歴は保持し、自動で管理者へ昇格したり全員を閲覧専用へ降格したりしない。新規利用者追加時の初期選択は「閲覧のみ」を推奨する。

旧DB/フルバックアップの役割変換、未知の役割値の拒否、最後の有効管理者保護を検証する。役割変更は管理者だけが行い、変更前の通常・復旧セッションを失効させる。FAQ編集者・閲覧者のパスワード紛失は管理者へ依頼し、管理者用復旧キーの使用権限を広げない。DBの役割制約と参照関係も変わるため、安全バックアップ付きマイグレーションを必要なDB改版として計画する。

## 2. 現行実装で確認した点

| 対象 | 現行 | 改修への影響 |
|---|---|---|
| 通常保存先 | `ProductionDataRoot`、`KnowledgeDatabase.Production`がC#専用LocalApplicationDataへ固定 | 文字列変更だけでDB移設を実装できない。初期化・起動前退避・中断回復・排他を含む |
| DB利用 | 本番はアプリ生存中にSQLite排他接続を保持 | 現行アプリを複数PCから同じDBへ接続しても共有機能にはならない |
| バックアップ | 任意の選択先へフルバックアップ。`settings/backup.json`に直近成功先を記録 | 明示的な既定先と「前回使用先」を分離する |
| CSV/JSON | WPFの保存・選択ダイアログ。固定の複合拡張子と用途別API | 書出先・取込時の初期表示先を追加する。取込処理は自動化しない |
| Codex | `CodexProposalFiles`と`SettingsService`がDB由来の固定rootを参照 | 提案箱だけでなく分類カタログ・FAQ委譲・メール委譲も整合させる |
| プラグイン | 4本のPowerShellスクリプトが固定のC# rootを独立に計算 | アプリとスクリプトを同じ設定契約へ更新する。導入済みプラグインの旧版は自動で変わらない |
| パス境界 | 固定正本、絶対パス、Git配下拒否、再解析点拒否、用途別のローカル/UNC制約 | DB用のローカル制限を一括解除せず、用途ごとに境界を追加する |

実利用者のDB・FAQ・委譲ファイル・設定ファイルは読んでいない。上記はソースコードの調査結果である。

## 3. 設定項目の提案

| 項目 | 変更内容 | ネットワーク先 | 適用方法 |
|---|---|---|---|
| フルバックアップ | 明示的な既定保存フォルダー | UNC・割当ドライブを許可する案 | 次回ダイアログから適用。保存先と上書きは引き続き確認 |
| バックアップ復元 | ファイル選択時の既定フォルダー | 許可する案 | 選んだ1ファイルだけ検査・確認後復元 |
| JSON書出 / 取込 | それぞれ別の既定フォルダー | 許可する案 | 書出/取込は明示操作のまま |
| CSV書出 / 取込 | それぞれ別の既定フォルダー | 許可する案 | CSV全行プレビュー・取込前退避を維持 |
| Codex連携 | 連携用親フォルダーを変更。配下の用途別サブフォルダー構成は固定 | 初期実装はローカルを推奨。共有先対応は別の互換・障害試験が必要 | 全連携先を一括切替。未処理提案の扱いを確認 |
| DB・画像等の正本 | 必要なら別のローカルドライブへ一体移設 | DBのネットワーク直置きは許可しない案 | バックアップ付き移設、再起動、検証後の確定 |
| 起動前/移行前/復元前の安全退避、一時・中断記録 | 当面ローカル管理領域を維持 | ネットワークだけへ移さない | 外部バックアップ先の設定から独立 |
| 外部資料・メール原本 | 今回の「保存先」設定の対象外 | 既存の利用方法を維持 | 資料本体の自動探索・移動を追加しない |

Codexの各フォルダーを個別に変更することも技術上は可能だが、提案箱と委譲元の食い違いを避けるため、最初は連携用親フォルダーを1つ選ぶ方式を推奨する。共有フォルダーを選ぶだけで複数人のCodex提案を安全に共用できる扱いにはしない。

## 4. 設定画面・既定値・障害時の動作

- 管理者専用の「保存先」欄を設け、用途、現在の設定、実際に使用するパス、標準/カスタムの区別を表示する。
- 各項目に「フォルダーを選ぶ」「接続・権限を確認」「標準に戻す」を用意する。入力文字列もC#側で同じ検査を行う。
- FAQ編集者・閲覧のみの利用者は変更できない。既存の利用権限を広げず、必要な利用者へは有効な連携先を表示する。設定の範囲はまず現在のWindows利用者が使用するアプリ環境とする。
- 「既定先」と「前回使った場所」を別管理し、明示設定した既定先を保存操作のたびに勝手に変更しない。優先順はカスタム既定先→従来の正常な前回成功先（移行互換）→安全な標準先とする。標準に戻した項目は過去の前回先へ戻さない。この優先順は未設定時の規則であり、設定済みの場所が使用不能な場合の自動フォールバックを意味しない。
- 標準候補はC#のローカル管理領域内の用途別入出力フォルダー。作成は明示的な設定保存・利用時に行い、起動しただけでネットワークへアクセスしない。既存設定の未設定値は互換的に扱う。
- 接続確認は選択されたフォルダーだけを対象とし、容量と読取/書込権限を用途別に検査する。書込確認用ファイルはアプリ専用のランダム名で作り、そのファイルだけ除去する。フォルダー全体を列挙しない。
- ネットワーク先が切断・未接続の場合は日本語で原因と対処を示す。勝手に別の場所へ保存したり、空のデータ環境を作って成功扱いにしたりしない。別の安全な場所を利用者が選ぶ操作は残す。
- 接続確認の待ち時間に上限を設け、UIを固めない。同期ファイルI/Oを単にTask化して「キャンセル済み」とするだけでは実I/O停止を保証できないため、ネットワーク検査を隔離する方式も試験する。
- 設定は固定のローカル設定領域へ版付きで保存する案とする。DB移設先を知るために移設先DBを先に開く循環を避ける。設定確定は一時ファイルと置換で行い、失敗時に元設定を保つ。
- バックアップを別PCへ復元しても絶対パスを自動で有効化しない。端末依存パスと共有DB側の業務設定を区別し、復元で含まれる旧`settings/backup.json`も再検査対象にする。Windows利用者間で設定を自動配布しない。

## 5. Git混入防止とファイル境界

1. リポジトリ配下は全用途で拒否する。現在のリポジトリだけでなく、祖先に`.git`ディレクトリまたはファイルがある場所も拒否し、worktree・別リポジトリを含める。
2. 設定を保存する時と実際に読み書きする直前の両方で再検査する。後からGit初期化された場所、リンク/ジャンクション差替え、相対パス、デバイスパス、代替データストリーム等による迂回を許さない。
3. Git判定や権限確認ができない場所は許可扱いにしない。ネットワーク先、割当ドライブ、UNCの別名表現で同じ安全性が保たれるか試験する。
4. 出力先にDB、添付、復旧記録、プログラム領域等を指定して、既存管理データを上書きできる設定は拒否する。重複・包含関係と用途間の衝突を検査する。
5. Codex/DB移設先はアプリ専用の空サブフォルダーか、正しく一致を確認できる既存領域に限る。既存の他環境を無断マージ・初期化しない。
6. ネットワークの資格情報・共有パス中の秘密をアプリ設定へ保存しない。アクセス権はWindows側で管理する。Codexへは秘密やDB参照先を渡す設定を追加しない。
7. `.gitignore`、コミット前・CI・リリース検査を維持する。利用者が手動で別リポジトリへコピーして強制追加する操作までアプリだけで完全防止できるとは説明しない。

## 6. Codex連携先の切替

変更対象は分類カタログ、FAQ委譲、確認待ち提案、必要なメール委譲の専用JSONとする。メール原本、添付本体、SQLite、未委譲FAQ、外部資料は対象外のまま。

- C#側に用途限定の保存先解決サービスを用意し、`CodexProposalFiles`、`SettingsService`、メール委譲と4本のプラグインスクリプトが同じ版付き設定契約を参照する。
- プラグインが読む設定には連携先・環境識別ID・設定世代等の必要最小限だけを公開する。認証情報やDB内容は含めない。設定ファイルの場所自体はローカル固定とし、任意の環境変数やCLI引数で読取先を拡大しない。
- C#とPowerShellの双方で境界検査する。「設定ファイルに書いてある」だけで読取/書込先を信頼しない。既存環境との取り違えは識別ID等で拒否し、設定のWindowsアクセス権も確認する。
- 変更前に対象パスと未処理提案・委譲の有無を表示する。初版では未処理のやり取りがある間の切替を止め、利用者が完了・明示整理してから再実行する案を推奨する。既存連携ファイルの移設を必要とする場合は別の確認付き処理にする。
- 設定世代を使い、切替前の依頼が旧フォルダーへ遅れて書き込まれることを防ぐ。進行中処理を終えてから変更し、提出直前も世代を確認する。旧プラグインが接続を継続する場合は明確に拒否・案内する。
- 未処理がない場合も履歴を捨てない。DBの提案履歴と再検討に必要なファイル参照が切替後も利用できることを確認し、必要な移設は明示確認付きで行う。
- 新規提案の確認待ち、FAQ修正の版競合、統合の非破壊、却下履歴、委譲番号限定、サイズ/深さ制限を保つ。場所の変更をCodexへの送信許可に読み替えない。
- 旧連携先の自動探索、両方を同時に読むフォールバック、名前が同じ提案の自動統合を行わない。ローカル設定ファイルを読めないときも旧Tauri領域へ接続しない。

## 7. DB保存先と複数人共有

### ネットワークドライブへのSQLite直置き

ファイルを配置できることと、複数PCから安全に運用できることは異なる。SQLite公式はネットワークファイルシステムの同期・ロックの不確実性を説明し、DBとそれを操作する処理を同じマシンに置く方式を示している。[SQLite公式のネットワーク利用指針](https://www.sqlite.org/useovernet.html)

WAL方式は別マシン間の共有メモリを使えないため、ネットワークファイルシステム上での複数ホスト利用に対応しない。ジャーナル設定やアプリの排他を外すだけでは安全な共有化にならない。[SQLite公式のWAL制約](https://www.sqlite.org/wal.html)

現行アプリは閲覧だけでも最終ログインや検索・閲覧履歴等を更新するため、利用者が「見るだけ」のつもりでも読取専用DBとは限らない。さらにDBファイルを直接読める共有権限を配ると、アプリのログイン・役割判定を経由せず、FAQや認証ハッシュへアクセスできる。直置きは今回の推奨案に含めない。

### 推奨する共有構成

各PCのKnowledgeApp → HTTPS → 常時稼働するC#共有サービス → そのホストのローカルDB・画像

約50名を管理者・FAQ編集者・閲覧のみの3役割で利用する前提から、C#共有サービスだけがホスト内のSQLiteを扱う構成を第一候補にする。編集者の人数と同時更新量は未確定であり、これは構成案であって性能保証ではない。接続PCへDBファイルの共有アクセス権は与えない。SQLite公式にも、DBと同じマシンに読み書きを仲介する処理を置く案がある。[SQLite公式の仲介方式](https://www.sqlite.org/useovernet.html)

同時更新量・規模・運用要件に応じてサーバー用DBも比較する。SQLiteはDBファイルごとに同時書込が1つという制約があるため、単純に人数だけで上限を断定しない。SQLiteから別DBへ切り替える場合、日本語検索・FTS5・SQL・移行・バックアップも再設計する。[SQLite公式の適用範囲](https://www.sqlite.org/whentouse.html)

共有化はパス設定機能より大きな改修となり、最低でも次を含む。

- 共通の業務処理をC#サービスから呼ぶAPI。任意SQL、任意シェル、任意ファイル操作を公開しない。
- サーバーでのログイン・利用停止・役割判定、端末ごとのセッション、TLSと接続先の確認。現行`AuthenticationService`の単一`_currentUser`を共有サーバー全員の状態として再利用せず、要求ごとに認証した利用者を解決し、通常・復旧セッションを分離する。現在の空パスワード初期値は共有利用開始前のセットアップで見直す。復旧キー・試行制限・失効もサーバー側へ集約する。
- FAQの同時編集では版番号等で競合を検出し、後から保存した人が黙って他人の編集を消さない。
- 画像、検索索引、利用者、監査、バックアップ、復元、更新処理の一元管理。復元・移行は全クライアントの更新を止めて行い、全セッションを失効させる。
- 通信断時の未保存入力保持と重複送信防止。切断時にローカル別DBへ自動で書き始めない。オフライン双方向同期は初版から外す案とする。
- Codex委譲・提案は利用者と共有環境を識別して管理し、共有フォルダー全体を全利用者へ公開しない。個々の利用者の明示委譲・承認権限をサーバーでも検査する。
- 接続先IDと端末内パスを分離する。利用者が指定するのは共有サービスへの接続先であり、各PCから見たDBのファイルパスではない。
- 常時稼働ホストの運用、停止時対応、バックアップ保管先、更新手順とデータ保持を決める。ホストが停止すれば共有利用も止まることを説明する。

今回とは別の運用で、常時稼働ホストを用意できず、最新版への即時反映が不要なら、管理者が明示発行した閲覧用スナップショットを各PCのローカルへ取り込む案を比較する。これには公開対象だけを抽出する専用形式・閲覧専用モードが必要で、認証ハッシュ入りフルバックアップを全利用者へ配る方式は採らない。

### 常時起動PCとNASの使い分け

初版はWindows PCでASP.NET Core共有サービスをWindowsサービスとして動かす構成を推奨する。利用者がデスクトップアプリを開き続けなくても動作する構成で、専用のサービス実行アカウント、起動設定、スリープ抑止、更新再起動、TLS、障害後の復元手順を決める。実行物とサービス用データを分け、利用者のデスクトップ用LocalApplicationDataをサービスの正本として流用しない。共有環境への移行は選択したフルバックアップと管理者確認で行う。[MicrosoftのWindowsサービスでのホスト方法](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/windows-service?view=aspnetcore-10.0)

NASはこの構成のバックアップ保存先にできる。保存先のWindows/共有権限はサービス実行アカウントで検証し、管理者がログイン中に割り当てたドライブ文字だけをサービスのアクセス根拠にしない。DB本体をPCからNASのSMB共有へ開く構成には戻さない。検証済みバックアップのNAS転送が失敗した場合はローカル退避を保ち、転送失敗を明示する。自動転送・保持世代は追加提案であり、保存先指定だけで実装済みにはしない。

NAS単独でホストする案は型番次第である。コンテナ/仮想マシンまたは必要な.NET実行環境、CPUアーキテクチャ、メモリ、永続ボリュームのファイルシステム、アクセス権、TLS、更新と再起動の仕組みを確認する。単なる共有フォルダー機能だけではC#共有サービスを実行できない。現在のWPF/WebView2アプリをNASへコピーして動かす方式ではなく、UIから分離したサーバーの配布物が必要になる。NAS上でSQLiteを動かす場合もサービスとDBを同じホストのローカルファイルシステムに置き、他PCはAPI経由にする。NAS自体にDBを置く場合のバックアップは別障害領域へも退避する。

### 50名利用を想定する追加試験と導入案

- 50アカウント・最大50同時セッションを合成条件として、一斉ログイン、検索、画像閲覧、履歴記録を確認する。登録数・実際の同時利用数を混同せず、応答時間、待ち行列、メモリ、タイムアウトを測る。実FAQ件数と画像容量の想定、目標応答時間は詳細設計時に決める。
- 複数の管理者・FAQ編集者が同じFAQを同時編集し、片方の保存後に他方の古い内容で上書きできないことを確認する。閲覧のみの利用者から更新・画像・取込・承認・管理APIを直接呼んでも拒否し、下書き等をID指定で取得できないことを確認する。
- 閲覧利用でも発生する履歴書込を含めてSQLiteの競合を測る。現行の全処理ロックや接続方式をそのまま50人の性能保証に使わず、長時間I/O・重い認証処理・バックアップが他利用者を無制限に止めない構成を検討する。
- 利用者間のセッション/復旧トークン/履歴/提案混在、利用停止後の拒否、復元時の全セッション失効を検証する。バックアップ転送中のNAS切断、共有ホストの再起動も合成環境で試す。
- まず管理者・FAQ編集者・少数の閲覧利用者で試行し、その後50名規模へ広げる案とする。WPFクライアントを継続するのが基本案だが、配布負担を減らす閲覧用ブラウザー画面も追加候補になる。ブラウザー対応は認証・API・ダウンロード・ローカル機能との切分けを伴う別改修で、現行UIを公開するだけで完成とはしない。

## 8. ローカル正本を移設する場合の追加設計

DBだけを切り離さず、画像・設定・退避・復旧記録等との対応を保つ。現DBへ管理者ログイン→移設先検査→フルバックアップ→新規専用領域へ安全なスナップショットと管理ファイルを複製→整合確認→次回起動先を確定→再起動して再認証、の手順を案とする。

単純な使用中DB/WAL/SHMのファイルコピーは行わない。移設中断記録と再実行可能な切戻しを持たせる。失敗時は元の正本を維持し、移設完了後も元データの自動削除はしない。デフォルトへの復帰も同じ移設処理とし、旧場所の古いDBをそのまま開いて「データが消えた」状態を生まない。OneDrive等の同期フォルダー、割当ネットワークドライブ、UNCを稼働DBの置き先へしない案とする。

## 9. 実装段階と完了条件

| 段階 | 実装対象 | 主な確認 |
|---|---|---|
| P | 管理者・FAQ編集者・閲覧のみの3役割 | UIとC#双方の権限判定、取込・画像・Codex経由の更新迂回、旧user→editor移行、既存ID/認証/監査保持、公開対象だけの閲覧 |
| A | 用途別保存先サービス・管理者設定・バックアップ/JSON/CSV既定フォルダー | 権限、標準復帰、既定/前回先の区別、再起動、取込非自動化、Git拒否、ネットワーク障害 |
| B | Codex親フォルダー切替と4本のプラグイン更新 | 同じ設定世代、未処理/進行中拒否、旧プラグイン、不一致、提案承認前非変更、メール原本非読取 |
| C（任意） | ローカル正本の移設 | 全管理データ保持、移設失敗・途中停止、再起動、再認証、デフォルトへ戻す、旧データ非削除 |
| D（別の構成変更） | C#共有サービス、接続設定、サーバー側認証・競合・画像・バックアップ | 複数端末、同時編集、通信断、権限偽装、復元/移行中停止、全端末セッション失効 |

各段階で正式3設計書、運用手順、プラグイン説明、配布方法を同期する。DB版は必要なDB構造変更が決まった時点で採番し、パス設定だけを理由に今のDB版を変更しない。工数・リリース版は確認した約50名・管理者/FAQ編集者/閲覧のみの前提と、実機情報・P/A/B/Dの詳細設計を基に見積もる。

試験は合成データで実施する。正常系に加え、`.git`ファイル/ディレクトリ、別Git、Gitの後付け、リンク差替え、用途間重複、別環境の既存データ、権限不足、容量不足、未接続ドライブ、途中切断、ファイル確定失敗、設定破損、旧設定移行、別PCバックアップ復元を確認する。型検査・単体/統合/画面テスト・ビルド・UTF-8/LF・Git混入検査を完了条件とする。

## 10. 避ける案・追加提案・未確定事項

避ける案：ネットワークSQLite直置き、稼働DBのクラウド同期、Git内への例外許可、連携先だけを片側で変更、設定変更に伴う無確認の一括移動/削除、ネットワーク切断時の別DBへの自動切替、任意場所の再帰探索、全利用者へのフルDB配布、初回からのオフライン双方向同期。

追加提案：標準に戻す、選択先だけの接続テスト、用途と有効パスの表示、バックアップと取込の別既定、端末依存設定の復元時再確認、Codex設定世代の一致検査、同時編集の競合通知。

約50名の利用可能性、管理者・FAQ編集者・閲覧のみの3役割、常時起動PCまたはNASの利用可能性は確認済み。残るのは実際の同時利用数、管理者・FAQ編集者数、FAQ/画像の規模、PC/NASの型番・OS、既存IDの統合方法、公開対象FAQの範囲、停止許容時間、NAS単独運用やブラウザー閲覧を採用するかである。未回答事項を性能保証や採用済み仕様へ読み替えない。
