# KnowledgeApp 今後の課題

2026-09-23：利用者の依頼に基づき、0.7.3までの変更をコミット・pushするための確認を実施した。詳細は本書9節。

2026-09-20：`Start-App.bat` と隣接 `KnowledgeApp-builds` の補助入口を追加し、共通ホームへ既存の検証済み0.7.3単一exeを1件登録する。SHA-256一致を確認し、`Test-CSharpStandaloneExe.ps1` と既存 `KnowledgeApp.StartupBenchmark.dll` による隔離合成DBのログイン表示・同梱依存・保存境界検査に成功（初回21,290ms、キャッシュ時2,046ms、通常の起動時間とは別）。実FAQ・本番DBを開かず、旧インストールは変更していない。要件・基本設計・READMEを更新。アプリとデータ境界は未変更のため製品再ビルドは不要。実FAQでの利用・共有サーバー接続は今回未検査。

2026-09-19：FAQタブが少ない場合の大きな間隔を修正した（C# 0.7.3）。タブ本体だけに適用していた260px上限を配置列にも適用し、左から6px間隔で並べる。件数が多い場合の均等縮小は維持する。詳細は本書8節。

2026-09-19：利用者から起動が速くなったことを確認済みとの報告を受領した。単一exeを通常配布へ統一し、`npm run build:exe`で最新UI・C#ビルドから配布exeの合成ログイン検査まで完結する入口を追加した。配布手順の改修であり、アプリは0.7.2を維持する。詳細は本書7節。

2026-09-16：起動改善と単一exe配布をC# 0.7.2として実装した。合成計測の中央値は旧Tauri 0.415秒、C#改善前1.151秒、改善後1.000秒（13.1%短縮）。保存仕様と安全バックアップを維持し、正式3文書を同時更新した。詳細・確認範囲は本書6節。約200項目の全手動回帰は今回一律には求めない。

2026-09-06：0.7.0の起動時白画面・異常終了は、WebView2の二重完了例外を特定して0.7.1で修正。実WebView2回帰試験を追加した。[修正・検証記録](起動時白画面修正_0.7.1.md)を参照する。利用者による修正版への更新・通常起動確認は別途確認する。

更新日：2026-09-23。C#正式化後の進行管理を本書へ集約する。要件の正本は[要件定義書](FAQシステム要件定義書.md)、実現方法の正本は[基本設計書](FAQシステム基本設計書.md)と[データ分離詳細設計書](ローカルFAQデータ・Git除外詳細設計書.md)であり、本書で要件を縮小しない。

## 1. 現在の確定状態

- **C#が正式版・今後の開発主系（FAQタブ間隔修正版0.7.3）**。起動改善・単一exe配布と安全境界を維持する。以前のPC切替は利用者の復元・確認報告により完了している。各更新版の対象PC受入は合成試験と区別する。
- 認証・権限、分類・検索、FAQ表示・編集、画像、履歴、CSV/JSON、バックアップ復元、タグ、タブ、メールの明示操作などの段階試験は受入済み。既存合格を取り消したり、最初からの全手動再試験を求めたりしない。
- FAQが空になったとの報告は、利用者による再復元後、再起動・FAQ追加とも正常との報告を受領し、**復旧済み・原因未特定として終了**する。再発時だけ調査を再開し、原因を参照先違いなどと断定しない。Codexが実DBや全FAQを検査した結果ではない。
- 旧Tauriソースの外部保管とC#通常ビルドの依存分離は完了。[外部保管実施記録](旧Tauri版_外部保管実施記録.md)を参照する。
- 正式利用の承認と、対象PC・実プラグインなどの全環境受入完了は別である。古い段階の「試作」「切替禁止」「全ゲート待ち」を現在の起動手順として使わない。

現行の起動・ビルド・配布手順は[README](README.md)、通常利用は[運用手順](KnowledgeApp運用手順.md)、切替の根拠・試験範囲は[第17段階記録](src-csharp/第17段階_CSharp正式切替記録.md)と[最終移行自動検証記録](src-csharp/最終移行自動検証記録.md)を参照する。

## 2. 未確認事項と再開条件

下表は既存の残件を集約したもので、実施日や次の実装順を新たに確約するものではない。重大な保存不能・データ不整合・権限迂回・未承認送信が判明した場合は、通常の改善より優先して修正し、関連範囲を再試験する。

| ID | 項目・現在の扱い | 再開条件／必要な確認 | 根拠 |
|---|---|---|---|
| ENV-01 | 切替済みPCとは別の利用端末のC#切替は未報告 | そのPCで使い始める前に、対象版・取得元、.NET/WebView2、実行権限・保護ソフト、C#専用保存先、通常認証・必要な権限、保存・再起動・ローカルバックアップ/復元を短く確認する。今回のPCの合格を転用しない | 要件1.17・14.4、第17段階記録 |
| CODEX-01 | 実会話でのC#向けFAQ往復・導入済みプラグイン接続は未確認 | 利用者の明示依頼時、合成FAQの新規・修正と合成メール委譲で、意図した提案箱への到達、承認前非変更、承認後反映、重複防止を確認する。リポジトリの専用スキル/コマンド試験と登録済みプラグインの確認を区別する。無断再登録・旧root読取で回避しない | 最終計画F6、第17段階記録 |
| REL-01 | 整理後のGitHub Actionsは未確認 | 利用者のpush指示後に通常C# CIと必要時の手動旧版互換CIを確認する。ローカルのクリーンビルド・NSIS試験は合格済みだが、GitHub実行済みとはしない | 外部保管実施記録 |
| REL-02 | 広範な実機の更新・削除・再導入・別PC復元は未確認 | 版更新、PC交換、複数端末利用・正式配布の前に、対象環境で保存データ保持と単一起動を確認する。合成隔離NSISライフサイクル51件と今回のPCの通常導入・復元は合格済み | 要件14.4、第17段階記録 |
| UI-01 | 未使用の画面幅・倍率・テーマ・支援機能の総当たりは延期 | 表示倍率変更、支援機能利用、利用者追加、表示不具合の際に、1280×720、900px以下、100/125/150%、緑/青、キーボード・フォーカス・読み上げの必要な組合せを確認する。保存・復元・エラーを読めない不具合は延期しない | 要件14.4、最終計画F5 |
| PERF-01 | 大規模C#性能・実画面応答の確認は後続 | 大量取込前、応答性問題、FAQ/分類の採番値9万件接近時に、合成10,000件・分類1,000件、CSV・大容量バックアップ、6桁管理ID表示を確認する。旧Rustの計測値をC#の保証値にしない | 要件14.4、旧MVP表・性能記録 |
| OPS-01 | ネットワーク/UNCバックアップの実環境障害試験は延期 | 共有先を使う前に、許可された合成試験で書込・切断・権限不足・容量不足時の保護を確認する。SQLiteはローカル固定、共有先はバックアップ用だけとする | 要件14.4、旧リリース前計画 |
| OPS-02 | 実環境の資料・実メールの使用許可と参照確認は別確認 | 実FAQへの参照登録・実メール委譲前に利用環境の規定を確認する。パスはコピー専用、WebはHTTP/HTTPS確認後にブラウザーへ委譲する。Codexが未委譲FAQ・原本メール・外部資料を探索しない | 要件14.4、AGENTS.md |
| SEC-01 | 復旧キー方式をC# 0.6.0へ実装・固定緊急認証廃止 | 合成データで発行、再設定、期限・入力停止、失効、DB第8版移行、バックアップ復元を検証。実環境へ更新後、ログイン可能な間に管理者がキーを発行・別保管する。実環境の更新・キー保管は利用者の操作として残る | [復旧キー実装記録](管理者パスワード復旧_実装計画.md) |
| SEC-02 | コード署名、公開範囲、正式配布経路・運用上の承認は条件付き延期 | 利用者追加、正式配布、機密情報を含む運用、利用環境の規定の要求時に判断を更新する。ハッシュ照合は署名・配布承認の代替ではない | 要件14.4、GitHub公開記録 |
| SEC-03 | 最新オンライン依存脆弱性監査は未確認 | オンライン監査を実施できる環境で確認し、依存更新・次回配布時に結果を残す。既存キャッシュによる復元・ビルド成功を監査合格へ読み替えない | 第17段階記録 |

開発・試験のPowerShellは7を優先する。5.1向けの既存UTF-8フォールバックとプラグイン互換試験は維持する。対象PCでソースから検証する場合は実行環境とGitフックも確認するが、通常利用アプリに開発ツールを必須化しない。

## 3. 正式化後の改善・追加機能

| ID | 課題 | 現行仕様と着手時の判断 |
|---|---|---|
| IMP-01 | JSON衝突の詳細表示と取込方式 | FAQ/分類の管理ID衝突は現行どおり拒否する。衝突した値・対象の分かりやすい表示、新規/更新の扱い、上書き確認等を別途相談する。内部IDと管理IDの統一や管理IDだけでの自動上書きは未決定。合成JSON不備のC2修正・再試験は完了済みであり、製品不具合の未修正として扱わない |
| IMP-02 | 検索の完全一致化・順位調整 | タグ変更前後の共通部分一致はT2で利用者受容済み。自然文検索を維持し、完全一致の必要性、除外語・同義語・重みは利用者の相談後に調整する |
| IMP-03 | `.pst`対応 | 未着手の追加機能。合成PSTの互換性、性能、文字コード、破損時保護、利用条件・利用環境の規定を確認してから設計する。現在は`.msg`の明示1回走査・選択・マスク・委譲だけで、自動監視・添付読取へ範囲を広げない |
| IMP-04 | 16MiB超要求・極深JSONの対応拡張 | 必要性が生じた場合だけ再検討する。16MiBは画面→C#の1要求JSON全体のUTF-8サイズで、DB容量ではない。容量/深さ超過の安全拒否・理由表示・未保存入力保持は実装済みで維持する。Codex提案1MiB・委譲5MiB・全体深さ127の安全境界は自動的に撤廃しない。通常JSONの極深構造のRust逆方向互換も延期 |
| IMP-05 | 用途別フォルダー・既定先設定 | 0.7.0で実装。単独Codexの親変更と端末のバックアップ/JSON/CSV、共有サーバーの退避先を提供。実NAS確認はOPS-01。共有Codex交換領域の親変更は別設計。[実装記録](保存先設定・複数人共有_実装計画.md) |
| IMP-06 | 複数人共有 | 0.7.0でWindows 11 HTTPSサービス・3役割・版競合・利用者別セッション/履歴・転送・バックアップを実装。157項目とHTTPS合成試験合格。実PC/NAS導入、実負荷・証明書運用は未確認。[導入手順](共有サーバー導入手順.md) |

自動バックアップのスケジュール・保持世代、管理者向け分析、お気に入り・解決状況、権限細分化・承認フロー、複数人用DB、関連FAQの新規編集は将来候補であり、今回の整理で実装を開始・確約しない。関連FAQを再導入する際はタグとの役割や既存関係の保持を別途設計する。名称・アイコンや画像上限など要件末尾の調整候補も、現在の確定値を無断で変更しない。

## 4. 試験・記録の進め方

1. 変更に関係する自動試験・合成データの確認を先に実行する。開発の画面起動には必ず`--rehearsal`を使う。実DB、未委譲FAQ、メール原本を直接読取・変更しない。
2. 自動試験、実画面操作、利用者確認を区別して記録する。人間には自動化できない実環境の必要最小限だけ依頼する。
3. 個人用の手順・結果はMarkdownを基本とし、Excelを必須にしない。`%USERPROFILE%\Documents\KnowledgeApp_Test\outputs\{フェーズ名}`へ保存し、過去のExcel・不合格初回結果を上書きしない。
4. 合格の根拠・対象版・範囲・再開条件を残す。未実施を合格へ変えない。旧段階の計画値や古い起動コマンドは現在の手順に転用しない。

## 5. 元文書との対応

元の37文書は全文をリポジトリ外へコピー・SHA-256照合済み。2026-09-23のコミット前確認で、指定37件すべてが既存の削除差分になっており、外部保管した37件と削除前のGit原文がバイト一致することを確認した。保管先と個別一覧は[文書整理ガイド](文書整理ガイド.md)を参照する。

| 履歴文書 | 本書への引継ぎ |
|---|---|
| リリース前作業計画書・旧MVP要件実装対応表 | ENV/REL/UI/PERF/OPS/SECの未確認事項、延期理由と再開条件。旧版の全必須チェックを完了へ書き換えず、現行の根拠と区別 |
| 次期必須課題実装計画書 | C#・タブ・`.msg`受入完了、`.pst`未着手、JSON/検索改善、全環境の残確認 |
| 次期機能 利用者テスト手順 | 合格済みのタブ/認証/メール試験は履歴へ。手動試験の最小化、Markdown基本、個人結果の外部保存を本書4節へ |
| C#正式移行 最終テスト計画案 | F1～F5の実装・合成試験と今回PCの切替は第17段階記録へ。F6実会話、F7対象環境、未使用規模・UI組合せ等は本書2～3節へ |
| 旧Tauri版 リポジトリ外保管計画 | 保管実施は完了。実際の保存先・復旧基準は外部保管実施記録へ。GitHub Actions確認はREL-01へ |

今回の文書整理はアプリ、UI、DB、認証、保存先、インポート判定を変更しない。9月6日の整理時点ではGitHubへのpushは未実施。9月23日の依頼によるコミット前確認は本書9節に記録する。

## 6. 2026-09-16 起動改善と単一exe配布

### 変更と計測

WebView2のネイティブ初期化をDB準備と重ねた。DB・完全安全バックアップ・分類カタログとブラウザーがすべて準備できた後だけログイン画面へ遷移する。DBをバックグラウンドで生成する方式にはせず、起動中の失敗と終了で資源所有を維持する。画面資源31件は変更前後でパスとSHA-256が完全一致した。

| 対象 | 通常起動5回の中央値 | 範囲 |
|---|---:|---:|
| 旧Tauri 0.4.4・隔離Release再ビルド | 0.414898秒 | 0.402192～0.424458秒 |
| C# 0.7.1・改善前 | 1.150785秒 | 1.137317～1.222413秒 |
| C# 0.7.2・改善後 | 0.999871秒 | 0.993093～1.022036秒 |

各系列で空の合成DBを用意し、初回と暖機1回を除外した。通常表示したログインフォームの入力・送信ボタンが有効になり、2描画フレーム経過後の通知を終点とする。このタスクの重いビルド・試験は計測中に停止した。C#は実際のMainWindow初期化を使用するがApp.OnStartupの起動排他・インストール排他を省いた検証ホストなので、配布exe同士の厳密な比較とは区別する。旧版は永続ブラウザープロファイルを再利用し、C#は毎回独立した一時プロファイルと起動前バックアップを維持する。実FAQの件数・画像量による時間、PC再起動直後の時間は未測定。

前後7起動ずつでログイン準備とプロセス終了コード0を確認した。直後に閉じる試験で、ブラウザー一時領域の清掃警告が前後各1回あった。未解放状態の領域を強制削除しない既存動作であり、清掃成功とは扱わない。継続する場合は終了タイミング・ブラウザー解放を別途調査する。数値・警告を分けた原記録はリポジトリ外の`Documents/KnowledgeApp_Test/results/startup-20260916/`へ保存した。

### 配布

単一exeに.NET・UI・SQLite・自己完結PathProbe・ライセンス案内を含める。WebView2 Runtimeは対象PCに必要。exeの置き場所と展開キャッシュをどちらもデータ保存先から保護し、FAQ・認証・設定は従来の固定LocalApplicationDataを使う。DB第9版・バックアップ第1版は変更しない。

利用用ファイルはリポジトリ外の`Documents/KnowledgeApp_Releases/0.7.2/KnowledgeApp.CSharp.exe`（228,409,060バイト）。SHA-256は`e9d5da4bf958dfbdafbaf6887615ec724aaa9753657ab43ff87327c779d3850e`。同じ場所にREADMEとmanifest・ハッシュ照合用ファイルを保存する。導入済みアプリの更新、実DBの探索、GitHubへの公開は行っていない。

単一exeそのものを隔離先へコピーし、同梱.NETの使用、SQLiteメモリDB、補助exe、両アプリフォルダーの保護、保存済みCodex連携先の再検査、同梱UIの全ハッシュ、混入禁止、合成DBでのログイン画面を確認した。初回展開・補助exe確認・DB新規作成・ログイン・終了を含む最終検査全体は約16.3秒、キャッシュ再利用時約2.16秒だった。これらは純粋なログイン起動時間ではなく、上表へ混ぜない。初回の展開と保護ソフトの検査で待つ場合がある。結果は原記録フォルダーの`standalone-final-result.json`へ保存した。

### 検証と残確認

- React 240件、型検査・UIビルド、C# Releaseビルド（警告・エラー0）、実WebView2の表示・2回再読込・403拒否・終了、起動の正常/DB準備失敗/環境作成直後終了/DB準備中終了4経路が合格。
- C#の既存17検査プロジェクトが合格。Security157、Production93、RestoreCrash351、SingleInstance37、UiBundle13、InstallationGuard10、Rehearsal75、Codex72、LegacyBackup774、RichCompatibility271、BackupConfirmation112、Shared175（新規パス境界16件・標準連携先再読込2件を含む）、FinalInterop117とDataCheck/MailCheck/MigrationGateを確認した。UiBundleCheckは以前の共有対応で個別ソースの依存型が不足していたため、Dataプロジェクト参照へ修正し、検査本体を維持して再試験した。
- プラグインコマンド、移行SQL7件の同一性・旧ソース非依存、生成した合成本文のTiptap検査、配布パッケージの既存47回帰が合格（後片付け後の終了コード0）。文字コード・差分・実データ混入検査も合格。単一exe作成先のGit内拒否・既存出力上書き拒否も確認した。
- 対象PCの実行許可・WebView2・実データでの通常利用は未確認。今回は単一exeを提供し、NSISインストーラーの再作成・導入更新試験は対象外。GitHub Actions上の実行も未実施。
- 利用者の確認は、起動、ログイン、代表FAQ表示、保存後の再起動、バックアップの5点を中心にする。以前の約200項目の手動試験を今回の変更だけで一律再実施する必要はない。既存の別環境・実プラグイン等の未確認事項はそのまま追跡する。
- 後片付けで、検査失敗時に作成したTEMP直下の合成フォルダー3件の削除が自動承認審査から`rejected: blocked by policy`として拒否された。理由の詳細は非提示。再試行せず保持し、配布物・実データへの影響はない。対象の葉名は`knowledgeapp-data-check-d9409744-0022-40fa-a9bd-ad517980c468`、`knowledgeapp-data-check-9be1417f-fa09-4309-8ce8-b9a1f60069ff`、`knowledgeapp-data-check-317caad1-fe96-4223-944a-14fe4158e504`。いずれも本番・固定検証DBへ到達していない失敗検査の領域である。

## 7. 2026-09-19 単一exeを通常配布へ統一

### 変更

- 既存の単一exe生成を`npm run build:exe`へ接続した。最新UIをビルドし、C#の依存復元・Releaseビルド、単一exe生成、実exeの合成ログイン検査まで1コマンドで完了する。
- 出力先省略時はWindowsのドキュメントフォルダー内`KnowledgeApp_Releases/0.7.2/build-日時-識別子`へ新規生成する。リポジトリ内・既存先はビルド前に拒否し、配布対象は`standalone/KnowledgeApp.CSharp.exe`だけとする。
- Newスクリプトは検査用DLLの指定を受け、ログイン・配置先境界検査の結果をmanifestへ記録する。SHA256SUMS.txtを自動生成し、CIは同じ検査を重複実行せず1回で記録する。
- 要件1.62・基本設計1.83・データ詳細0.85、AGENTS、README、C# README、運用手順を更新。導入はexeコピー、更新は終了後のexe交換へ統一した。アプリの機能・データ保存先・DB第9版・バックアップ第1版は変更していない。

### 生成・検証

`npm run build:exe`を引数なしで実行し終了コード0。生成先は`Documents/KnowledgeApp_Releases/0.7.2/build-20260919-082211-ba0f6de3/standalone/KnowledgeApp.CSharp.exe`、228,409,060バイト、SHA-256は`b23ee63740e73d6644399f99b9c350a60c84b6fb239f7fdfc3bc25d0da486788`。同じ出力先のSTANDALONE-MANIFEST.jsonへ検査結果を保存した。9月16日の配布物は上書きしていない。

- UI型検査・ビルドとC# Releaseビルド（警告0・エラー0）が合格。
- 最終exeを隔離フォルダーへコピーし、初回展開・再利用の2回で同梱.NET・SQLite・補助exe・UIハッシュ・アプリ領域保護・既存Codex連携先の再検査・合成DBログイン表示と終了が合格。実DB・固定検証DBには接続していない。
- wrapperの構文検査、リポジトリ内・既存出力先の早期拒否2ケースが合格。
- 文字コード・Git差分・実データ混入の最終検査が合格。実ファイルのSHA-256とmanifestが一致し、別担当の読取レビューでもブロッカーは見つからなかった。

### 残確認

利用者からWindows 11の既存WebView2を使用する回答を受領し、その条件で確定した。WebView2本体の同梱は行っていない。別の対象PCでの実行許可・通常利用、GitHub Actionsでの実行は未確認。NSIS生成・導入は今回の対象外で、従来方式を維持する。配布手順だけの変更のため、前回合格したReact240件・C#全体・配布47件や約200項目の全手動試験は繰り返さない。ユーザー報告で起動改善は確認済みとし、別PCの通常利用まで合格と推定しない。

## 8. 2026-09-19 少数FAQタブの間隔調整（0.7.3）

### 変更と原因

タブ列は画面全幅を`1fr`で均等割りしていた一方、タブ本体には260px上限があり、2件など少数時に列内の未使用幅が大きなタブ間隔として残っていた。列も`minmax(0, 260px)`へそろえ、左寄せ・6px間隔を明示した。収まらない件数では列を均等に縮小する。最大10件、重複防止、11件目拒否、選択・閉じる・スクロール保持の処理は変更していない。

要件定義1.63（FR-VIEW-14・受入12.8）と基本設計1.84（FAQタブ）を更新した。データ詳細設計は、保存先・DB・外部送信・配布方式の変更がないため変更不要と確認した。README・運用手順と配布版の表示を0.7.3へそろえた。

### 検証

- `npm test -- src/app/FaqTabs.test.tsx src/app/AppShell.test.tsx`：2ファイル・7件合格。既存の列指定期待値だけを更新し、操作・上限・スクロール保持検査を維持した。
- 実際のFaqTabsProvider/FaqTabBarと製品CSSをローカルの合成ページで描画し、幅1100px・650pxの各領域で1・2・4・10件を確認した。隣接間隔は全ケース6px、少数時260px、多数時は均等縮小、横はみ出しなし、閉じるボタン幅30pxを維持した。スクリーンショットで視認性も確認した。
- 2件から2つ目を選択して閉じ、残ったタブが選択されることをブラウザーでも確認した。合成タイトルだけを使い、実FAQ・DBへアクセスしていない。検証用ブラウザーとローカルサーバーは終了した。
- `npm run build:exe`が終了コード0で完了。UI型検査・ビルド、C# Release（警告0・エラー0）、最終単一exeの同梱依存・合成ログイン・配置先保護検査が合格。最終差分・文字コード・実データ混入検査も合格した。今回の変更に約200項目の全手動回帰は求めない。

配布物は`Documents/KnowledgeApp_Releases/0.7.3/build-20260919-124950-4d58bcc7/standalone/KnowledgeApp.CSharp.exe`、228,409,060バイト。SHA-256は`e7c720773e9230002ec9b6da8e1fc6446724c6a7915901a59ecd7791598d38f0`。manifest・ハッシュ照合ファイルと実exeの一致、ProductVersion 0.7.3を確認した。利用者の既存exeは上書きしていない。利用者の実FAQ・別PCでの表示確認は未実施。

## 9. 2026-09-23 コミット・push前の確認

利用者の依頼に基づき、C#正式移行後の未送信2コミットと、復旧キー・共有機能・起動改善・単一exe配布・FAQタブ調整・文書整理までの変更を現在の`main`から送信する。`git fetch origin`後にリモート側だけの変更がないことを確認した。作成者は従来のGitHub非公開メール設定を維持する。

- 差分検査、UTF-8（BOMなし）・LF、利用者データ混入検査が合格。ビルド出力・配布exeはソースのコミット対象に含めない。
- 旧文書の削除37件は整理ガイドと完全一致し、外部保管した原文37件とGit原文のバイト一致を再確認した。残存Markdownのローカル相対リンク85件に参照切れはなかった。
- 今回は文書の時点差だけを修正し、アプリ設計は変更しない。正式3文書の既存変更と実装・配布手順の対応を確認した。6～8節および9月20日に記録した合成試験を再実行扱いにせず、約200項目の全手動回帰は追加しない。
- 整理後のGitHub Actionsの結果は、このコミット時点では未確認。pushで起動する品質ワークフローの実行状況と結果をGitHub上で確認する。別PC受入・実プラグイン往復など既存の未確認事項は維持する。

exe受取側のCodex連携は、各PCのWindows利用者に対応するローカル交換領域を使う設計であり、開発リポジトリを必須にしない。ただしFAQプラグインはexeに内蔵・自動登録されず別途導入が必要。配布先向けのプラグイン書出し・導入案内・接続確認をまとめる画面は未実装の改善候補とし、今回のコミット作業で追加しない。
