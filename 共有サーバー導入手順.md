# KnowledgeApp 0.7.0 共有サーバー導入手順

2026-09-06。Windows 11 x64の常時起動PCで共有サービスを動かし、DB・画像はそのPCのローカルへ保存する。I-O DATA製NASはバックアップ先として使う。NASの型番・SMB対応・実際の権限は未確認のため、NAS上でのサービス稼働やSQLite直接共有は行わない。

この手順は新しい共有環境を導入する管理者向け。実際のFAQ、DB、パスワード、復旧キー、証明書秘密鍵、NAS認証情報は公開・非公開を問わずGitへ絶対にコミットしない。導入スクリプトは配布物に含まれるが、開発作業では実行・サービス登録・実データ移行していない。

## 1. 用意するもの

- ホスト：Windows 11 x64、スリープしない運用、十分なローカル空き容量。ASP.NET Core Runtime 10 x64を利用環境の方針に従って導入する。
- 各利用端末：KnowledgeApp C# 0.7.0、.NET 10 Desktop Runtime x64、WebView2 Runtime。
- ホストの固定DNS名または固定IP。その名前に一致する、有効なサーバー認証用TLS証明書をホストの「ローカルコンピューター／個人」証明書ストアへ配置する。各端末が発行元を信頼できること。証明書エラーを無視する設定はない。
- 専用のWindowsサービス用アカウント。サービスとしてのログオン、アプリの読取・実行、証明書秘密鍵の読取、必要なNASフォルダーだけのアクセス権を付与する。普段の管理者アカウントを使い回さない。
- NASはUNCパスを推奨する。ドライブ文字の割当はサービスから見えないことがある。認証はWindows・組織のアカウント管理で設定し、アプリ設定にパスワードを書かない。旧NASに合わせてSMB1やゲスト認証を有効にしない。

## 2. 新規設置

1. 共有サーバーZIPを、Gitリポジトリ外のローカルフォルダー（例：`C:\Program Files\KnowledgeApp.Shared`）へ全体展開する。書換権限はWindows管理者だけ、サービスアカウントには読取・実行とする。exeだけを抜き出さない。
2. 管理者としてPowerShell 7を開き、配布物の`Install-KnowledgeAppShared.ps1`を実行する。`-InstallDirectory`、`-HostName`、`-CertificateThumbprint`、`-ServiceCredential (Get-Credential)`、必要なら`-Port`を指定する。引数へ平文パスワードを書かない。
3. コンソールの案内で初期管理者ID `0000`の新しいパスワードを2回入力する。共有環境は空欄パスワードで起動・ログインできない。スクリプトが返すURLとサーバーIDを控える。
4. 「証明書の管理」から秘密鍵の読取権限をサービス用アカウントへ与え、実行フォルダー・ログオン権限を確認する。TCPポート（既定7443）は必要なLAN/VPNの接続元範囲だけ許可する。インターネットへ直接公開しない。
5. `Start-Service KnowledgeApp.Shared`で開始する。登録スクリプトは確認前にサービスを開始したりファイアウォールを開けたりしない。サービスは自動起動で登録される。証明書・権限などで開始できないときはサービスを停止した状態で同じexeをコンソール起動し、起動エラーを確認する。
6. 各端末のログイン画面にある「接続方式」で「共有サーバーへ接続」を選び、URLとサーバーIDを入力して保存し、アプリを再起動する。保存前にTLSとサーバーIDが検査される。
7. 管理者でログインして復旧キーを発行し別保管する。利用者の管理で個別アカウントを作り、管理者・FAQ編集者・閲覧のみを選ぶ。新規作成の初期選択は閲覧のみ。

登録処理が途中で失敗した場合もデータを削除してやり直さない。`%ProgramData%\KnowledgeApp.Shared`の設定と初期化状態を確認し、必要な段階から再開する。初期管理者が設定済みなら`--initialize`は再設定を拒否する。サービスが未登録なら同じアカウントとexeで`New-Service`を使用して登録を完了する。秘密鍵・ファイアウォール・NASの設定内容は環境に依存するため、ネットワーク管理者と確認する。

## 3. 既存データの引継ぎ

1. 移行元の単独利用アプリで、管理者パスワードが空欄でないことを確認し、フルバックアップを作成する。元データとバックアップを保持する。
2. 共有接続の管理者で「設定・情報」からそのバックアップ1件を選び、プレビュー・確認後に復元する。DBファイルを手でNASへ置いたり、サーバーへコピーしたりしない。
3. 復元前のサーバー状態は自動で安全退避される。復元後は全接続が再ログインを要求され、復元したバックアップ内のID・パスワードを使用する。空欄パスワードしかない管理者データは置換前に拒否される。
4. 旧一般利用者はID・パスワードハッシュ・監査を保持したFAQ編集者になる。必要な利用者を閲覧のみに変更する。パスワードが空欄だった利用者には管理者が非空のパスワードを再設定する。旧履歴と表示設定は移行時点の先頭管理者へ引き継がれる。自動同期・双方向統合はしない。

## 4. 日常操作と保存先

| 対象 | 設定・保存場所 |
|---|---|
| DB・画像・安全退避 | ホストの`%ProgramData%\KnowledgeApp.Shared`。正本の任意移設は今回対象外 |
| 端末のJSON/CSV/バックアップ書出・取込初期フォルダー | 各端末の「保存先の設定」。設定変更だけでは既存ファイルを移動しない |
| サーバーからNASへのバックアップ | 共有管理者の「共有サーバーのバックアップ退避先」で指定。明示的なフルバックアップ作成時に転送。定期実行は未追加 |
| 設定の標準復帰 | 「標準に戻す」。未設定時だけ旧バックアップの直近成功先を優先。明示的な標準復帰後は旧保存先を使わない |
| 端末ごとの接続・保存先設定 | C#専用データルートの`device-settings`。フルバックアップから除外 |
| 単独利用のCodex連携 | 管理者が親フォルダーを変更可能。アプリが専用の子フォルダーを作成し環境・世代・Windows権限を検査する |
| 共有利用のCodex連携 | 端末の`shared-exchanges`に利用者・共有環境・世代別の専用交換領域を作る。現段階ではこの領域の親フォルダー変更は提供しない |

設定先が使えなくなっても別の場所へ黙って切り替えない。接続・権限確認は別プロセスで行い、10秒でタイムアウトする。実際の大容量転送はOSのネットワークI/O待ちが発生し得るため、保存結果が返るまでアプリを終了しない。

バックアップは検証済みのサーバー内アーカイブを保持した後にNASへ転送し、内容を再検証して完成名へ変更する。NASに失敗した場合は画面に警告を出し、サーバー内退避と端末へのダウンロードを利用できる。ダウンロードもサイズ・SHA-256を検証後に完成名へ確定し、途中ファイルで既存ファイルを上書きしない。安全退避は自動間引きしないため、空き容量を監視し、管理者が保存期間を決めて整理する。

## 5. 権限と同時編集

- 管理者：利用者・マスター・保存先・一括入出力・削除/復元・バックアップを管理する。最後の有効な管理者は停止・降格できない。
- FAQ編集者：FAQの追加・編集・公開・複製、明示的なCodex委譲と自分の提案の確認ができる。新分類が必要な場合は管理者へ分類の登録を依頼し、その分類を選んで提案を承認する。提案の確認操作も利用者別であり、他人のアカウントを共用しない。
- 閲覧のみ：公開済みかつ非表示・削除・統合されていないFAQと画像を閲覧できる。URL/APIを直接指定しても権限を越えて参照・変更できない。
- 表示設定と検索・閲覧履歴は利用者別。同じアカウントを複数人で共用しない。
- 同時編集で先に保存された場合、後から古い版を保存する操作は拒否される。入力を保持したまま最新内容を確認して編集し直す。自動上書き・自動結合はしない。
- セッションはメモリーだけに保持し、無操作30分（未認証2分）、最大8時間で失効する。役割・パスワード・有効状態の変更、復元、サービス再起動でも再ログインが必要。一般ログイン失敗は5回で15分停止、管理者復旧キーの試行制限はDBへ永続化する。
- 通信が切れた場合、同じ保存内容の再送は同じ処理番号で照合する。結果不明時は入力を残してFAQ・履歴を確認する。共有障害を理由にローカルDBへ自動接続しない。

## 6. Codex連携

更新したプラグインの4コマンドと`ExchangeLocation.Common.ps1`を同時に配布・更新する。リポジトリを修正しても導入済みプラグインは自動更新されない。旧プラグインは新しい保存先に対応しないため、進行中の会話を完了または停止してから切り替える。

単独利用で変更する場合、未処理提案があれば先に承認/却下を完了する。専用JSONの委譲履歴は新しい世代へコピーし、元フォルダーを残す。元の会話へ新しい環境番号を継ぎ足して古い依頼を再利用せず、必要な依頼をアプリから改めて発行する。

共有利用ではアプリが分類カタログと明示した委譲だけをHTTPSで交換する。CodexはDB・添付画像・メール原本を直接読まない。提案はアプリを開いて提案一覧を確認した時に送信され、承認前にFAQへ反映されない。利用者ごとに所有権を検査し、他人の委譲番号や提案番号で取得・承認できない。親フォルダーの自由変更、NASを介したCodex交換は共有版の初回実装には含めない。

## 7. 更新・停止・実機受入

更新は利用者へ保存・ログアウトを案内し、検証済みフルバックアップを確保してからサービスを停止する。新しい配布物を別フォルダーへ展開して検査し、サービスの実行パスを更新して再開する。`ProgramData`のデータを配布物で上書き・削除しない。サービス削除や端末アプリの削除でもFAQデータは別管理のまま保持する。障害時に古いexeだけへ戻すと新DB版を読めない場合があるため、正本を手作業で置換せず、対応版のアプリから選択バックアップを復元する。

導入前に合成FAQで、別PCのログイン・3役割・同時編集競合・画像表示・バックアップ復元・再起動を確認する。実NASで書込/読取、接続切断、容量不足、権限不足時に元データと既存バックアップが残ることも確認する。型番不明のNASへの互換性と50人の実機性能は、開発用の50合成アカウント試験だけでは保証しない。自動試験の記録は[保存先設定・複数人共有 実装計画](保存先設定・複数人共有_実装計画.md)を参照する。

参考：[MicrosoftのWindowsサービス運用手順](https://learn.microsoft.com/en-us/aspnet/core/host-and-deploy/windows-service?view=aspnetcore-10.0)、[KestrelのHTTPS設定](https://learn.microsoft.com/en-us/aspnet/core/fundamentals/servers/kestrel/endpoints?view=aspnetcore-10.0)。
