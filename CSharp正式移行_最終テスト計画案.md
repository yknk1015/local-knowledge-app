# C#正式移行：重大リスクに絞った最終テスト計画案

作成日：2026-09-05

採用・実装更新：2026-09-06。利用者がF1～F7と人間の確認最小化を承認済み。ファイル名は参照を維持するため「計画案」のままとする。

最新更新（第17段階）：利用者はFAQ約11件の小規模本番切替と、今後のC#開発主系化を明示承認した。旧Tauriとの共有rootを廃止し、C#専用rootへの明示バックアップ復元に変更する。下記の第16段階の保留判断は過去の記録とし、未実施試験そのものは未実施のまま残す。現在の実施手順は`src-csharp/CSharp正式切替ガイド.md`、自動検証は`src-csharp/第17段階_CSharp正式切替記録.md`を参照する。人間にはログイン・明示復元・件数と代表FAQ確認だけを基本として依頼する。

## 1. 結論と位置づけ

完了追記（2026-09-06）：利用者から自身による復元・確認完了の報告を受領した。今回のPCのC# 0.5.0への本番切替は完了し、今後はC#を正式版・開発主系とする。以下は計画策定から切替までの履歴であり、会社環境など未報告の確認を全合格とするものではない。結果は`src-csharp/第17段階_CSharp正式切替記録.md`を正とする。

16MiB超のFAQや極端な入れ子まで機能を拡張してから移行する必要はない。推奨する判定は「合意した通常利用範囲でUI・機能・データが非デグレであり、範囲外の入力も安全に拒否できること」である。既に合格した段階試験をすべて手動でやり直すのではなく、既存自動回帰を再利用し、最後の実行物で下記F1～F7を確認する。

この計画は採用済みであり、要件1.3(3)・1.15～1.16、基本設計4.6.12、詳細設計2.0.7へ反映した。計画採用は試験結果や本番切替の許可ではなく、未実施のゲートを合格へ書き換えない。既定起動は固定検証環境のままとし、実データの候補試験は別途明示手順・起動確認を経て行う。

第15段階D1～D4を含む既存の利用者合格は維持する。以下の「未確認」は、過去の合格を取り消す意味ではなく、本番向け接続や配布へ変更した最後の構成に関する確認である。個人記入用の結果票は従来どおりリポジトリ外に置く。

## 2. 現状から見つかった重要な点

以下は計画作成時点の調査記録。第16段階の実装・検証状況は末尾7節で追跡する。

| 種別 | 確認した事実・リスク | 移行前の扱い |
|---|---|---|
| 未実装 | C#は本番ではなく固定検証ルートを開く。管理ファイル境界も検証・合成ルートだけを許可する | 本番ルートと移行手順を安全に実装する必要がある。フォルダー名の変更や試作表示の削除だけでは正式化しない |
| 未実装 | 起動排他はC#試作用であり、Tauriとの共有排他ではない | 同じデータへの旧新版の同時書込みを防ぐ。別ルート方式なら、正本・同期しないこと・切戻しを明確にする |
| 未接続 | C#の委譲は検証ルート、現行プラグインは本番ルートを参照する | 正式な接続先をそろえ、無害な合成FAQで実往復を確認する |
| 未実装・未検証 | C#配布は試作用フォルダーとZIP。C#向けNSISの導入・更新・削除は未完成 | 採用する正式配布方式でデータ保持を確認する。現在のNSIS要件を採る限り、NSIS確認は必須 |
| コード上の課題 | 16MiB超要求はホストで無応答のまま拒否され、通常の保存要求では画面側の30秒タイムアウトになる | 上限拡張ではなく、安全なサイズ案内・入力保持・待機解除を補完して確認する。応答待ち期限を設けない処理にも無限待機を残さない |
| 未再現の重点リスク | 復元はDBの反映後に画像等のディレクトリを交換する。捕捉可能な例外の巻戻しはあるが、この途中でプロセスが停止した場合の再起動復旧は今回確認できていない | 合成環境で途中停止を試験する。不整合を正常状態として使い続けず、安全バックアップから確実に復旧できることを必須とする |

データ欠損が実機で発生したと断定するものではない。最後の行はコードレビューで見つけた確認優先度の高いリスクであり、故障注入による再現確認前である。通常の復元・例外処理についての既存合格とは区別する。

## 3. 最終テスト：必須7項目

各行は一つの試験群。細かな拒否条件は自動試験でまとめ、人間には最後の実機確認だけを依頼する。実施用候補版ができるまでは、現在の第15段階で本番接続や実プラグイン送信を試さない。

| ID | テスト項目・止めたい重大事故 | 合格条件 | 主な担当・現在の不足 |
|---|---|---|---|
| F1 | 本番起動・保存先・旧新版の排他：別DBを開く、既存DBを初期化する、二重書込みする事故 | 正式保存先で起動・再起動できる。既存データを初期化しない。旧新版・二重起動・終了処理中の起動で同じデータへ競合書込みしない。誤ルート・欠落・破損・リンク経由は元データを変えず止まる | Codex：本番向け実装と合成試験。利用者：最後の起動・二重起動確認。現状は試作専用のため実装が必要 |
| F2 | データ移行と旧版への切戻し：FAQ・画像・認証等の消失、戻せない状態 | 旧版の正規フルバックアップから本文・画像・分類・タグ・管理ID/内部ID・状態・履歴・設定・利用者/権限を保持する。既知の検索結果も比較する。切替直前のバックアップを保持し、C#で保存した通常のFAQと画像を含むバックアップの旧版復元も隔離環境で確認する。切戻し時点と、その後の更新の扱いを明記する | Codex：Rust/C#実処理で合成データ比較。利用者：許可した実バックアップをアプリで選び、件数と代表FAQを確認。本番コピー比較は未実施 |
| F3 | 保存・復元失敗と範囲外入力：部分更新、画像欠損、永久待機、保存成功の誤表示 | 破損/差替えバックアップ、書込失敗、復元のDB反映後・画像交換途中の停止と再起動で、正常完了か安全な停止・復旧へ至る。元バックアップと安全退避を保持し、DBと画像の不整合状態を通常利用させない。16MiB超・深さ超過は明確な理由と対処を表示し、未保存入力・保存済みデータを保持して次の操作ができる | Codex：合成環境だけで障害注入と既存回帰。利用者：復旧案内が分かることだけ確認。途中停止とサイズ案内を重点補完 |
| F4 | 認証・権限と外部アクセス：他人の権限で保存する、未選択データを漏らす事故 | 既存管理者・一般利用者の認証とログアウトが正しい。管理者専用操作は画面を迂回しても拒否する。復元後の旧セッションを拒否する。WebViewからDB等を読めず、URL確認・パスのコピー専用・メール明示選択の境界を維持する | Codex：既存自動試験を本番向け配線で再確認。利用者：実機の通常認証と一般権限を短く確認。従来全項目の手動再試験は不要 |
| F5 | 日常操作とUI：通常FAQを作れない・探せない・ボタンに届かない状態 | 一つの合成FAQで作成→画像選択/貼付→保存→検索→編集→複製→削除/復元→再起動を通す。表・コピー用テキスト・参考URL、代表CSV/JSON往復、10タブとスクロールも確認する。実際に使う画面サイズ/倍率で重要ボタンとエラーが読め、利用テーマのデザイン・操作性を維持する | Codex：既存回帰と可能な画面確認。利用者：最終実機の操作感。既存合格を再利用して一連操作にまとめる |
| F6 | 実Codex往復：提案が届かない、別FAQを変更する、勝手に公開する事故 | 正式な接続先で、合成FAQの新規案・修正委譲・合成メール委譲を実プラグイン経由で往復する。意図した提案だけが届き、承認までは元FAQ不変、承認後だけ反映、再起動・再読込で重複しない。版競合・画像保持・統合は既存自動回帰も使う。未選択FAQ・メール原本・添付を自動探索しない | Codex：接続実装・既存回帰。利用者の明示依頼と会社規定に従って無害な合成内容だけを委譲。現状は未接続 |
| F7 | 導入・更新・アンインストールと実端末：起動不能、更新/削除によるデータ消失 | ソースのないWindows環境で採用する正式パッケージを導入・起動し、FAQ保存後の更新・再起動・アンインストール・再導入でもデータを保持する。依存ランタイム不足を案内する。配布物に実データを含めない。会社で使い始める前に、そのPCの権限・保護ソフト・保存先・実際の表示倍率で起動と保存/バックアップを確認する | Codex：パッケージと隔離環境の試験。利用者：実際の会社PCで短い確認。C#向け正式配布はこれから |

### 事前条件と合格判定

- 最終候補のバージョンとSHA-256、対象PC・通常利用範囲・保存先・配布方式を固定し、異なる候補の結果を混ぜない。
- F1～F7でデータ消失/不整合、権限迂回、未承認の情報送信、通常利用で保存不能/起動不能、復旧不能が1件でも残れば切替しない。発見した項目と関連回帰だけを修正・再試験する。
- 既存の単体・統合・React回帰、型検査・ビルド、Git/配布混入・文字コード検査は最終候補でも実行する。既存手動結果は証拠として利用し、変更が及ばないものまで全部やり直さない。
- 実バックアップは利用者がアプリで明示選択する。Codexが実SQLite・未委譲FAQ・画像・メール原本を探索して内容を会話へ取り出すことはしない。実データ検査用の専用ローカル比較を追加する場合は対象・出力範囲を先に定め、本文等をログに残さない。
- 障害試験・アンインストール試験は合成データの隔離環境で行い、本番を意図的に壊さない。本番適用は事前退避と利用者の切替承認後に行う。
- F2で実際に使う既存FAQが上限にかかる場合は「特殊だから対象外」で済ませず、そのFAQの保持・分割等を利用者と合意してから移行する。切捨て・黙った省略は禁止する。
- フルバックアップにはCodex提案・委譲ファイルが含まれないため、切替/切戻し前に未処理依頼を完了または保留整理する。DB内の提案受付・承認履歴が復元されても、外部の提案・委譲ファイルは同じ時点へ戻らない。その時点差による二重反映・古い依頼の再承認を防ぐ。

## 4. 今回は拡張・総当たり確認を待たなくてよいもの

| 項目 | 推奨する扱い | 残す最低限の確認 |
|---|---|---|
| 16MiB超の要求を成功させる機能 | 当面は対応対象外として明記する。大きな説明はFAQの分割や既存の外部資料参照を使う | F3の安全拒否・案内・入力保持。通常の許可サイズ画像が通ること |
| 通常JSONの極端な入れ子をRustへ逆輸入 | 極端な構造の完全互換は延期する | 通常の本文/表/画像の往復とF2の切戻しは必要。深さ超過は黙って変換せず拒否する |
| 全解像度×全倍率×全テーマの手動総当たり | 実際に使うPC・倍率・テーマの確認を先行し、未使用組合せは後続へ | 表示崩れで保存・復元・エラー確認ができないものは延期不可。React/CSSと通常UIの非デグレは維持する |
| 10,000件等の未使用規模・全共有先障害の実機網羅 | 実際の件数と利用予定規模を先に確認し、それ以上は後続へ | 現在の規模での検索/保存速度。共有バックアップを利用するなら使用開始前にその共有先と失敗時保護を確認する |
| `.pst`対応 | 現行版にない追加機能としてC#移行と分離する | 既存`.msg`の明示選択・マスク・委譲は維持する |
| JSON衝突の詳しい表示・取込方式、検索の完全一致化 | 利用者との従来合意どおり移行後の別課題 | 現行の管理ID衝突拒否・元データ保持と通常JSON往復は維持する |
| NSISを待たず一人でフォルダー版を使用 | 別途承認した限定試行なら検討できるが、現行の正式配布要件を満たした扱いにはしない | その方式でもF1～F6、実機起動・更新時データ保持・切戻しは必要。会社規定は省略しない |

16MiBはDB全体、FAQ本文だけ、または添付画像の合計の制限ではなく、画面からC#へ送る1要求のJSON全体のUTF-8サイズである。画像選択は専用処理、貼付画像は1件ごとにBase64で送信し、保存本文では管理画像の参照を使う。現行の画像10MiB上限はBase64化すると約13.34MiBであり、通常の画像1件が直ちに16MiBを超える設計ではない。ただし最終候補では実際の要求サイズと成功/拒否を自動確認する。

## 5. 実装・確認の推奨順

1. F1の正式保存先・移行方式・旧新版の排他を実装し、F3のサイズ案内と復元途中停止のリスクを補完する。
2. F2のデータ比較・切戻し、F4の権限/安全境界を同じ本番候補構成で自動確認する。
3. F6の実接続とF7の正式パッケージを完成させる。
4. 最終候補で既存回帰とF1～F7を確認し、人間には実バックアップの代表表示・実機の操作感・会社PC確認を絞って依頼する。
5. 結果と承認した対応範囲を確認してC#正式切替を判断する。未使用の極端な規模だけを理由に、合格した通常利用の移行を際限なく延期しない。

## 6. 調査根拠

計画作成時点で実施したのは設計・ソース・既存試験記録の読み取りレビューである。その後の実装・試験は7節へ追記し、当初レビューと区別する。

- 本番未接続：[MainWindow.xaml.cs](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src-csharp/KnowledgeApp.CSharp/MainWindow.xaml.cs:73)、[FileSystemBoundary.cs](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src-csharp/KnowledgeApp.Data/FileSystemBoundary.cs:23)
- 試作専用起動排他：[SingleInstancePolicy.cs](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src-csharp/KnowledgeApp.CSharp/SingleInstancePolicy.cs:20)
- 容量制限と無応答拒否：[HostRequestEnvelope.cs](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src-csharp/KnowledgeApp.CSharp/HostRequestEnvelope.cs:8)、[MainWindow.xaml.cs](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src-csharp/KnowledgeApp.CSharp/MainWindow.xaml.cs:304)、[csharpBridge.ts](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src/api/csharpBridge.ts:121)
- 復元のDB反映と管理ディレクトリ交換：[KnowledgeDatabase.FullBackup.cs](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src-csharp/KnowledgeApp.Data/KnowledgeDatabase.FullBackup.cs:96)
- プラグイン固定参照先：[get-delegation.ps1](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/codex-plugins/knowledgeapp-faq/scripts/get-delegation.ps1:18)
- 試作ZIPの生成：[New-CSharpTrialPackage.ps1](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/scripts/New-CSharpTrialPackage.ps1:146)

作成時は要件定義書1.3・1.13・1.14、基本設計書4.6・4.6.11、ローカルデータ詳細設計書2.0.2～2.0.6への影響を確認し、設計変更なしとした。採用後は「承認した通常利用範囲の非デグレ」と対象外の明記、既存データ保持・復旧・安全拒否の必須条件を正式文書へ反映している。

## 7. 第16段階：採用後の進捗

人間に過去の段階試験を再実施してもらわず、合成環境で起動・排他・切戻し・復元停止・権限・入出力・配布をまとめて確認する。ソース・テスト生成コードはGit対象、生成データと個人結果はリポジトリ外とする。

| 対象 | 実装・自動化 | 実データ・実機として残す確認 |
|---|---|---|
| F1 | 固定本番候補、既定いいえの起動確認、旧Tauri互換排他、SQLite占有、起動前安全バックアップ、合成既存DB検査 | 利用者が候補で許可した既存環境の起動・代表表示 |
| F2 | C#→Rust→C#の実バックアップ往復、ID・本文・画像・認証・履歴・設定と元アーカイブ保持 | 実際に引き継ぐ件数・代表FAQを利用者がアプリで確認 |
| F3 | 復元・復旧の合成子プロセス停止、再起動巻戻し、破損時停止、要求16MiB/深さ144の送信前拒否・編集保持 | 実機で不明な案内が出た場合だけ報告。故意の停止再試験は不要 |
| F4・F5 | 既存のサービス・権限・安全境界・React回帰を再利用。メール画面をログイン後に限定 | 日常環境の表示倍率・操作感を短い一連操作で確認 |
| F6 | 実PowerShellコマンドの合成委譲・提案往復、UTC日時原文保持、承認前非変更 | 登録済み実プラグインと実会話の接続確認。コマンド試験をこの代替にしない |
| F7 | 検証ペイロードからNSIS生成、隔離導入・更新・削除・再導入、実行中拒否とデータ保持 | 会社PCの実際の権限・保護ソフト・ランタイム・保存とバックアップ |

現在、本番DBやメール原本をCodexが開く作業は行っていない。実プラグインの登録確認ができない実行環境ではソース修正と合成コマンド試験までとし、無断で登録・設定を変更しない。最終実行物と結果が揃ってから、人間に必要な手順だけを別紙へまとめる。

### 第16段階で追加発見した移行前必須課題

C#復元中断後、未更新の旧Tauriを先に起動すると中断記録が無視され、復旧前状態へ新たに書き込む可能性がある。次のC#自動巻戻しでその追加入力が失われるため、通常の切戻しに関係する重大項目としてF1/F3へ追加した。Rustソースの起動前拒否と合成回帰を追加するが、既存インストール済みexeへの反映とは別である。

このため第16段階の候補モードはハード無効化し、本番DB接続前に停止する。引数なしの固定検証版は使用できる。復旧保護を持つ旧版の切戻し入口を確認してから有効化する。現時点で追加の利用者手動テストは依頼せず、既存合格を維持する。

第16段階の自動試験・最終ペイロードの結果は[最終移行自動検証記録](C:/Users/takow/Documents/game_development/program/WebKnowledgeSystem/src-csharp/最終移行自動検証記録.md)を参照する。ソース上の旧版起動ガード10件を含むRust回帰、C#起動84件・復旧315件、相互連携106件、NSIS50件・パッケージ39件等が合格した。通常インストールや本番切替は実行していない。
