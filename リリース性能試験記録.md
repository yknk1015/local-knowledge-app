# KnowledgeApp リリース性能試験記録

## 1. 試験条件

| 項目 | 内容 |
|---|---|
| 実施日 | 2026-08-16 |
| OS | Microsoft Windows 11 Home 10.0.26200 |
| CPU | AMD Ryzen 9 8940HX with Radeon Graphics |
| メモリ | 31.2 GB |
| Rust | rustc 1.97.1 / cargo 1.97.1 |
| Node.js | v24.14.0 |
| ビルド | `cargo test --release --locked` |
| データ保存先 | OS一時フォルダ内の試験専用SQLite。利用中DBとリポジトリ配下には生成しない。 |

試験フィクスチャーは、分類1,000件、公開FAQ 10,000件、検索履歴1,000,000件（うち0件検索100,000件）である。FAQはFTS5索引と検索補助表を含み、`LAN`を`ネットワーク`へ展開する同義語を登録し、展開後の日本語検索結果が10,000件となる条件を設定した。

## 2. 結果

| 対象 | 実測 | 判定基準 | 判定 |
|---|---:|---:|---|
| 分類1,000件の取得 | 8 ms | 原則500 ms以内 | 合格 |
| FAQ 10,000件一致の同義語展開付き日本語検索・重要度順・先頭50件 | 56 ms | 原則1,000 ms以内 | 合格 |
| FAQ詳細取得 | 0 ms（1 ms未満） | 原則500 ms以内 | 合格 |
| 履歴100万件から0件検索10万件を数え、2,000ページ目の50件を取得 | 582 ms | 境界動作とページング完了 | 合格 |
| FAQ 10,000件のCSV書出し | 50 ms | 処理完了、10,000件、約3.24 MiB | 合格 |
| 約190.15 MiBのSQLiteバックアップ | 4,596 ms | 元DBを損なわず完了 | 合格 |

フィクスチャー作成はFAQ・分類が1,126 ms、履歴100万件が9,959 msで完了した。生成後のDBは199,389,184 bytes、CSVは3,400,241 bytesだった。

## 3. 再実行方法

リポジトリルートで次を実行する。

```powershell
./scripts/test-release-performance.ps1
```

試験は`PERF_RESULT`行へ各実測値を出力し、検索1秒、詳細0.5秒、分類0.5秒の基準超過を失敗として返す。実行環境や実装が変わった場合は、その時点の値と判断を本書へ追記または更新する。

## 4. 残る実機確認

- 1280×720表示で検索中、CSV処理中、バックアップ中に操作不能と誤認しない表示になっていること。
- 会社PCのウイルス対策・ストレージ・ネットワークバックアップ先を含む処理時間。
- Excelで10,000件CSVを開いた場合の操作性。通常運用で一度に扱う件数を会社運用手順へ反映する。
