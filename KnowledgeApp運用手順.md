# KnowledgeApp運用手順

### C# 0.7.3をインストールせず使う

1. 配布された`KnowledgeApp.CSharp.exe`をPCの専用ローカルフォルダーへコピーして起動します。バックアップやCodex連携先は、このフォルダーとは別に選んでください。配布用のソース・UIフォルダー・DLLを別にコピーする必要はありません。
2. .NETはexeに内蔵しています。WebView2 RuntimeはPCに必要です。不足時は画面の案内に従って導入環境を確認してください。
3. 初回は実行用ファイルを利用者別の一時キャッシュへ展開するため、2回目以降より時間がかかります。保存済みFAQは従来どおり`%LOCALAPPDATA%/jp.local.webknowledgesystem.csharp`に残り、同じWindows利用者ならインストーラー版のデータを引き続き利用します。ログインも従来のID・パスワードを使います。
4. 更新はアプリを終了してexeを差し替えます。exeを移動・削除してもFAQは消えません。別PCへFAQを引き継ぐ場合は、既存のフルバックアップ・復元手順を使用します。

0.7.2は起動準備を短縮し、インストール不要の配布方法を追加した版です。既存の分類・FAQ・認証・バックアップ形式は変更していません。今回の更新確認は起動、ログイン、代表FAQ表示、保存後の再起動、バックアップを中心とし、以前の全手動試験を繰り返す手順にはしません。

0.7.0で管理者・FAQ編集者・閲覧のみの3役割、用途別保存先設定、Windows 11共有サーバーへの接続を追加しました。単独利用は従来のC#専用保存先を維持します。共有導入・既存データ引継ぎは[共有サーバー導入手順](共有サーバー導入手順.md)を参照してください。DBをNASへ直接置かず、PC内のサービスからアクセスします。実環境への自動移行やサービス登録は行いません。

## 1. 対象と前提

現行文書の案内は[文書整理ガイド](文書整理ガイド.md)、対象PC等の未確認事項と再開条件は[今後の課題](今後の課題.md)へ集約する。旧版の試験書は履歴であり、本書の代わりに古い試作版の起動コマンドを実行しない。

2026-09-06、利用者から今回のPCでの復元・確認完了を受領した。C#を正式主系とし、今回の復旧キー対応版は0.6.0とする。既存の0.5.0切替完了と今回更新の確認は区別する。以下の初回引継ぎ手順は未移行のPC向けで、移行済みPCに再度の復元を求めるものではない。

本書はKnowledgeApp C# `0.7.3`をWindows 11 64ビットの対象PCで利用するための初回設定、日常利用、バックアップ、更新時の手順を示す。2026-09-19以降はインストール不要の単一exeを通常配布とする。利用環境の規定、情報セキュリティ規程、配布担当者の指示が本書と異なる場合は利用環境の規定を優先する。

2026-09-06に少量FAQの利用者承認を受け、今後の開発・通常利用をC#へ切り替える。旧Tauriから初めて引き継ぐ場合は、下記初期設定より先に[正式切替ガイド](src-csharp/CSharp正式切替ガイド.md)を参照する。C#は`LocalApplicationData/jp.local.webknowledgesystem.csharp`を正本とし、旧版の保存先と同期しない。引継ぎは正規フルバックアップをアプリで明示復元し、復元後は元のID・パスワードを使用する。対象環境での実行許可・保存先・画面倍率の確認は別に必要である。

2026-08-16付で、利用者本人だけの対象PC一人利用試行と、複数人への正式配布を分離した。一人利用開始前は対象コミット、起動、利用者データ分離、通常認証・復旧キー、ローカルバックアップ・復元、Git混入防止を最低確認とする。コード署名、正式配布経路、広範な実機・表示・ネットワーク確認は、複数人利用または正式配布へ進む前に再開する。

KnowledgeAppのSQLite DBは各Windows利用者のローカルデータフォルダへ置く。ネットワークドライブや共有フォルダへDBを直接置いて複数PCから共用しない。ネットワークドライブはフルバックアップの保存先、またはアプリ外で管理する外部資料の参照先としてだけ使用する。

## 2. exeの配置と初回設定

1. 配布担当者から指定された`KnowledgeApp.CSharp.exe`を取得し、版、SHA-256、署名状態を配布案内と照合する。
2. exeを専用のローカルフォルダーへコピーする。アプリのインストールは不要で、.NETは内蔵する。対象PCにはWebView2 Runtimeが必要。バックアップ・Codex連携先はexeの配置先と分ける。
3. exeをダブルクリックして起動する。任意でこのexeへのショートカットを作成できる。初回は同梱ファイルの展開を待つ。
4. 既存C#データがある場合は現在のID・パスワードを使う。新規のデータ領域だけログインID`0000`、パスワード空欄で開始する。
5. 「利用者の管理」で初期管理者の「パスワード再設定」を選び、利用環境の規定に沿うパスワードへ変更する。
6. 必要な利用者を追加し、管理者・FAQ編集者・閲覧のみの権限を設定する。共有アカウントの利用可否は利用環境の規定に従う。
7. 「設定・情報」で「空欄のパスワードを許可する」をOFFにする。既に空欄で登録済みの利用者は自動変更されないため、個別にパスワードを再設定する。

8. 「フルバックアップを作成」で初回バックアップを作成し、KnowledgeAppをインストールしたPCだけに依存しない安全な保存先へ保管する。

パスワード、秘密鍵、個人情報、利用環境の規定で保存を禁止された情報はFAQへ登録しない。

### 管理者の復旧キー（0.6.0）

管理者でログインし「設定・情報」の「管理者の復旧キー」で現在のパスワードを入力すると発行できます。英数字16文字（4文字区切り、表示19文字）のキーは発行直後だけ表示します。紙やパスワード管理アプリなど、FAQやソースと別の安全な場所に保管してください。初回案内はスキップできますが、キーも他の管理者も使えない場合はアクセスを復旧できなくなることがあります。既存利用者のキーは未発行のまま移行します。

紛失時はログイン画面の「パスワードを忘れた場合」から管理者IDとキーを入力し、新しいパスワードを2回入力して確定します。新しいキーを保管して閉じ、新パスワードで通常ログインしてください。一般利用者は管理者に再設定を依頼してください。通常パスワード変更・利用停止でも古いキーは無効になるため、変更後に再発行します。初期空パスワードを変更した直後も同様です。

固定共通の緊急ログインは廃止しました。DB第7版は更新前のフル安全バックアップ作成後に第8版へ移行します。既存FAQ・利用者・通常パスワードは保持します。過去のバックアップ復元では当時のパスワードやキーへ戻り得るため、必要に応じて再設定・再発行してください。第8版DBを旧アプリで使うことは保証しません。

## 3. FAQの登録・検索

- FAQは1つの分類へ所属させ、別の観点はタグで表す。分類は最大5階層である。必要なタグは先に共通メニューの「タグマスター」で登録し、FAQ作成・編集画面のプルダウンから選択する。
- 新しいFAQでは個別の検索情報と関連FAQを入力しない。関連FAQの新規編集は将来候補であり、旧版から残る検索情報・関連FAQは互換データとして自動保持される。
- 概要は最初に結論が分かる短い文とし、回答は必要な手順、注意、備考、参考リンクの順で整理する。
- 「症状」「原因」「対象」「エラーコード」「対応手順」「注意事項」「検索用語」を必要に応じて入力すると、自然文や表記違いで見つけやすくなる。
- 公開前は下書きで確認し、利用者に見せる内容だけを公開する。廃止、非表示、論理削除は用途が異なるため、表示理由に合わせて選ぶ。
- 検索では検索語、分類範囲、下書き表示、更新日・重要度の並び順を指定できる。検索結果は50件単位で表示される。
- 同じ意味の表記は「同義語の管理」でグループ化する。同じ語を複数グループへ重複登録しない。
- 「検索・閲覧履歴」では利用状況や0件検索を確認できる。履歴削除は対象期間・条件と件数を確認してから実行する。

## 4. 外部資料の参照

- HTML、Excel、PDF、Wordなどの資料本体は外部管理のディレクトリまたはWebサーバーへ置く。KnowledgeAppへ取り込まない。
- ローカルパス、ドライブ文字付きパス、UNCパスは回答内のコピー用テキスト枠に記載する。KnowledgeAppは存在確認、自動補完、自動オープンを行わない。
- 複数PCから同じ資料を参照する場合は、PCごとに異なるドライブ文字より、管理担当者が管理するUNCパスを優先できるか管理担当者へ確認する。
- Web資料はHTTP/HTTPS参考URLだけを使用する。表示文字、接続先ホスト名、完全なURLを確認してから既定ブラウザーで開く。
- 外部資料のアクセス権、更新、ウイルス対策、リンク切れ、可用性、資料本体のバックアップは資料管理者側で管理する。
- KnowledgeAppの検索・フルバックアップ・JSON・CSVには外部資料本体を含めない。FAQに保存した参照先文字列と参考URLはFAQデータとして扱う。

## 5. バックアップと復元

管理者が「設定・情報」から操作する。

### バックアップを作る時期

- 本格利用開始前
- アプリの更新・上書きインストール前
- CSVまたはJSONの取込前。取込処理でも直前に安全バックアップが自動作成される。
- 分類整理やFAQ大量更新の前
- 運用方針で定めた定期周期

「フルバックアップを作成」で保存先とファイル名を選ぶ。同名ファイルの上書きは内容を確認した場合だけ行う。ネットワーク保存先を使用するときは、接続、書込権限、空き容量を確認する。処理が失敗した場合、途中ファイルを正式バックアップとして扱わない。

定期的に、業務データを含めてよい専用PCまたは別の許可された保存先へ復元し、FAQ件数、分類、画像、設定、検索が期待どおりであることを確認する。実データを開発用リポジトリ、GitHub、メール、許可されていないクラウドへ移さない。

### 復元手順

1. 「設定・情報」→「バックアップから復元」を選ぶ。
2. `*.faqbackup`を選び、アプリが表示する形式、作成日時、件数を確認する。
3. 「この内容を復元」を選ぶ。現在の状態は復元直前にPC内の安全バックアップへ自動退避される。
4. 完了後、FAQ、分類、画像、設定、利用者、検索を確認する。

破損、形式不一致、空き容量不足、書込権限不足が表示された場合は復元を続行せず、別の検証済みバックアップまたは保存先を使用する。

## 6. CSV・JSONによる受け渡し

### CSV

1. 「FAQの管理」→「CSVエクスポート」で最新の`*.knowledge-faq.csv`を書き出す。
2. Excelで開き、見出し・列順・形式版・管理ID・回答本文ハッシュを削除または並べ替えずに編集する。
3. 数式として実行させたい値をCSVへ入れない。書出し時は式注入防止処理が行われる。
4. 「FAQの管理」→「CSVインポート」でファイルを選び、全行の新規・更新・変更なし・警告・エラーを確認する。
5. エラーが1件でもある場合はCSVを修正して選択し直す。確認後に一括反映する。

CSVは添付画像、リッチテキスト構造、利用者設定などを完全移行する形式ではない。完全な移行・災害復旧にはフルバックアップを使う。

### JSON

「FAQの管理」→「JSON入出力」は分類、FAQ、タグ、同義語、関連・統合情報の構造的な受け渡しに使用する。添付画像本体、利用者、検索・閲覧履歴、アプリ外で管理する外部資料本体は含まれない。インポート時は形式と全件を検査し、直前バックアップ後に一括反映する。

CSV・JSONはFAQ内容を含むため、ソースフォルダ、Git管理フォルダ、リリース成果物へ置かない。

## 7. 更新・アプリ本体の削除

1. 更新前に現行版と対象版を記録し、フルバックアップを作成する。
2. 配布担当者が指定した新しいexeの版、SHA-256、署名状態を確認する。
3. KnowledgeAppを終了してexeを差し替える。補助方式のNSIS版を利用している場合は、そのインストーラーで更新する。
4. 起動後、ログイン、FAQ件数、分類、検索、画像、設定、CSVプレビュー、バックアップ作成を確認する。
5. DB移行エラーが表示された場合は繰り返し起動せず、表示された原因・対処と移行前安全バックアップを保全して担当者へ連絡する。

単一exeを使わなくなった場合は、アプリを終了してexeと任意で作成したショートカットを削除する。FAQ・認証・設定は固定のLocalApplicationDataに残る。NSIS版の削除はそのアンインストール手順に従う。利用者データを完全削除する必要がある場合は、対象パスとバックアップの要否を確認したうえで利用環境のデータ廃棄手順に従う。

## 8. 障害時の連絡情報として残すもの

- 発生日時、アプリ版、Windows版
- 画面に表示された日本語エラー番号、原因、対処
- 実行していた操作と再現手順
- バックアップ名、作成日時、保存先。FAQ本文やパスワードは転記しない。
- 更新時は更新前版、更新後版、配布exeのSHA-256

FAQ・データベース・履歴・添付画像・バックアップ・診断ログなどの利用者データと、パスワード・APIキー・トークン・秘密鍵・緊急認証情報は、公開・非公開を問わずGitHubを含むGitリポジトリへ絶対にコミットしない。文書・ソース・画像への転記も禁止する。問い合わせ時に外部サービスへ提示する情報は必要最小限とし、利用環境の規定と担当者の許可を確認して機密情報・個人情報・認証情報を除去する。
