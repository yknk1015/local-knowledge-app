# KnowledgeApp 0.4.1 Surface実機リハーサル手順

> 本手順は`Surface実機テスト計画書_0.4.1.md`に基づく。0.3.3・0.3.4の部分結果は流用せず、P0を最初から実施する。

## 1. パッケージと端末の確認

1. ZIP外側の`.sha256`をメモ帳で確認し、PowerShellでZIPのSHA-256と照合する。
2. ZIPをSurfaceのローカルフォルダへ展開し、展開先で次を実行する。

   ```powershell
   ./verify-surface-release-package.ps1
   ```

3. `PASS: Surface検証パッケージは記録どおりです。`と表示されることを確認する。
4. 実行ポリシーでスクリプトを実行できない場合は回避せず、`Get-FileHash`とファイルのプロパティでインストーラー3件とZIPを個別確認する。
5. 結果票の端末情報を記入する。パスワード、会社FAQ、個人情報、会社の機密パスは記入しない。

## 2. 0.2.0の新規導入

1. 表示倍率を100%にする。
2. `KnowledgeApp_0.2.0_x64-setup.exe`を利用者単位でインストールする。
3. ショートカットから起動し、コマンド入力なしで画面が開くことを確認する。
4. 分類`Surface検証 > ネットワーク`を作成する。
5. 次の公開FAQを作成する。

| 項目 | 合成データ |
|---|---|
| タイトル | `Surfaceでネットワーク接続を確認するには？` |
| 概要 | `Windowsの設定画面で接続状態を確認します。` |
| 回答 | `■確認方法`、`1. Windowsの設定を開きます。`、`2. ネットワークとインターネットを確認します。` |
| 重要度 | 2 |

6. `knowledgeapp-test-image.png`を選択して追加し、代替テキストを`KnowledgeApp試験用アイコン`として保存する。
7. 検索結果と詳細画面でFAQと画像を確認する。
8. `Surface-0.2.0-before-update.faqbackup`をSurface内の試験用バックアップ先へ作成し、アプリを終了する。

## 3. 0.3.4への更新と旧互換データの作成

1. `KnowledgeApp_0.3.4_x64-setup.exe`で上書き更新する。
2. 起動し、ID`0000`・空パスワードでログインする。
3. 0.2.0の分類、FAQ、回答、画像、公開状態が保持されていることを確認する。
4. 初期管理者のパスワードを試験用の値へ変更し、「空欄のパスワードを許可する」をOFFにする。値は結果票へ書かない。
5. 既存FAQを編集し、旧画面で次を登録する。

| 旧項目 | 合成データ |
|---|---|
| 症状 | `接続できない` |
| 原因 | `接続設定` |
| 対象 | `Surface` |
| エラーコード | `NET-TEST-01` |
| 対応手順 | `設定画面を確認する` |
| 注意事項 | `試験用データだけを使用する` |
| タグ | `ネットワーク` |
| 検索用語 | `ネットワーク確認` |

6. 2件目の公開FAQ`SurfaceでWi-Fiを再接続するには？`を作成し、最初のFAQと関連FAQに設定する。
7. 最初のFAQの詳細で旧検索情報、タグ、関連FAQが表示されることを確認する。
8. アプリを終了・再起動し、変更後パスワードだけでログインできることを確認する。旧空パスワードが成功した場合は即時中止する。
9. `Surface-0.3.4-before-update.faqbackup`を作成してアプリを終了する。

## 4. 0.4.1への更新とP0回帰

1. `KnowledgeApp_0.4.1_x64-setup.exe`で上書き更新する。
2. 変更後パスワードでログインし、旧空パスワードが無効なままであることを確認する。
3. 旧分類、2件のFAQ、画像、状態、管理ID、作成者・更新者、設定が保持されていることを確認する。
4. FAQ編集画面が「基本情報」「回答」「タグ」「公開・表示設定」の4区分であることを確認する。
5. 「検索情報」と関連FAQの追加・削除・候補検索欄が表示されないことを確認する。
6. タグ区分がタグマスターのプルダウン選択であり、自由入力欄でないことを確認する。
7. 既存FAQの概要末尾へ`0.4.1確認済み`を追加し、タグは変更せず保存する。
8. 詳細画面で、0.3.4で登録した症状、原因、対象、エラーコード、対応手順、注意事項、検索用語、関連FAQが消えていないことを確認する。
9. 画像を含む既存FAQをもう一度保存し、`ATT-004`が表示されず、保存直後と再起動後に画像が表示されることを確認する。
10. 新しいFAQへ画像をファイル選択と貼り付けの各方法で追加し、それぞれ保存する。`ATT-004`が表示されず、保存直後と再起動後に表示できることを確認する。
11. 10で作成したFAQを公開し、検索結果と詳細画面の両方で画像を確認する。

## 5. タグマスター

1. 共通メニューの「タグマスター」を開き、0.3.4で登録した`ネットワーク`と使用件数を確認する。
2. 新規タグ`ディスプレイ`を追加する。
3. `ＤＩＳＰＬＡＹ`など正規化後に同じになるタグを登録し、重複エラーになることを確認する。
4. FAQ編集画面で`ディスプレイ`をプルダウンから追加し、`ネットワーク`と複数選択して保存する。
5. タグマスターで`ディスプレイ`を`画面表示`へ変更し、FAQ詳細と検索結果へ反映されることを確認する。
6. 使用中の`画面表示`を削除し、使用件数と先にFAQから外す必要がある旨を表示して拒否することを確認する。
7. FAQから`画面表示`を外して保存し、使用件数0件を確認して削除する。
8. 未使用タグ`削除確認用`を追加し、確認ダイアログ後に削除できることを確認する。

## 6. 検索・履歴・外部資料

1. 同義語`パソコン`／`PC`を登録する。
2. `PC ネットワーク`、`NET-TEST-01`、変更後のタグ名で検索し、旧互換情報とタグが検索索引に残っていることを確認する。
3. 「現在地以下」「現在地のみ」「すべて」、50件ページング、更新日・重要度の各並び替えを確認する。
4. 検索履歴、0件検索、閲覧履歴、期間・件数絞り込みを確認する。
5. `external-reference-test.html`の絶対パスを回答のコピー用テキスト枠へ登録する。
6. 詳細画面でコピーし、メモ帳へ貼り付けて完全一致することと、KnowledgeAppがパスを自動実行しないことを確認する。
7. コピーしたパスを利用者自身がブラウザーへ貼り付け、合成HTMLが表示されることを確認する。
8. `https://example.com/`を参考URLとして設定し、表示名、接続先、完全URLの確認後だけ既定ブラウザーで開くことを確認する。

## 7. JSON・CSV・10,000件

1. `six-digit-management-id.fixture.json`を`test-six-digit.knowledge-export.json`へコピーし、JSONインポートする。
2. 分類1件、FAQ1件、エラー0件を確認し、`FAQ-100000`と`CAT-100000`が通常幅・900px以下で欠けないことを確認する。
3. FAQ CSVを`Surface-base.knowledge-faq.csv`へエクスポートし、Excelで日本語、17列、管理IDを確認する。
4. 概要だけを変更して保存し、CSVプレビュー、変更対象、取込前安全バックアップを確認して反映する。
5. 次を実行して10,000件の合成CSVを作成する。

   ```powershell
   ./New-KnowledgeAppLargeCsvFixture.ps1 `
     -BaseCsv ./Surface-base.knowledge-faq.csv `
     -DestinationCsv ./Surface-10000.knowledge-faq.csv `
     -RowCount 10000
   ```

6. プレビューと取込の開始・終了時刻を記録し、処理中表示、完了件数、エラー件数を確認する。
7. 取込後の検索、50件ページング、FAQ詳細、タグ検索を操作する。

## 8. バックアップ・復元・単一起動

1. 10,000件を含むフルバックアップをローカルへ作成し、時間とサイズを記録する。
2. バックアップ中にショートカットからもう一度起動し、別ウィンドウ・別プロセスが増えないことを確認する。
3. FAQを1件変更し、ローカルバックアップから復元する。復元前安全バックアップが作られ、変更前へ戻ることを確認する。
4. 会社規定で許可されたネットワーク先がある場合だけ、書込、切断、権限不足、容量不足を確認する。許可先がなければ未実施とする。

## 9. 表示・操作・再インストール

1. 100%、125%、150%の表示倍率で、1280×720以上の主要画面、FAQ編集4区分、タグマスター、FAQ管理を確認する。
2. 900px以下の幅で管理ID、タグ名、操作ボタンに欠け・重なりがないことを確認する。
3. 緑・青テーマ、Tab／Shift+Tab、Enter、Space、フォーカス表示、エラー通知への移動を確認する。
4. KnowledgeAppをアンインストールし、利用者データが自動削除されないことを確認する。データフォルダを手動削除しない。
5. 0.4.1を再インストールし、変更後パスワード、FAQ、画像、タグ、旧互換情報、設定、10,000件が保持されることを確認する。

## 10. 結果返却

`Surface実機試験結果_0.4.1.md`を記入して返却する。DB、バックアップ、CSV、JSON、履歴、パスワードは返却しない。不合格時は合成データだけが見える証跡とエラー番号を添える。

